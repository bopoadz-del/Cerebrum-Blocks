# FastAPI & Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
aiofiles>=23.2.0
python-dotenv>=1.0.0

# Shared infrastructure — Redis clients for cache_manager and session/memory blocks
redis>=5.0.0

# Marketplace security — publisher identity and Ed25519 block signing
cryptography>=42.0.0

# HTTP Client
aiohttp>=3.9.0
httpx>=0.25.0
requests>=2.25.0

# Web Scraping
beautifulsoup4>=4.12.0
ddgs>=1.0.0

# Image Processing
pillow>=10.1.0

# Vector Search / RAG — local embedding model required for pgvector ingestion
# and retrieval. sentence-transformers pulls torch; installs cleanly on Linux
# and Windows with pip.
# chromadb>=0.4.18
sentence-transformers>=2.2.2
numpy>=1.24.0

# OCR
# easyocr>=1.7.0
pytesseract>=0.3.10

# PDF Processing
pymupdf>=1.27.0
pdfplumber>=0.11.0
pypdf>=5.0.0

# Construction Intelligence Blocks (Week 1)
sympy>=1.12
pandas>=2.0.0
openpyxl>=3.1.0

# Sandboxed code execution for formula_executor_v2 / reasoning engine
RestrictedPython>=6.0

# Construction Domain Blocks (Week 2)
ezdxf>=1.1.0
shapely>=2.0.0

# Intelligence Blocks (Week 3)
pint>=0.23
ifcopenshell>=0.7.0

# Intelligence Blocks (Week 4)
scikit-learn>=1.4.0

# Domain formula backends — Library Container (Layer 4)
CoolProp>=6.5            # thermodynamic / HVAC property lookups
QuantLib>=1.30           # quantitative finance — PyPI name is `QuantLib` (imports as `import QuantLib`)

# ML experiment tracking — used by app/core/mlflow_tracker.py.
# No-ops gracefully when MLFLOW_TRACKING_URI is unset.
mlflow>=2.10.0

# PostgreSQL async driver — activates the Postgres backend for the
# historical_benchmark block when DATABASE_URL is set; otherwise the block
# falls back to its file-backed store.
asyncpg>=0.29

# Document parsing — promoted from optional after real-world tests
# showed DOCX uploads silently producing zero text and BOQ tables in
# PDFs being missed without table-aware extraction.
python-docx>=1.1.0       # Word documents (.docx) — used by app/blocks/pdf.py
pdfplumber>=0.11.0       # PDF table-aware extraction — better at BOQ tables than text-only

# Optional - domain libraries (install as needed)
# spacy>=3.7.0           # NLP for spec_analyzer
# reportlab>=4.1.0       # PDF generation
# statsmodels>=0.14.0    # statistical modeling
# xgboost>=2.0.0         # gradient boosting
# polars>=0.20.0         # fast DataFrames

# AI/ML APIs - Optional (uncomment if needed)
# openai>=1.3.0
anthropic>=0.40.0
# groq>=0.5.0

# Translation - Optional (uncomment if needed)
# googletrans>=4.0.0
# deepl>=1.16.0

# Voice
SpeechRecognition>=3.10.0
gTTS>=2.5.0

# Translation
deep-translator>=1.11.0

# Microsoft OneDrive - Optional (uncomment if needed)
# msal>=1.25.0

# Google Drive - Optional (uncomment if needed)
# google-auth>=2.23.0
# google-auth-oauthlib>=1.1.0
# google-auth-httplib2>=0.2.0
# google-api-python-client>=2.108.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-json-report>=1.5.0
respx>=0.21.0
python-magic>=0.4.27

# High-quality PDF -> Markdown extraction is optional and local-only.
# See requirements-marker.txt. The platform base image does NOT install it
# because it pulls torch, transformers, and ~1.35 GB of model weights.
# If not installed, the `marker` block returns a helpful error and callers
# can fall back to the generic `pdf` block.

# Image processing (capture, OCR, image blocks)
opencv-python-headless>=4.9.0
mcp>=1.6.0

# Error tracking — optional. Activated when SENTRY_DSN is set.
sentry-sdk[fastapi]>=2.0.0

# Prometheus metrics exposition — used by app/routers/metrics.py to
# expose /metrics in the Prometheus text format and to record per-request
# counters/histograms via the metrics middleware.
prometheus_client>=0.19
