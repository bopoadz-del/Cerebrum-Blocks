# Finance Domain Blocks

Placeholder for domain-specific block modules.

Add block implementations here, list block IDs in ``manifest.json`` ``blocks``,
and map install paths in ``artifacts`` when the kit is published to ``bundle/``.
