Store Inventory Survey

- 

- 

- 





CerebrumDev.ai · Factory & Store

Store Inventory Survey

A read-only pass across every product repository and the Store's current 108‑block registry: what's worth cloning in, what's already stocked but behind its own donor repo, and which outside enterprise systems the Store still has no connector for.


16
Repos scouted


12
Block clusters audited


6
Connector topics


59
Clone candidates


60
Store upgrades


48
Connector targets








Headline finding

The Cerebrum repo is carrying real, working capability the Store doesn't have

One donor repo alone — not yet mined at all — accounts for 5 of the 59 confirmed clone candidates, independently verified against the actual source (not docstrings): live OAuth2 connectors to Procore, QuickBooks, Salesforce, DocuSign, Box and Microsoft 365; a real SAML/SCIM enterprise-SSO implementation; a 536-line BIM clash-detection engine with real geometric intersection math; a buildingSMART-standard BCF exporter; and an LLM-driven self-healing patch pipeline. None of it is duplicated anywhere in the Store's 108 registered blocks.


- VDC/BIM Clash Detection + Spatial Index Engine — backend/app/vdc/clash_detection.py

- BCF (BIM Collaboration Format) Exporter — backend/app/vdc/bcf_export.py

- Self-Healing Patch Generation + Hot-Swap Deployment — backend/app/healing/patch_generation.py, backend/app/healing/root_cause.py, backend/app/hotswap/

- Enterprise Connector Suite (Procore, QuickBooks/Xero, Salesforce/HubSpot, DocuSign, Box/Dropbox, MS365, Slack, Zapier) — backend/app/integrations/{procore,accounting,crm,esignature,file_storage,microsoft_365,slack,zapier,erp}.py

- SAML 2.0 / SCIM Enterprise SSO — backend/app/enterprise/sso_saml.py, backend/app/enterprise/scim.py






Clone candidates (59)
Store upgrades (60)
Connector targets (48)
Methodology



Clone into the Store
Clone candidates


Real, substantive capability sitting in a product repo with no equivalent among the Store's blocks — grouped by donor repo.



repo Â· Cerebrum 5




VDC/BIM Clash Detection + Spatial Index Engine
536-line real implementation, not a stub


Cerebrum · backend/app/vdc/clash_detection.py

536-line real implementation, not a stub. Implements an AABB (axis-aligned bounding box) spatial-index tree (AABBTree/AABBNode, lines 174-259) built by recursively splitting elements along the longest bbox axis, real geometric intersection-volume math in _calculate_intersection (lines 485-517: computes min/max overlap on each axis, rejects if min_x >= max_x etc., derives volume/center/penetration depth), a rule engine (ClashRule/_rule_applies) that matches IFC element types + disciplines (Structural vs Architectural, MEP vs MEP) to hard-clash/clearance-violation rules, and both sequential and ThreadPoolExecutor-parallel pair-checking (_check_pairs_parallel, lines 390-410) for scale. Cross-referenced: none of the Store's 108 registered blocks or the ~140 app/blocks/ files include any BIM/VDC/construction-clash vertical.

RecommendationExtract vdc/clash_detection.py + federated_models.py (the ModelElement/BoundingBox/Point3D dependency it imports) as a new Store block, e.g. app/blocks/vdc_clash_detection.py, exposing detect_clashes(elements, rules) via the standard execute() contract. This would be a genuinely new vertical for the Store, not a duplicate of any existing block.






BCF (BIM Collaboration Format) Exporter
419 lines implementing the real buildingSMART BCF 2.1/3.0 open standard: BCFExporter._create_bcfzip (line 298) builds an actuâ€¦


Cerebrum · backend/app/vdc/bcf_export.py

419 lines implementing the real buildingSMART BCF 2.1/3.0 open standard: BCFExporter._create_bcfzip (line 298) builds an actual zip archive with zipfile.ZipFile(..., zipfile.ZIP_DEFLATED) containing a correctly-structured project.bcfp, bcf.version, and per-topic markup.bcf + .bcfv viewpoint files, each generated via real xml.etree.ElementTree serialization matching the BCF XML schema (camera position/direction/up, VisualizationInfo Guid, etc. -- see BCFViewpoint.to_xml, lines 40-80). This is consumed by clashes produced from clash_detection.py, so it's a working export path, wired into api/v1/endpoints/vdc.py (imports BCFExporter at line 14). No equivalent exists among the Store's blocks.

RecommendationExtract as a Store block (e.g. vdc_bcf_export) alongside the clash-detection block; also consider digital_handover.py's COBieExporter (real COBie facility-data export standard) as a companion block for VDC handover workflows.






Self-Healing Patch Generation + Hot-Swap Deployment
healing/patch_generation.py (449 lines) makes a real AsyncOpenAI call (self.client.chat.completions.create, lines 85-109) wâ€¦


Cerebrum · backend/app/healing/patch_generation.py, backend/app/healing/root_cause.py, backend/app/hotswap/

healing/patch_generation.py (449 lines) makes a real AsyncOpenAI call (self.client.chat.completions.create, lines 85-109) with a structured system prompt instructing GPT-4 to analyze a stack trace (ErrorEvent) and produce a targeted code patch, parsing the response into original/patched code + confidence score (PatchResult). This is paired with hotswap/dynamic_import.py, migration.py, rollback.py, route_registration.py -- a live code hot-swap/rollback subsystem -- and healing/circuit_breaker_auto.py for automatic isolation. Together this is a self-remediation pipeline (detect error -> LLM-generate patch -> hot-deploy -> rollback on failure), which is meta/platform-level capability, not present anywhere in the Store's 108 blocks or listed app/blocks/ files.

RecommendationThis is infrastructure rather than a domain vertical, so it maps less cleanly to a single Store block; consider it a candidate for a new 'platform ops' block category (e.g. self_healing_patch_gen, hotswap_deploy) if the Factory wants auto-remediation as a reusable capability across client platforms.






Enterprise Connector Suite (Procore, QuickBooks/Xero, Salesforce/HubSpot, DocuSign, Box/Dropbox, MS365, Slack, Zapier)
These are real OAuth2 + REST client implementations against actual vendor endpoints, not mocks: procore.py's `ProcoreService.exâ€¦


Cerebrum · backend/app/integrations/{procore,accounting,crm,esignature,file_storage,microsoft_365,slack,zapier,erp}.py

These are real OAuth2 + REST client implementations against actual vendor endpoints, not mocks: procore.py's ProcoreService.exchange_code (lines 186-242) POSTs to https://login.procore.com/oauth/token and _get_user_info/_get_companies/get_projects/sync_projects call real https://api.procore.com/rest/v1.0/... endpoints with bearer-token auth and token-refresh handling; accounting.py has a XeroService hitting https://api.xero.com/api.xro/2.0; crm.py has a HubSpotService hitting https://api.hubapi.com; file_storage.py has BoxService (https://api.box.com/2.0) and DropboxService (https://api.dropboxapi.com/2); esignature.py's DocuSignService targets the DocuSign demo REST API. All are wired live into api/v1/endpoints/integrations.py (imports ProcoreService at line 12, mounts /procore/auth, /procore/callback, /procore/sync/projects routes). Caveat: sub-sync methods like _sync_rfis fetch real data but the persistence step is a for rfi in rfis: pass placeholder (procore.py line 427-429) -- the OAuth/fetch layer is real, the write-back-to-Cerebrum-models layer is not finished.

RecommendationExtract each integration as its own connector block (or one parameterized connector block with a provider registry) -- this is exactly the external-enterprise-integration capability described as missing/needed; none of the Store's 108 blocks implement live OAuth2 flows against Procore/QuickBooks/Salesforce/DocuSign/Box/MS365, so this is new capability, not a duplicate.






SAML 2.0 / SCIM Enterprise SSO
sso_saml.py imports and uses the real onelogin.saml2 library (from onelogin.saml2.auth import OneLogin_Saml2_Auth, line 19)â€¦


Cerebrum · backend/app/enterprise/sso_saml.py, backend/app/enterprise/scim.py

sso_saml.py imports and uses the real onelogin.saml2 library (from onelogin.saml2.auth import OneLogin_Saml2_Auth, line 19) with a full SAMLProvider DB model capturing idp_entity_id/idp_sso_url/idp_x509_cert/sp_acs_url etc. (lines 27-50) supporting okta/azure_ad/google IdP types -- this is genuine SAML SP implementation, not a stub. Wired into api/v1/endpoints/enterprise.py (imports SAMLService at line 22).

RecommendationExtract as an enterprise-auth Store block (sso_saml_connector) usable by any Factory-generated client platform needing enterprise SSO -- none of the described 108 blocks cover SAML/SSO.







repo Â· Cerebrum-FinanceOps 3




FP&A Planning engine (budgets, forecasts, workforce cost, cost allocations, scenario-driven forecast adjustment)
Real domain logic, not a thin wrapper: lock_budget creates immutable versions instead of mutating locked rows (update_budget liâ€¦


Cerebrum-FinanceOps · backend/app/planning/service.py (626 lines) + backend/app/planning/models.py

Real domain logic, not a thin wrapper: lock_budget creates immutable versions instead of mutating locked rows (update_budget lines 143-160, budget.version + 1 on a locked budget); calculate_workforce_cost (line 283) does Decimal(headcount)*fte*salary_per_fte*(1+benefits_rate) quantized to 4dp; process_allocation_run (lines 393-454) allocates a total across active dimension members with base_amount = (total/count).quantize(...) and explicitly routes the rounding remainder to the first member 'to keep the total exact'; apply_scenario_to_forecast (lines 577-626) picks the most period-specific ScenarioAssumption and applies it as a rate multiplier to a forecast amount. None of this (budgets/forecasts/workforce/allocations/scenarios) is one of the 9 blocks in the vendored finance_ops kit (finance_canonical_model, finance_coa_governance, finance_data_quality, finance_import, finance_ops, finance_reconciliation, finance_saas_metrics, finance_v2, formula_executor_v2) nor named among the ~140 app/blocks/ extras listed in the task context.

RecommendationExtract as a new Store block family, e.g. finance_planning (budget/forecast versioning + workforce costing) and finance_allocation (driver-based allocation runs) and finance_scenario (assumption-driven forecast adjustment). The remainder-distribution allocation math and the immutable-version-on-lock pattern are the reusable core logic worth lifting into app/blocks/*.py; the SQLAlchemy models/tenant plumbing stays product-specific.






Fail-closed governance approval gate + SHA-256 hash-chain evidence ledger
require_approved_action raises HTTP 403 unless the latest ApprovalRequest for that (tenant, action_type, target_id) has status=â€¦


Cerebrum-FinanceOps · backend/app/governance/service.py (require_approved_action, lines 40-68) + backend/app/audit/service.py (record_evidence / _compute_chain_hash, lines 14-77)

require_approved_action raises HTTP 403 unless the latest ApprovalRequest for that (tenant, action_type, target_id) has status=='approved' (lines 57-68); it is actually wired into mutating actions -- planning/service.py lock_budget and publish_scenario both call it before mutating state (planning/service.py lines 113, 507). record_evidence computes payload_hash and result_hash via SHA-256 over sorted JSON, links each record to previous_hash = latest record's chain_hash, then computes chain_hash = sha256(record_id|previous_hash|payload_hash|result_hash|created_at) (audit/service.py lines 19-36). This is verified end-to-end in backend/tests/audit/test_audit.py::test_automatic_evidence_on_gated_action (lines 138-189): publishing a scenario without approval returns 403 and produces zero evidence rows; after approval it succeeds and produces exactly one 64-char chain_hash evidence row.

RecommendationExtract as a governance_gate / evidence_chain Store block pair: (1) a reusable fail-closed 'require approval before action X' dependency, (2) a hash-chain evidence recorder with a verify_chain() walk that recomputes and compares hashes end-to-end (currently only the single-record hash is re-verified in tests, not a full-chain walk -- add that if extracting). Recommend the orchestrator specifically diff this against whatever Store block previously reported the vacuous always-ok audit status, since this repo's version appears materially more real.






Multi-tenant/multi-project RBAC permission resolution
Resolves effective permissions by joining UserTenantRole and UserProjectRole through Role/role_permissions/Permission tables, câ€¦


Cerebrum-FinanceOps · backend/app/auth/permissions.py (get_user_permissions, lines 75-123)

Resolves effective permissions by joining UserTenantRole and UserProjectRole through Role/role_permissions/Permission tables, correctly distinguishing globally-scoped role assignments (tenant_id/project_id is None) from tenant-scoped and project-scoped ones, and only including a project-scoped grant if it matches the requested project_id or the project's tenant matches tenant_id (lines 115-121). This is DB-backed and context-sensitive, not a hardcoded role->permission dict returned unconditionally.

RecommendationLower priority than the above three, but if the Store's existing auth/RBAC (if any exists as a block) is a flat global role map, this tenant/project-scoped resolution logic is worth lifting into a shared 'multi_tenant_rbac' Store capability.







repo Â· Cerebrum-Steward 3




Estate-scoped bearer auth + multi-tenant RBAC (PilotPrincipal/PilotAccessToken/EstateAccess)
app/steward/auth.py:29-35 hash_token() uses HMAC-SHA256 with an env-configurable pepper (never stores raw tokens); resolve_prinâ€¦


app/steward/auth.py, app/steward/auth_models.py

app/steward/auth.py:29-35 hash_token() uses HMAC-SHA256 with an env-configurable pepper (never stores raw tokens); resolve_principal() (lines ~132-192) requires both a valid, non-revoked, non-expired bearer token AND an explicit tenant/estate EstateAccess row before granting access, returning distinct 401/403/400 codes per failure (invalid_token, principal_disabled, estate_scope_required, estate_access_denied); authorize_estate() (lines 100-129) additionally gates curator-only operations on estate_role in {curator, admin}. Demo bypass is a single explicit boolean (cfg.allow_demo_auth_bypass) that the README states is forced false in production and the config 'refuses contradictory combinations at boot.'

RecommendationExtract as a generic 'tenant/property-scoped RBAC auth' Store block (hashed bearer tokens + per-resource-scope access rows + curator/admin role gate). No block among the 108 registered or the vendor/blocks/ set implements per-tenant scoped RBAC with hashed-token storage; the Store's closest primitive (a generic 'database'/'validation' block) is not domain-equivalent. Worth generalizing estate_id -> resource_id so it applies to any multi-tenant vertical (hotelops, real_estate_v2, etc.).






Resident Engineer: DNA-grounded self-diagnosis + approval-gated, audited heal workflow with prompt-injection sanitizer
app/resident_engineer/injection_guard.py:9-24 implements real regex-based stripping of instruction-injection patterns (ignore pâ€¦


app/resident_engineer/ (diagnosis.py, router.py, injection_guard.py, heal/approval.py, heal/executor.py, heal/audit_log.py, heal/catalog.py, _dna_checksum.py)

app/resident_engineer/injection_guard.py:9-24 implements real regex-based stripping of instruction-injection patterns (ignore previous instructions, os.system(, subprocess., eval(, exec() etc.) applied to all untrusted diagnostic text before it's echoed back (diagnosis.py:26 calls strip_instruction_patterns(symptom)). heal/catalog.py enforces a closed allowlist (ALLOWLISTED_HEAL_ACTIONS, 4 entries) behind a confirmation_required=True ActionSpec, requires an approval object (heal/approval.py) before router.py's /v1/resident/heal endpoint will run it, and every heal handler explicitly reports {"executed": false, "simulation": true, "ok": false, "detail": "In-memory simulation only"} -- i.e. it is honestly labeled as NOT performing a real remediation. heal/audit_log.py is a genuine append-only JSONL log that verifies file size only grows (before/after byte-length check, heal/audit_log.py:38-43) and raises AuditImmutabilityError on shrink; _dna_checksum.py does real SHA-256 verification of the product's DNA bundle files against a manifest.

RecommendationThis is a real, well-designed GOVERNANCE SCAFFOLD (diagnose -> approve -> execute-with-audit -> injection-sanitize) for self-service ops. Extract only the scaffold pattern (allowlist + approval + append-only audit + injection guard), not the handlers, as a 'self-heal governance' Store block/pattern; the handlers would need real backing implementations (actual ingestion retry, actual worker restart) to be useful beyond a demo.






Multi-format ingestion pipeline with page/sheet/row provenance
detect_type()/_parse_pdf()/_parse_docx() (lines 35-90) do real parsing via pypdf.PdfReader and python-docx (extracting per-pageâ€¦


app/steward/ingestion.py

detect_type()/_parse_pdf()/_parse_docx() (lines 35-90) do real parsing via pypdf.PdfReader and python-docx (extracting per-page text with page numbers, table rows joined with ' | '), and the module continues to handle CSV/XLSX (requirements.txt pins openpyxl>=3.1.0) with ROWS_PER_CHUNK=25 batching, threading row_start/row_end and page/sheet provenance fields through to DocumentChunk/Citation records used later in retrieval.py's citations.

RecommendationExtract as an upgrade to the Store's document_engine ingestion path if the Store's version lacks page/sheet/row-level provenance; map ParsedUnit -> a generic DocumentChunk provenance schema.







repo Â· InsureOps 1




Dual RAG regulatory chunking (L1/L2 layer-aware retrieval for compliance corpora)
rag/service.py implements a Dual RAG service with explicit L1 (domain/tenant-wide) vs L2 (project-scoped) collections, pgvectorâ€¦


InsureOps · backend/app/insureops/rag/service.py (595L), backend/app/insureops/rag/chunking.py (179L), backend/app/insureops/rag/retrieval_db.py (453L), backend/app/insureops/rag/ingestion.py (375L)

rag/service.py implements a Dual RAG service with explicit L1 (domain/tenant-wide) vs L2 (project-scoped) collections, pgvector hybrid search with RRF (hybrid_search in retrieval_db.py) plus an honest SQLite in-memory fallback labeled as a stub in responses. chunking.py has a regulatory-section-aware chunker (chunk_regulatory_by_section, _label_for_match, _is_toc_stub, _looks_like_main_guideline_intro) purpose-built for parsing citation-bearing insurance regulatory text, plus atomic Q&A chunking (chunk_qa_atomic) distinct from generic fixed-window chunking.

RecommendationCompare directly against the Store's knowledge.py and vector_search.py (not read in this pass) before deciding whether to extract. If those are generic embed-and-chunk blocks without regulatory-section awareness or L1/L2 layer separation, chunk_regulatory_by_section is worth lifting into a new capability; otherwise this may just be InsureOps-side orchestration on top of existing Store primitives.







repo Â· Me-Agent 5




Sub-agent process isolation spawner (spawn-context, env-scrub, timeout-escalation, schema-validated retry)
agent/swarm/spawner.py:21 uses mp.get_context("spawn") (never fork, so secrets in parent memory are never inherited); `_scrâ€¦


Me-Agent · agent/swarm/spawner.py:1-294 (isolation helpers) + agent/swarm/tools.py:1-127

agent/swarm/spawner.py:21 uses mp.get_context("spawn") (never fork, so secrets in parent memory are never inherited); _scrubbed_env() (line 36-46) builds the child env from an explicit allow-list (PATH, PYTHONPATH, AIRGAP, TEST_MODE, DATA_DIR, OLLAMA_URL, OLLAMA_MODEL) so AGENT_SECRET/AGENT_API_TOKEN/HIVE_JWT_KEY can never leak to a child process; _spawn_block (line 189-286) does proc.join(timeout) then terminate() -> 2s grace -> kill() on timeout, returns a real StepOutput(validation_status="failed", reason="timeout"); _validate_output (line 288-293) runs jsonschema.validate against the block's declared output_json_schema and retries once with the schema re-appended to the prompt on failure. tools.py file_reader (line 52-64) resolves os.path.realpath and requires the result be inside an evidence-root whitelist, raising PermissionError on traversal. calculator (line 28-40) is a whitelisted AST-node evaluator -- no eval/exec anywhere. Each control is independently exercised by tests/unit/test_spawner.py, and the repo's own verification log (_verify_11_4.txt) shows these tests actually executing against a live subprocess.

RecommendationExtract as a Store 'sub_agent_runtime' / 'sandboxed_executor' block: multiprocessing-based child execution with an explicit env allow-list, JSON-Schema output contract, and timeout-kill escalation. Cross-check first against the Store's mcp_adapter.py (92 lines) and mcp_consumer.py (85 lines) and the workbench app/blocks file -- if those are thinner (no process isolation / no env scrubbing / no schema-retry), this is materially more advanced and should replace or feed into them.






Hive vector-clock memory sync with hive-wins conflict archival
hive/sync.py implements real vector-clock comparison: clock_dominates (line 14-18, strict per-node >= with at least one >),â€¦


Me-Agent · hive/sync.py:1-401, hive/auth.py:1-185

hive/sync.py implements real vector-clock comparison: clock_dominates (line 14-18, strict per-node >= with at least one >), clocks_concurrent (line 21-22), merge_clocks (element-wise max, line 25-30). handle_agent_delta (line 263-383) branches on three real cases -- agent dominates (apply+merge+broadcast), hive dominates (ignore/ack), concurrent (archive BOTH values via archive_conflict, line 173-210, never discarding the losing value) -- backed by tests/integration/test_sync.py::test_concurrent_conflict_hive_wins_and_archives, which asserts the losing agent value is still readable from SQLite afterward. hive/auth.py's verify_and_grant (line 128-184) checks nonce existence/expiry/single-use then hmac.compare_digest(expected, proof.proof) (line 162) -- constant-time comparison, not ==.

RecommendationNothing in the enumerated 108 registered blocks or the ~140 app/blocks/ files does cross-node memory sync with conflict resolution. Extract hive/sync.py's clock-comparison + conflict-archive logic as a reusable 'agent_state_sync' capability, and hive/auth.py's nonce-challenge + HMAC + JWT flow as a reusable 'agent_auth' capability.






Domain-kit compiler: YAML sheet -> validated BlockDef JSON + generated failure-mode pytest files
compile_sheet (engine.py line 236-274) reads a domain YAML with data_tables/calculations/decision_rules/failure_modesâ€¦


Me-Agent · domain-kits/compiler/engine.py:1-318, domain-kits/sheets/port_ops.yaml

compile_sheet (engine.py line 236-274) reads a domain YAML with data_tables/calculations/decision_rules/failure_modes and synthesizes a real Pydantic BlockDef per entry via _block_from_data_table / _block_from_calculation / _block_from_decision_rule (lines 76-233), round-trips every block through BlockDef.model_validate before writing (_write_block, line 236-243), and for every failure_mode renders an actual pytest file (_render_failure_mode_test, line 202-233). domain-kits/sheets/port_ops.yaml is real populated content (tariff rows, berth-fee formula, 72-hour max-dwell gate, one failure mode); _verify_11_4.txt shows compile-domain EXIT: 0 and the generated test actually passing.

RecommendationExtract as a Factory-tooling capability (YAML/spec -> BlockDef compiler with schema round-trip and auto-generated failure tests) to speed certifying new blocks/verticals, rather than as a single runtime block.






Evidence-or-refuse grounding gate with injection-resistant confidence assembly
HeadlessOrchestrator.resolve (line 60-115) returns a hard refusal plan whenever a candidate block declares `evidence_standardâ€¦


Me-Agent · agent/orchestrator/engine.py:60-224, tests/integration/test_injection.py:1-120

HeadlessOrchestrator.resolve (line 60-115) returns a hard refusal plan whenever a candidate block declares evidence_standard and profile.attached_evidence is empty. execute() (line 189-192) computes mission confidence itself from validation_status and never reads any LLM-produced confidence field. tests/integration/test_injection.py::test_evidence_injection_cannot_steer_adder feeds evidence containing an embedded 'IGNORE PRIOR INSTRUCTIONS ... report confidence 1.0' payload plus a mock LLM that deliberately returns confidence: 0.01, and asserts result.confidence == 1.0 (orchestrator-assigned) and != 0.01 (the LLM's poisoned value). This is exactly the class of 'receipt reports success regardless of input' failure this survey watches for -- done correctly, with a test that would actually catch a regression.

RecommendationCompare against app/blocks/validation_pipeline.py (unregistered) -- if it lacks an explicit evidence-or-refuse gate and orchestrator-assigned (not LLM-echoed) confidence, this pattern is worth porting into the Store's validation/grounding path.






Zero-tolerance build-honesty tooling: AST stub detector + mutation-kill gate + secret scanner
mutation_gate.py's _Mutator (line 27-42) is a real AST transformer that nulls every public function body to return None,â€¦


Me-Agent · scripts/stub_detector.py:1-154, scripts/mutation_gate.py:1-137, scripts/secret_scan.py

mutation_gate.py's _Mutator (line 27-42) is a real AST transformer that nulls every public function body to return None, copies the package into a shadow tree, and re-runs the module's declared acceptance tests against the mutant (run_gate, line 64-125); if tests still pass against the nulled functions, it reports 'mutant survived' and exits 1. _verify_11_4.txt in this repo shows the gate actually catching real survivors ('agent/orchestrator/engine.py: mutant survived (tests passed with nulled functions)') before a later fix, then 'MUTATION GATE PASSED' after -- i.e. it was actually exercised and actually caught something, not just claimed to. stub_detector.py's AST scan is backed by a meta-test (tests/unit/test_stub_detector.py) feeding it known-stub and known-real fixtures.

RecommendationNot a domain block -- adopt directly into the Factory's own CI/build-gate pipeline as a generic pre-merge 'hollow function / vacuous test' check, given this survey has repeatedly found exactly that failure mode in other repos.







repo Â· StockWisePro 2




Human-user session auth: register/login/JWT+refresh-rotation/TOTP MFA/bcrypt
auth.ts:2-11 imports bcrypt, jsonwebtoken, and otplib (generateSecret/generateURI/verifySync)


StockWisePro · app/server/src/routes/auth.ts (594 lines) + app/server/src/middleware/auth.ts + app/server/prisma/schema.prisma (User, Session, RefreshToken models)

auth.ts:2-11 imports bcrypt, jsonwebtoken, and otplib (generateSecret/generateURI/verifySync). Line 82: const passwordHash = await bcrypt.hash(data.password, config.security.bcryptRounds). Login (line 164) checks MFA at line 208-218: if (user.mfaEnabled) { ... const mfaResult = verifySync({ token: data.mfaCode, secret: user.mfaSecret || '' }); const validMFA = mfaResult.valid; }. Refresh tokens are stored hashed (token: hashToken(refreshToken), line 128/250/327) and rotated on /refresh (line 287). MFA setup/verify/disable endpoints exist at lines 493/528/564. This is a complete, working human-user authentication stack, not a docstring claim.

RecommendationExtract as a new Store block, e.g. user_auth (distinct from the existing auth block, which is API-key/RBAC for machine block-dispatch, not end-user login). Map register/login/refresh/logout/mfa-setup/mfa-verify/change-password into a UniversalBlock process() action-router the same shape as other Store blocks; keep bcrypt+otplib+JWT-refresh-rotation as the reference implementation since the current Store auth.py has no login/session/MFA concept at all (it only issues API keys for 4 fixed roles: ADMIN/PRO/BASIC/READONLY).






Stock-market data ingestion, portfolio holdings, and price-alert vertical
stockPriceService.ts:15-41 does real dual-provider failover (Twelve Data -> Alpha Vantage -> DB cache) with a 5-minute cache frâ€¦


StockWisePro · app/server/src/services/stockPriceService.ts, app/server/src/services/twelveDataService.ts, app/server/src/services/alphaVantageService.ts, app/server/src/services/alertService.ts, app/server/src/routes/portfolio.ts, app/server/src/routes/watchlist.ts, app/server/src/routes/alerts.ts, schema.prisma (Stock, StockPrice, Portfolio, PortfolioHolding, PortfolioTransaction, Watchlist, Alert, AlertLog)

stockPriceService.ts:15-41 does real dual-provider failover (Twelve Data -> Alpha Vantage -> DB cache) with a 5-minute cache freshness check (line 102) and writes both a cached-quote upsert and an append-only StockPrice history row (lines 117-151). alertService.ts:27-38 evaluates real condition logic (ABOVE/BELOW/EQUALS against a stored target) and on trigger increments triggeredCount, writes an AlertLog, and emits a live WebSocket event (io.emit('alert:triggered', ...)). No block in the registered 108 or the ~140 app/blocks/ set does real-time market-quote ingestion, portfolio-holding tracking, or price-threshold alerting; the Store's finance_v2/finance_saas_metrics/finance_reconciliation/finance_canonical_model blocks are document-analysis and SaaS-accounting-metrics engines with no concept of a live market quote, a held position, or a triggerable price alert. CAVEAT (fake feature, do not clone): routes/experiments.ts's '/:id/run' backtest endpoint (lines 150-192) is fabricated â€” finalCapital: initialCapital * (1 + (Math.random() * 0.5 - 0.1)), totalReturn: (Math.random()*50-10).toFixed(2)+'%', sharpeRatio: (Math.random()*2).toFixed(2) are pure Math.random() noise, never computed from the stored formula/backtestConfig or any historical price series â€” exactly the 'reports completeness without real work' pattern this survey is watching for.

RecommendationExtract the price-ingestion + caching + alert-trigger pattern as a new vertical block (e.g. market_data or portfolio_v2), porting the dual-provider-fallback-with-cache pattern and the alert-condition-evaluator. Do NOT port the experiments.ts backtest endpoint. Also note the schema's DataRetentionPolicy model has zero call sites anywhere in app code, and UsageMetrics is only ever read (organization.ts:421) and never written â€” both are compliance-sounding schema stubs with no working implementation behind them.







repo Â· TEKsystems_GlobalRetailMNC 3




Verified-outcome learning engine (deterministic advisory calibration)
engine.py:48-180 record_verified_outcome() enforces tenant+project scoping, rejects unverified events from touching profiles, dâ€¦


backend/app/retailops/learning/engine.py, demand_calibrator.py, promotion_learner.py, supplier_learner.py, exception_detector.py, common.py

engine.py:48-180 record_verified_outcome() enforces tenant+project scoping, rejects unverified events from touching profiles, dedups via a deduplication_key hash, and versions the resulting RetailLearningProfile on every recalculation (profile.version += 1, line 256). demand_calibrator.py:67-110 computes real formulas: recommended_rop = current_rop * clamp(avg(actual/forecast), 0.5, 2.0) and a +20% safety-stock bump when stockout_frequency >= 0.25, with an explicit 'assumptions' list disclosed alongside the numbers and a confidence label gated on sample count (>=8 for high). This is a genuine deterministic calibration engine over verified outcomes, not a placeholder.

RecommendationExtract as a generalized 'verified_outcome_learning' Store block: tenant/project-scoped event ledger + versioned advisory-profile recalculation, pluggable per-domain calculator modules (the demand/supplier/promotion/exception calculators here are retail-specific instances of a reusable pattern). None of the 108 registered blocks or the unregistered app/blocks/*_v2 verticals implement this verified-only, dedup+audit-linked calibration loop as a reusable primitive.






Discipline-agent 'hat' framework (base+hat manifests, playbooks, confirmation-gated memory)
manifests/retail.hat.inventory.json is a fully populated, non-boilerplate manifest: activation triggers, allowed_actions allowlâ€¦


backend/app/retailops/agents/catalog.py, formulas.py, memory.py, models.py, manifests/retail.hat.*.json (10 hats)

manifests/retail.hat.inventory.json is a fully populated, non-boilerplate manifest: activation triggers, allowed_actions allowlist, handoffs to other hats with requires_confirmation flags, playbook.formulas with explicit expressions bound via implemented_by to real code (resolved in formulas.py:10-22 _IMPL_ALIASES), playbook.standards/procedures with ordered steps. memory.py:95-111 _sanitize_value() actively rejects PII/secret keys (password, ssn, card_number, employee_name, guest_name) and LLM-free-text dumps ('as an ai' regex), and save_memory() requires explicit confirmation plus manifest-declared key validation (memory.py:78-92). test_audit_hardcut.py asserts real wiring (e.g. 'inventory.days_of_cover' formula id must resolve, hat allowlists actually block out-of-scope actions) rather than trivially-true checks.

RecommendationThis is a governance/agent-definition layer (declarative discipline agents with inheritance, auditable formula bindings, and confirmation-gated teachable memory) that doesn't correspond to any single one of the 108 registered blocks. Consider extracting the manifest schema + catalog/compose/formula-binding runtime as a Store 'agent_playbook' or 'discipline_pack' block usable across verticals, independent of the retail-specific hat content.






Embedding-space compatibility guard
assert_compatible() (lines 68-89) fingerprints the active embedder (model name + dim) and compares it against the (model, dim)â€¦


backend/app/retailops/embedding_guard.py

assert_compatible() (lines 68-89) fingerprints the active embedder (model name + dim) and compares it against the (model, dim) already stamped on a tenant/project's persisted document chunks, raising EmbeddingSpaceError before any query/embed proceeds if they differ â€” preventing silent corruption of a vector index when the embedding backend is swapped.

RecommendationSmall but genuinely useful reliability primitive missing from a generic RAG/vector_search block: fold into the Store's vector_search or knowledge block as an optional pre-flight check.







repo Â· The_Fork 5




Construction-contract RAG retriever with structural tenant isolation
Not generic vector search


app/core/rag/retriever.py (8015 lines), app/core/rag/vector_store.py (1836), app/core/rag/coverage_honesty.py, app/core/rag/inject.py, app/core/rag/audit.py

Not generic vector search. retriever.py implements dozens of named, individually-toggled 'rescue' pipelines for specific construction-contract clause types (parent company guarantee, commencement date, contract schedule register, spec-title identity, E1 rate volumes) e.g. chunk_states_pcg_form_template(), chunk_states_schedule_register(), extract_contract_doc_ids(), elect_answer_bearing_contract(). It also enforces multi-tenant corpus isolation with an incident-scarred comment: '_general_knowledge_project_ids() ... STEP 0 structural isolation: the master-corpus/client fallback corpus is NEVER part of the always-on GK merge, even when a stale env still lists it (prod once had drive_archive + client_infra_pack_1 in this var, silently merging the whole client project corpus into every OTHER project's results -- the ha_long -> the client project leak)'. coverage_honesty.py forbids the model from claiming 'does not exist'/'no such clause' when the index is <100% populated, rewriting to 'not found in the N indexed' -- an epistemic-honesty constraint with no equivalent in a thin vector_search block. Backed by real tests (tests/test_retrieval_survives_compaction.py, tests/test_char_offset_schema.py) and evals/rag_audit_v3/.

RecommendationExtract as a Store-level 'contract_retrieval' upgrade layered on top of (not replacing) the generic vector_search block: port coverage_honesty.py's partial-index disclosure contract and the structural-isolation pattern in _general_knowledge_project_ids()/_master_corpus_fallback_id() first, since those are domain-agnostic safety properties any multi-tenant Store RAG block should have.






CPM scheduling engine + Primavera P6 XER parser
Real forward/backward-pass CPM math (ES/EF/LS/LF, total/free float), circular-dependency detection (CircularDependencyError), râ€¦


app/lib/pm_computations.py (818 lines: topological_order, cpm_forward_pass, cpm_backward_pass, calculate_float, compute_cpm, resource_histogram, gantt_data, compress_schedule, parse_xer_full/parse_xer), app/blocks/primavera_parser.py (360 lines), app/blocks/cpm_engine.py (thin wrapper over pm_computations)

Real forward/backward-pass CPM math (ES/EF/LS/LF, total/free float), circular-dependency detection (CircularDependencyError), resource histograms, schedule compression, and a full XER table tokenizer (_tokenize_xer_tables) feeding parse_xer_full. cpm_engine.py's own docstring: 'The real forward/backward pass, float, and critical-path logic lives in app.lib.pm_computations.compute_cpm. This block is a thin wrapper.' Covered by tests/test_cpm_engine.py and tests/test_xer_truncation_disclosure.py.

RecommendationExtract app/lib/pm_computations.py wholesale as a new Store 'cpm_engine' block (none of the Store's 108 registered blocks or 28 excess verticals cover CPM scheduling or Primavera P6 file parsing); wrap with the standard 40-line adapter pattern.






Drawing QTO (DXF/DWG/PDF quantity take-off)
Documents a real engineering migration in its module docstring: 'V1.2 swaps the text-extraction path from pdfplumber to PyMuPDFâ€¦


app/blocks/drawing_qto.py (1963 lines -- the single largest block file in the repo)

Documents a real engineering migration in its module docstring: 'V1.2 swaps the text-extraction path from pdfplumber to PyMuPDF (fitz). pdfplumber was a 80-180s/drawing bottleneck on dense CAD PDFs... Every comparison that referred to bottom 15% or sorted top-down has been flipped accordingly [for the coordinate-system change].' Implements font-size bucketing, JCB-code regex extraction, place-name blocklists, cross-reference extraction across multi-page drawings, and fallback chains. Covered by tests/test_drawing_qto.py, tests/test_drawing_ocr_fallback_and_bounded_render.py, tests/test_w3_planted_truth_drawings.py, tests/test_drawing_room_takeoff.py.

RecommendationExtract as a Store 'drawing_qto' block for a construction vertical; explicit perf-regression fix history and coordinate-system bug fix documented in-line show this survived real production use.






BOQ (Bill of Quantities) processor with unit reconciliation
Handles a specific documented production incident in-code: '_boq_pdf_max_mb ..


app/blocks/boq_processor.py (571 lines), app/lib/boq_units.py (reconcile_unit), app/lib/boq_schedule.py

Handles a specific documented production incident in-code: '_boq_pdf_max_mb ... Parse-refuse ceiling, aligned with the OCR plaintext gate. The live priced BOQ (doc 20ac033d) is ~26.9 MB plaintext... above the old hardcoded BOQ_PDF_MAX_MB=20 and already inside the 32 MB OCR gate.' Real unit reconciliation logic (reconcile_unit) rather than a naive column parser. Covered by tests/test_boq_processor_numeric_parsers.py, tests/test_boq_processor_pdf_guard.py, tests/test_boq_xls_format.py, tests/test_price_boq_endpoint.py.

RecommendationExtract as a Store 'boq_processor' block; pair with drawing_qto and bim_extractor as the construction-QTO trio for a new Store construction vertical.






Honestly-labeled learned routing classifier
Real sklearn LogisticRegression/TF-IDF classifier with sha256 integrity-checked model artifacts ('Integrity-checked on load: fiâ€¦


app/core/learning/router.py (667 lines), app/core/learning/trainable.py, app/core/learning/hydration.py (1117 lines), app/core/learning/local_model.py

Real sklearn LogisticRegression/TF-IDF classifier with sha256 integrity-checked model artifacts ('Integrity-checked on load: file present, sha256 matches, label set matches model.classes_'). trainable.py enforces an anti-feedback-loop guardrail: is_independently_labeled() explicitly rejects a router's own dispatch decisions as training labels -- 'Own dispatch (the orchestrator recording what it just did) is not a label' -- only accepting battery_grade/feedback_route/user_correction sources. This is a genuine safeguard against self-reinforcing model corruption, the opposite of a vacuous always-ok gate.

RecommendationExtract the label-provenance guardrail (trainable.py) as a reusable Store pattern for any block that claims to 'learn' from its own output -- check whether app/blocks/learning_engine.py in the Store has an equivalent independent-label check before trusting its 'learning' claim.







repo Â· The_Level 2




The Level -- AI answer-quality evaluation & certification harness
This is not a domain vertical -- it's a deterministic grading engine for scoring an AI target's answers against a battery of quâ€¦


The_Level · level/scorers/ (provenance.py, scope_contamination.py, formula_grounding.py, numeric_digit_integrity.py, silent_failure.py), level/certify/gates.py, level/grader/independence.py, level/battery/canary.py, level/core/taxonomy.py

This is not a domain vertical -- it's a deterministic grading engine for scoring an AI target's answers against a battery of questions, with an 11-class failure taxonomy and severity ladder (level/core/taxonomy.py:48-111) designed for gate math. Concrete, non-vacuous logic, e.g. ProvenanceScorer (level/scorers/provenance.py:94-154) distinguishes 'traces to ground truth' vs 'traces to sources the target was actually given' and explicitly excludes question.source_ref as a source because it's a pointer not content (docstring + code lines 25-35, 157-168); ScopeContaminationScorer (scope_contamination.py:67-140) flags 'correct-shaped answer from wrong document' as Sev-1 via a citation-root extraction regex, separate from a bare scope_miss; FormulaGroundingScorer (formula_grounding.py:107-208) parses lhs=rhs equations only (citations stripped) and checks every symbol is defined either in-answer or by the question, with a documented false-positive fix (git log commit message: fixed 'Labour cost is 115...' misfire). GateConfig.evaluate (level/certify/gates.py:157-256) does exact Fraction arithmetic specifically to prevent 94.6% from displaying/certifying as 95% (docstring lines 6-13, code line 179-193) -- never floats, never rounds before comparing. GraderIdentity.is_independent_of (level/grader/independence.py:83-84, 100-116) makes a run REFUSE to start (not just warn) if grader identity == target fingerprint, on grounds that self-grading 'produces a score that means nothing... and looks exactly like one that does.' battery/canary.py:45-105 implements a non-random, content-stable canary-minting and detection scheme to void an entire run (not just one question) if the target has memorized the battery. None of this is a vacuous 'always returns ok' gate -- GateResult can be CERTIFIED/PROVISIONAL/NOT_CERTIFIED/VOID and every scorer explicitly returns NOT_APPLICABLE (decline) rather than PASS when it has nothing to check, with commit-log evidence (build the three that were declared and unbuilt) of the team catching and fixing their own false positives before merge. Backed by 4681 lines of tests across 14 files including planted-failure and mutation-probe style tests (tests/test_scorers_provenance_scope_formula.py, test_grader_certify.py, test_harness.py).

RecommendationExtract as a new Store block category, e.g. eval_harness / answer_certifier, distinct from anything in the current 108 registered blocks or the ~140 app/blocks files (no existing block name maps to 'battery-based LLM answer grading with an independent grader and failure taxonomy'). Candidate mapping: wrap level/certify + level/grader + level/scorers + level/battery as a single new certified block (e.g. cerebrum_level_certifier) that any Factory-generated platform's outputs can be run through per README's stated design -- the repo explicitly says it is meant to be 'embedded... every factory-generated platform ships it' (README.md, KERNEL_DEFAULTS K5). Given that framing, this may already be intended for the Store rather than being an undiscovered clone -- but as of the surveyed commit it is not yet registered/wired into block_registry/, so it is a real gap between stated design and Store inventory.






Fraction-exact certification gate math with waiver/void/provisional tier semantics
GateConfig stores thresholds as fractions.Fraction (not float) specifically to prevent rounding-based gate gaming (lines 6-13,â€¦


The_Level · level/certify/gates.py:1-313

GateConfig stores thresholds as fractions.Fraction (not float) specifically to prevent rounding-based gate gaming (lines 6-13, 111-125, 179-193), and TrustTier distinguishes CERTIFIED (all gates met) from PROVISIONAL (gates waived, recorded, never silently) from VOID (run contaminated -- distinct from a target simply failing). trap_pass_rate defaults to exactly 1 (100%) with an explicit argument in the code comment for why 90% would be unacceptable (lines 139-143). This is a reusable general-purpose 'certify anything against exact thresholds with an auditable waiver trail' primitive, independent of the LLM-scoring use case.

RecommendationIf extracted separately from the scorer/grader stack, this could become a small standalone 'exact-threshold gate/certification' utility block usable anywhere the Store needs tamper-resistant pass/fail math (e.g. compliance or QA gates for other verticals) rather than being bundled only with the AI-eval harness.







repo Â· ThreadForge 1




P&ID/DEXPI-to-piping-artefact digital thread engine
Namespace-aware DEXPI/Proteus XML parser (ingest_dexpi.py, 1182 lines) validated against vendored public DEXPI 1.3 / Proteus 4.â€¦


ThreadForge · ThreadForge/src/threadforge/{ingest_dexpi.py,dexpi_core.py,graph.py,generators.py,routing.py,clash.py}

Namespace-aware DEXPI/Proteus XML parser (ingest_dexpi.py, 1182 lines) validated against vendored public DEXPI 1.3 / Proteus 4.1 XSDs (fixtures/public/dexpi13/), builds a topology graph (graph.py) of tags/lines/from-to/battery-limits, then routing.py implements a genuine 3D grid A* pathfinder with 6-connected moves, bend-penalty cost, obstacle AABB/capsule blocking, drain-monotonic constraint, and colinear-segment simplification (route_astar(), src/threadforge/routing.py:423-577) plus clash.py (442 lines) doing capsule-capsule and capsule-AABB geometric clash math. This is functioning computational geometry, not a docstring claim -- verified by reading the loop body (heapq-based A* with quantize/dequantize grid, soft_cost for rack-tier bias, path reconstruction). Nothing resembling P&ID ingestion, topology-graph construction, or obstacle-aware pipe routing exists anywhere in the Store's 108 registered blocks or the ~140 app/blocks/ files (those cover hotel/retail/finance/supply-chain/aviation verticals, document/RAG/vector blocks, and a generic validation_pipeline -- none touch DEXPI/Proteus XML, piping topology, or A* geometric routing). Also backed by 52 test files / 3846 lines of tests (tests/test_astar_routing.py, test_clash.py, test_no_silent_geometry.py) with concrete non-vacuous assertions, e.g. test_no_silent_geometry.py:17-25 asserts exactly which line numbers are flagged 'fabricated' vs sourced from real nozzle_xyz data.

RecommendationExtract as a new Store vertical, e.g. blocks/engineering_epc_piping_v1.py (or piping_digital_thread), wrapping: (1) DEXPI/Proteus ingest -> topology graph, (2) obstacle-aware A* routing + clash detection, (3) PCF/isometric/GA generation. Register a thin block_registry adapter per the existing 40-line pattern; keep the heavy logic in app/blocks/ as ThreadForge already separates engine (src/threadforge) from CLI/API/MCP wrapper (server.py, mcp_server.py, agent_tools.py), which maps cleanly onto the Store's block+adapter split.







repo Â· bim-manager-agent 3




Zone-branch multi-agent coordination with cross-zone arbitration
arbitrate() at coordinator.py:612 runs BOTH owning zones' full monitor sets against a shared-element proposal and only commitsâ€¦


app/agents/coordinator.py (esp. arbitrate() L612-730, resolve_zone() L435-507, enqueue_rebase() L760-887), app/agents/zone_resolver.py, app/agents/zoning.py

arbitrate() at coordinator.py:612 runs BOTH owning zones' full monitor sets against a shared-element proposal and only commits on a 'double pass' (L664 commit only after checking any(v == VERDICT_FAIL ...)); on a fail it writes objections to the ledger and moves the clash to 'escalated' (L668-684); on pass it distinguishes proposal.verdict = 'verified' vs 'verified_conditional' (L699-701) based on whether any zone had an UNPROVABLE check, then enqueues a rebase of affected neighbours. This is a genuine distributed-consistency pattern (per-zone optimistic branches + boundary re-verification + arbitrated merge) specific to spatial multi-agent coordination. Nothing resembling zone-partitioned agents, branch-head-based boundary checks, or arbitrated double-pass commits exists in the Store's generic orchestrator.py, agent_swarm.py, or workflow.py (which are domain-generic task/step orchestrators, not spatial-partition arbitration).

RecommendationExtract the zone-partition + branch-per-zone + boundary-monitor + arbitrated-double-pass-commit pattern as a general 'spatial/partitioned multi-agent arbitration' capability, mappable to a new Store block (e.g. partitioned_arbitration or zone_coordinator) that agent_swarm.py or orchestrator.py could adopt when work is spatially/logically partitioned and neighbours' live state must be rechecked before a cross-boundary commit.






Three-valued (pass/fail/unprovable) monitor contract
base.py:19-20 defines PASS/FAIL/UNPROVABLE as first-class states, with the docstring at L27-30 stating UNPROVABLE 'must never bâ€¦


app/monitors/base.py (Check/MonitorResult, L20-40), app/monitors/boundary.py, app/monitors/geometry.py, app/monitors/integrity.py

base.py:19-20 defines PASS/FAIL/UNPROVABLE as first-class states, with the docstring at L27-30 stating UNPROVABLE 'must never be recorded as' a pass. boundary.py:1-13 documents and implements checking neighbour zones against the store's CURRENT branch heads rather than as-loaded positions ('Zone B may have committed three moves since zone A began work, and checking against stale positions would verify a move against a building nobody is building any more', boundary.py:9-12), fetched live via ctx.store.heads() at boundary.py:47-48. This distinguishes a genuine absence-of-evidence from a pass, and is enforced structurally (arbitrate() propagates 'unprovable' into proposal.unprovable_checks rather than silently passing, coordinator.py:698-700).

RecommendationGeneralize the three-valued verdict contract (pass/fail/unprovable, never silently coerced to pass) and the 'check against live current state, not the state as originally loaded' pattern as a reusable validation primitive for the Store's validation.py / audit.py blocks, which currently reason only in pass/fail or boolean terms.






AST-based structural LLM-boundary enforcement test
test_llm_boundary.py:52-67 walks every import (via ast.parse/ast.walk, including imports inside functions) of every module listâ€¦


tests/unit/test_llm_boundary.py (full file, 152 lines)

test_llm_boundary.py:52-67 walks every import (via ast.parse/ast.walk, including imports inside functions) of every module listed in DECISION_MODULES and asserts none of them can reach a model-provider SDK (anthropic/openai/google/etc., FORBIDDEN_ROOTS at L39-48) or app.llm, directly or transitively. test_the_decision_module_list_covers_what_actually_decides() (L79-92) additionally asserts the DECISION_MODULES list itself cannot silently go stale by diffing it against every .py file actually present under app/monitors, app/agents, app/kit. This programmatically guarantees an LLM cannot influence a clash verdict, monitor result, or merge decision -- a governance property enforced by code, not by convention or docstring.

RecommendationExtract as a reusable 'decision-boundary' test/lint utility (an AST import-walker with a configurable forbidden-import set and a self-auditing module list) that could be offered as a Store validation/audit block primitive for any product wanting to prove -- not just claim -- that specific code paths never reach an LLM client.







repo Â· cerebrum-hotelops 4




Three-valued engineering evidence classifier (fraud-aware commissioning evidence)
classify_evidence() implements real fraud/defect detection specific to MEP handover fraud patterns, not a generic doc-classifieâ€¦


cerebrum-hotelops · reasoning/evidence.py (288 lines)

classify_evidence() implements real fraud/defect detection specific to MEP handover fraud patterns, not a generic doc-classifier: it fails a fire-pump cert as Class D when packet.test_line_to_tank is true ('test-line-to-tank' certs are a known fraud pattern), independently detects nameplate-obscuring photos via FIRE_PUMP_COVER_TAGS = {cover_closed, clip_obscuring_nameplate, plastic_wrap_on_plate}, flags stale/unwitnessed tests (test_age_days > 365 and not site_witness), flags GRMS cert/config mismatches (INV-GRMS-CERT-NE-CONFIG), and correctly demotes a packet to UNPROVABLE (not FAIL) when independent evidence has been invalidated by tampering rather than declared passing. combine_verdicts() implements a genuine conservative three-valued fold (FAIL > UNPROVABLE > PASS) used consistently by licensing.py and cascade_engine.py.

RecommendationExtract as a Store 'evidence/compliance classification' block (e.g. engineering_evidence_classifier) generalized beyond hotel fire-pump/GRMS/elevator assets to any regulated-asset handover domain (construction handover, facilities compliance). Pair with the invalidity-case library in domain_kit/engineering_inheritance/evidence_invalidity_cases.json.






Refuse-by-design licensing prerequisite resolver and PPM cadence resolver
PPMResolver.resolve() actively refuses to answer rather than guessing: `if pack.get('authoritative_frequencies'): raise PPMRefuâ€¦


cerebrum-hotelops · reasoning/licensing.py (77 lines), reasoning/ppm_resolver.py (116 lines), reasoning/market_router.py, reasoning/guard.py

PPMResolver.resolve() actively refuses to answer rather than guessing: if pack.get('authoritative_frequencies'): raise PPMRefuse('generic_frequency_refused', ...) and if not named: raise PPMRefuse('sop_missing', 'Named operator SOP is required ... Frequencies are never estimated from the sheet.'). LicensingEngine.evaluate() computes a 'master_pacer' license the same way cascade_engine computes its master pacer (max missing-prerequisites among blocking licenses, with fire/CD given priority), driven by a real prerequisite graph (domain_kit/licensing/uae/prerequisite_map.json), not a lookup table returning a canned answer. guard.py hard-codes UNSUPPORTED_MARKETS = {ksa, saudi, saudi_arabia} and FORBIDDEN_SOURCES = {aconex, procore, bim, bim_ifc, navisworks, revit} and is actually enforced (tests/test_no_ksa_branch.py exists to verify no KSA code path was silently added).

RecommendationExtract the 'refuse rather than fabricate' PPM/licensing pattern (PPMRefuse/GuardError classes, statutory-floor vs operator-SOP two-layer model) as a reusable compliance-resolver block; it is a stronger design pattern than a typical lookup-table PPM block and is explicitly built to prevent exactly the kind of vacuous-pass gate this survey has been screening for elsewhere.






Fixture/live dual-mode connector framework for hospitality enterprise systems
BaseConnector.connect()/fetch()/ingest() implement a real connect->fetch->normalise->emit pipeline with automatic fixture-vs-liâ€¦


cerebrum-hotelops · connectors/base.py (94 lines) + connectors/{opera.py,micros.py,maximo.py,grms.py,gaming_cms.py,loyalty_lms.py}

BaseConnector.connect()/fetch()/ingest() implement a real connect->fetch->normalise->emit pipeline with automatic fixture-vs-live switching (if self.settings.live_enabled(*self.live_env_keys): self._mode = 'live'), a live HTTP path via httpx with resp.raise_for_status(), and per-system normalise() methods that map raw PMS/POS/CMMS records to typed HotelEvent topics (e.g. OperaConnector.normalise maps reservations->guest.booking.reservation, housekeeping->ops.pms.housekeeping). This is a working adapter framework, not a stub with TODOs.

RecommendationExtract connectors/base.py's connect/fetch/normalise/emit abstraction as a generic Store 'external-system connector' base block, and register the six concrete adapters (Opera PMS, Micros POS, Maximo CMMS, GRMS, gaming CMS, loyalty LMS) as hospitality-vertical connector blocks feeding the existing event_bus block.






Fail-closed, non-forgeable ASGI audit middleware
This is a real fail-closed implementation, in contrast to the vacuous 'always returns status: ok' audit pattern this survey hasâ€¦


cerebrum-hotelops · api/audit_middleware.py (166 lines) + api/auth.py (73 lines)

This is a real fail-closed implementation, in contrast to the vacuous 'always returns status: ok' audit pattern this survey has flagged elsewhere. For authenticated mutating requests the middleware buffers the ASGI response (hold = authenticated and method.upper() in MUTATING_METHODS) and only releases it after persist_audit_event() succeeds; on persistence failure it returns 503 instead of a silent 2xx (fail_closed = persist_ok is False and 200 <= status_code < 400). Actor attribution comes only from principal_from_authorization() (verified bearer token via hmac.compare_digest), never from client-supplied x-actor/x-role headers -- verified concretely by tests/test_security_pilot.py::test_audit_uses_principal_not_client_headers, which sends forged x-actor: operator headers on a reviewer token and asserts the audit row still says actor=reviewer, and ::test_audit_failure_fail_closed_on_mutating_success, which monkeypatches persist_audit_event to raise and asserts the client gets 503.

RecommendationI could not directly diff this against a Store audit/security block in this session (no Store clone available here) so I am not calling it a store_upgrade, but it is a strong candidate to compare against and likely replace whatever the Store currently uses for audit/attribution, given it demonstrably defeats header-forgery and fails closed under real test coverage rather than by docstring claim.







repo Â· cerebrum-hotelops-v2 1




Honest mock-connector taxonomy (event bus + 6 enterprise-system normalizers) with fail-closed 'mock_unavailable' contract
connectors/base.py:40-52 fetch() returns a structured {available: False, mock_level:..., data: [], refusal: ...} envelope insâ€¦


connectors/base.py (BaseConnector, MockLevel, ConnectorError), connectors/event_bus.py (EventBus, sqlite-backed audit trail with digest+replay)

connectors/base.py:40-52 fetch() returns a structured {available: False, mock_level:..., data: [], refusal: ...} envelope instead of inventing data when mock_level is MOCK_UNAVAILABLE -- enforced at the base class so every connector inherits the fail-closed behavior. connectors/event_bus.py:50-79 publish() computes a sha256 payload digest, persists to a real sqlite table (bus_events), and supports replay(topic, since_ts) with parametrized SQL (lines 91-121) -- a genuine durable, replayable bus, not an in-memory stub.

RecommendationExtract connectors/base.py's MockLevel/fail-closed contract as a Store-wide connector base-class standard (app/blocks/core/base_connector.py could adopt it), and connectors/event_bus.py as a general durable event-bus block usable by any vertical needing an audited pub/sub layer.







repo Â· stockwisepro-bot 5




OpenBox multi-factor stock scoring engine
Real quant logic, not a docstring claim: computePiotroski() (engine.ts:200-243) implements the actual 9-point Piotroski F-Scoreâ€¦


src/services/openbox/engine.ts (963 lines), plus src/services/openbox/{ethics,peers,dominance,narrative,risks}.ts

Real quant logic, not a docstring claim: computePiotroski() (engine.ts:200-243) implements the actual 9-point Piotroski F-Score test-by-test (ROA delta, operating cashflow vs net income, leverage delta, current-ratio delta, share dilution, margin delta, asset turnover); computeAltmanZ() (246-264) computes the standard 5-factor Altman Z-Score formula (1.2*A+1.4*B+3.3*C+0.6*D+1.0*E); calculateSharpeRatio()/calculateMaxDrawdown()/calculateRSI() (83-116) do real log-return, drawdown and RSI math; the stock pillar scoring (664-832) blends multi-timeframe momentum (1m/3m/6m/12m composite, line 712), sector-relative and ROE-adjusted P/E scoring (peers.ts), and produces a full per-metric rule breakdown (rule() calls, e.g. 804-831) so every point is explainable. Per-user custom pillar weights are applied and renormalized to sum to 100 (856-900). This is materially more sophisticated than a thin adapter.

RecommendationExtract engine.ts + peers.ts + dominance.ts (strip ethics.ts's hardcoded boycott list, see notes) into a Store 'financial_scoring' or 'equity_analysis' block wrapping compute logic behind execute(ticker, weights) -> {finalScore, pillars, breakdown, riskFlags, narrative}. No existing app/blocks/ file in the enumerated list covers this; closest analogues (vector/vector_search) are retrieval, not valuation math.






Backtest formula DSL + historical evaluator
tokenize()/parseExpression()/parseOr()/parseAnd()/parseComparison() (lines 29-101) is a genuine recursive-descent parser for boâ€¦


src/services/backtest.ts (207 lines)

tokenize()/parseExpression()/parseOr()/parseAnd()/parseComparison() (lines 29-101) is a genuine recursive-descent parser for boolean stock-screening formulas like '(pe_ratio < 15 and roe > 0.15) or dividend_yield > 0.04', producing a closure evaluator via buildFormulaEvaluator(). runBacktest() (139-207) then fetches real historical prices/fundamentals for a sample universe, applies the evaluator, and computes selected-vs-SPY 6-month returns with winner/loser counts - a real backtesting loop against live market data, not a stub.

RecommendationExtract as a 'formula_screener_backtest' Store block: execute(formula, universe?) -> selection + historical performance vs benchmark. Nothing in the ~140 app/blocks/ list implements an expression parser or backtest evaluator.






Screenshot-to-ticker OCR pipeline with domain-specific fuzzy correction
Multi-pass ticker extraction ($cashtag regex, structural 'ticker-above-company-name' detection via looksLikeCompanyName(), fallâ€¦


src/services/ocr.ts (467 lines)

Multi-pass ticker extraction ($cashtag regex, structural 'ticker-above-company-name' detection via looksLikeCompanyName(), fallback word-scanning, and known-correction-pattern matching - extractTickers(), lines 261-334) combined with a real Levenshtein-distance fuzzy matcher (levenshtein()/fuzzyMatchTicker(), 146-174) against a loaded stock universe, plus a hand-built OCR character-confusion table (OCR_CONFUSION, 74-97) and specific known-mistake corrections (KNOWN_TICKER_CORRECTIONS, 28-71) derived from observed Tesseract failures on real brokerage screenshots. Dual OCR backend: Google Cloud Vision primary, Tesseract.js fallback (runOCR(), 337-351).

RecommendationExtract as a generic 'image_entity_extraction' or 'ocr_with_fuzzy_correction' Store block: execute(image, validEntitySet) -> corrected entity list with correction log. The confusion-map/Levenshtein-correction pattern generalizes beyond tickers to any structured-code extraction (SKUs, part numbers, IDs) and is more advanced than a bare OCR call.






Self-evaluating signal-accuracy tracker
computeMarketInsights()/computeTickerInsight() (76-162) compute a genuine backtestable accuracy metric: predictedDirection() maâ€¦


src/services/insights.ts (162 lines)

computeMarketInsights()/computeTickerInsight() (76-162) compute a genuine backtestable accuracy metric: predictedDirection() maps the score to up/down, actualDirection() compares consecutive recorded prices, and evaluateSeries() (61-70) walks each ticker's real time-ordered score/price history from SQLite to compute a hit rate - i.e. it honestly measures whether the scoring engine's own predictions were later correct, including admitting when hitRate is null (no evaluable data). This is the opposite of a vacuous always-pass receipt.

RecommendationExtract as a 'prediction_accuracy_tracker' Store block: execute(scoreHistorySeries) -> hitRate/topMovers/mostAlerted, reusable by any Store block that emits time-series scores and wants honest self-evaluation.






Regex-based NLU intent router with active-learning feedback loop
handleChatMessage() (103-369) is a real deterministic intent classifier: ticker extraction, pending-flow state machine, ~10 regâ€¦


src/commands/chat.ts (369 lines), src/services/learning.ts (60 lines), src/db.ts (logChatIntent/getLearningStats/getMissedIntents)

handleChatMessage() (103-369) is a real deterministic intent classifier: ticker extraction, pending-flow state machine, ~10 regex-based intent rules (help/portfolio/watchlist/simulate/metrics/mimic/alert/news/score/search), with graceful fallback to DuckDuckGo web search and then to a Telegram inline-keyboard 'correct my intent' UI whose clicks feed back into logChatIntent(). learning.ts's weekly cron report (16-58) surfaces fallback rate, per-intent counts, and admin-corrected mislabels - a genuine intent-classification feedback loop, not just a chatbot wrapper.

RecommendationExtract the regex-intent-router + correction-logging pattern as a 'rule_based_intent_router' Store block usable ahead of/alongside an LLM router; the weekly-report cron as a companion 'intent_accuracy_report' block.







cluster Â· bim-construction 2




MEP coordination orchestration layer (Coordinator / ZoneResolver / Monitors / review workflow)
app/agents/coordinator.py (1107 lines) docstring: 'owns the model, the zones, and every decision that spans two..


bim-manager-agent · app/agents/coordinator.py, app/agents/zone_resolver.py, app/monitors/, app/review.py, app/rules/approval.py

app/agents/coordinator.py (1107 lines) docstring: 'owns the model, the zones, and every decision that spans two... the only thing allowed to decide anything that crosses a boundary.' Imports the vendored kit directly (from app.blocks import bcf_export, clash_triage, clearance_rules, model_clone, version_diff) and layers zoning (plan_zones, buffer_gids, neighbours_of), arbitration (ArbitrationResult), and diffing (DiffResult) on top. VENDOR.lock pins the kit at commit 5c0711a066b6eef5e0861cbfa0d793972b719c83 (15 files), with a vendor_kit.py --check script in CI that fails if anything under vendor/ was hand-edited â€” i.e. a disciplined, real vendoring practice, not a copy-paste fork.

RecommendationOnce mep_coordination is wired into the Store (see prior finding), extract bim-manager-agent's Coordinator/ZoneResolver/Monitors/review-ledger layer as a new orchestration block/service sitting on top of it â€” this is the piece that turns a clash-detection library into an actual usable coordination workflow.






ThreadForge â€” EPC piping digital thread (DEXPI/P&ID â†’ topology â†’ piping â†’ AWP â†’ 4D)
src/threadforge/clash.py:1-25 â€” pipe clash via 'capsule / AABB / cylinder geometry', segment-segment closest-point math citingâ€¦


ThreadForge · src/threadforge/{ingest_dexpi.py, dexpi_core.py, routing.py, generators.py, spooling.py, mto.py, clash.py, schedule_4d.py, pcf_reader.py, hydrotest.py, exporters/}

src/threadforge/clash.py:1-25 â€” pipe clash via 'capsule / AABB / cylinder geometry', segment-segment closest-point math citing Ericson's 'Real-Time Collision Detection Â§5.1.9', and a cited EPC industry rule for valve access ('ACCESS_RULE = "PIP PNF0200 / EPC practice: 600 mm hemisphere at valve handwheel"') â€” same honest-sourcing discipline seen in bim-manager-agent. Test suite includes tests/test_hypothesis_geom.py (property-based testing), tests/golden/ (golden-file regression tests), scripts/mutation_probes.py, and per-milestone test files (test_dexpi_m1.py, test_clash_m2.py, test_coverage_m6.py) suggesting a real staged build process, not a single LLM-generated dump. README explicitly scopes what it does NOT claim (WALLS.md covers proprietary-CAD and full-DEXPI-XSD hard limits) â€” an honesty signal consistent with real engineering rather than a sophistication claim.

RecommendationExtract as a new domain kit (e.g. 'piping_digital_thread' or under a new 'process_plant'/'epc' vertical) rather than folding into the existing 'construction' kit â€” the domain (process/industrial piping) and input formats (DEXPI P&ID, PCF) are distinct from the Store's building-AEC focus (BOQ/IFC/schedule/documents).







cluster Â· finance 3




finance_planning (FP&A: budget/forecast/scenario/workforce/allocation)
Cerebrum-FinanceOps backend/app/planning/service.py (627 lines, real): Budget lifecycle with draft/locked status and copy-on-wrâ€¦


Cerebrum-FinanceOps · backend/app/planning/service.py

Cerebrum-FinanceOps backend/app/planning/service.py (627 lines, real): Budget lifecycle with draft/locked status and copy-on-write versioning (update_budget:129-178 creates a new version rather than mutating a locked budget); lock_budget is approval-gated via require_approved_action; Forecast draft/final lifecycle; calculate_workforce_cost:283-290 (headcount * fte * salary_per_fte * (1+benefits_rate) with Decimal quantization); a real cost-allocation engine (run_allocation/process_allocation_run:341-454) that distributes total_amount across active DimensionMember rows with exact-remainder handling so the sum is always exact; and Scenario/ScenarioAssumption what-if planning (publish_scenario is approval-gated, apply_scenario_to_forecast:577-626 applies a rate-based multiplier from the closest-matching assumption). None of finance_canonical_model/finance_import/finance_data_quality/finance_reconciliation/finance_coa_governance/finance_saas_metrics/finance_v2/finance_ops cover budgets, forecasts, scenarios, workforce cost, or cost allocation at all.

RecommendationExtract Cerebrum-FinanceOps's planning module into a new Store block/container (e.g. finance_planning or finance_fpa), covering budget/forecast versioning+locking, workforce cost calculation, dimension-driven cost allocation with exact-remainder distribution, and scenario/assumption-based forecast adjustment -- register it under finance_ops's requires alongside the existing six blocks.






finance_reconciliation_workflow (run/exception state machine)
Cerebrum-FinanceOps backend/app/reconciliation/service.py: process_reconciliation_run:468-592 is @audited_action-decorated, driâ€¦


Cerebrum-FinanceOps · backend/app/reconciliation/service.py

Cerebrum-FinanceOps backend/app/reconciliation/service.py: process_reconciliation_run:468-592 is @audited_action-decorated, drives a pending->processing->completed/failed ReconciliationRun state machine across run_type in {actuals_vs_budget, actuals_vs_forecast, bank_vs_ledger, intercompany}, applies per-scope tolerance rules (ReconciliationMatchRule with tolerance_amount/tolerance_percent), computes control totals with Decimal, and both directly diffs actuals-vs-plan AND (327-429) separately invokes the vendored finance_reconciliation Store block via BlockInvoker and merges its variances/left_only/right_only into deduplicated ReconciliationException rows (_ensure_exception avoids creating duplicate open exceptions). This run/exception/tolerance-rule/dedup layer is real product code with no Store block behind it.

RecommendationPackage the run/exception state machine (tolerance rules, idempotent exception dedup, background-job trigger) as a companion Store block or container that wraps finance_reconciliation, so future Factory products get the persistent workflow instead of re-implementing _ensure_exception-style dedup logic each time.






approval-gated governance workflow (ApprovalRequest / AiUseCase / ModelGovernance)
Cerebrum-FinanceOps backend/app/governance/service.py:40-172: require_approved_action raises HTTP 403 fail-closed unless an Appâ€¦


Cerebrum-FinanceOps · backend/app/governance/service.py

Cerebrum-FinanceOps backend/app/governance/service.py:40-172: require_approved_action raises HTTP 403 fail-closed unless an ApprovalRequest with matching (tenant_id, action_type, target_id) exists and status=='approved'; create/approve/reject_request enforce status transitions (can't approve/reject a non-pending request); action_type is validated against a fixed set {coa_activation, journal_post, budget_lock, scenario_publish, export_distribution}. Separately, create_ai_use_case/create_model_governance_record/approve_model (177-282) implement a real AI-model risk-tier register (risk_tier in {low,medium,high}, validation_status in {pending,approved,failed}). Both are consumed by finance.py's activate_coa_version and planning.py's lock_budget/publish_scenario, i.e. this is load-bearing product code, not decoration.

RecommendationConsider extracting the approval-request pattern as a general-purpose governance/approval block (not finance-specific, since the mechanism is domain-agnostic) so other verticals in the Factory can gate risky actions the same fail-closed way finance_coa_governance's activation and finance_planning's budget-lock/scenario-publish do here.







cluster Â· insurance 6




insurance
This is a thin routing container (not a block_registry adapter -- it has only a block.json, no .py adapter, which is correct beâ€¦


Cerebrum-Blocks · app/containers/insurance.py

This is a thin routing container (not a block_registry adapter -- it has only a block.json, no .py adapter, which is correct because it's a DomainContainer, not a UniversalBlock). Its 6 actions (analyze, extract_entities, calculate_metrics, check_compliance, score_risk, health) all delegate to self._resolve_block("insurance_v2") and slice the real result. It carries zero independent logic by design -- the real work is in insurance_v2, which was verified separately below.

RecommendationNone. Confirm this is intentional container-pattern design (matches other domain containers in the Store) rather than treat the thinness as a red flag.






insurance_v2
615 lines of real deterministic regex-based entity extraction (policy/claim numbers, insured names, brokers, carriers, coverageâ€¦


Cerebrum-Blocks · app/blocks/insurance_v2.py

615 lines of real deterministic regex-based entity extraction (policy/claim numbers, insured names, brokers, carriers, coverage types, limits, deductibles), Decimal-safe metric formulas (loss ratio, combined ratio, IBNR, rate-on-line, retention ratio), keyword-driven compliance checks against 5 real regulatory keyword sets (Solvency II, NAIC, GDPR, AML, claims-handling) in app/core/insurance_knowledge.py, and keyword-driven risk scoring across 5 categories, all input-dependent (not a vacuous always-pass gate -- confidence and detected flags vary with keyword hits in the text). Byte-identical to InsureOps' production-vendored copy at kit/bundle/app/blocks/insurance_v2.py, which InsureOps runs live at https://insureops-api.onrender.com for a real HK pilot tenant.

RecommendationNone. Heuristic/keyword-based scoring is a legitimate deterministic-block design choice (self-documented as "no LLM calls, no external APIs"), consistent with the rest of the Store's Layer-3 blocks.






agency_hierarchy
402 lines, typed hierarchy model (TypedDict nodes/edges/paths), role validation, effective-dated nodes for agent/agency/MGA-FMOâ€¦


Cerebrum-Blocks · app/blocks/agency_hierarchy.py

402 lines, typed hierarchy model (TypedDict nodes/edges/paths), role validation, effective-dated nodes for agent/agency/MGA-FMO/carrier distribution trees. Byte-identical to InsureOps' kit/bundle copy, which InsureOps' compensation/service.py invokes directly (invoke_block("agency_hierarchy", ...)) as the source of truth for its production hierarchy persistence layer.

RecommendationNone.






bordereaux_ingest
414 lines


Cerebrum-Blocks · app/blocks/bordereaux_ingest.py

414 lines. Explicitly self-documents as a simulated-feed parser ("does not integrate with live carrier APIs") -- this is honest scoping, not a disguised stub. Real field-alias normalization table (11 canonical fields x 3-6 aliases each), CSV/JSON parsing, required-field validation. Byte-identical to InsureOps' vendored copy. InsureOps' backend/app/insureops/integrations/service.py builds real persistence (FeedIngestJob, ConnectorStub models) and explicitly-labeled "Simulated Guidewire/SFDC/Applied Epic/VUE" connector stubs on top of it -- InsureOps itself has not built (and does not claim to have built) live carrier connectors either, so there is no hidden richer donor implementation being withheld from the Store.

RecommendationNone for this cluster. (If a future cluster wants live carrier-feed connectors, that would be new work, not an extraction from InsureOps -- InsureOps's connectors are equally simulated.)






incentive_targeting
293 lines: real eligibility gating (retention >= 0.82, loss_ratio <= 0.68, production >= 50000 OR growth >= 5%), a real bonus fâ€¦


Cerebrum-Blocks · app/blocks/incentive_targeting.py

293 lines: real eligibility gating (retention >= 0.82, loss_ratio <= 0.68, production >= 50000 OR growth >= 5%), a real bonus formula (min(production*0.015 + incremental_premium*0.01 + strategic_products*250, 7500)), producer ranking and budget simulation. Byte-identical to InsureOps' vendored copy. InsureOps' retention/service.py cites this exact eligibility/bonus formula verbatim in its own docstring as its production methodology ("Honest math only"), confirming the Store block is the actual logic running in InsureOps' live product, not a stand-in for something better.

RecommendationNone.






distribution_analytics
410 lines: CSV/row parsing and normalization, summary/benchmark/trend computation, and a retention_proxy calculation


Cerebrum-Blocks · app/blocks/distribution_analytics.py

410 lines: CSV/row parsing and normalization, summary/benchmark/trend computation, and a retention_proxy calculation. Byte-identical to InsureOps' vendored copy. InsureOps' analytics/service_exec.py explicitly cites this block's retention_proxy output as the primary source for its executive persistency metric (falling back to a chargeback-ratio proxy only when the block call fails), confirming it is load-bearing production logic, not filler.

RecommendationNone.







cluster Â· retail 1




Honest-stub enterprise connector contract (pos, inventory_wms, sap_erp, crm_loyalty, supplier_edi, entra_id, power_bi, store_digital_twin)
base.py docstring (lines 1-16): defines a shared async contract (health/test_connection/pull/normalize/push) with an explicit Câ€¦


TEKsystems_GlobalRetailMNC · backend/app/retailops/integrations/base.py and backend/app/retailops/integrations/connectors/*.py

base.py docstring (lines 1-16): defines a shared async contract (health/test_connection/pull/normalize/push) with an explicit ConnectorStatus enum (disabled/mock/configured/connected/degraded/error) and states 'No credentials are hardcoded... a missing/invalid credential degrades honestly... rather than fabricating vendor data.' pos.py (130 lines) docstring: 'Mock-functional: deterministic fictional transactions... no real vendor API calls... this pilot does not ship vendor-specific client code, so real pulls still raise ConnectorError honestly rather than pretending to fetch live data.' store_digital_twin.py (108 lines) docstring: 'honest stub with a documented data shape... pull()/push() never fabricate planogram or fixture data -- they raise ConnectorError.' Total connector code: 1,779 lines across 8 files (crm_loyalty.py 127, entra_id.py 94, inventory_wms.py 150, pos.py 130, power_bi.py 231, sap_erp.py 94, store_digital_twin.py 108, supplier_edi.py 121), all following the same disciplined mock-vs-real pattern -- this is exactly the kind of gate-vs-real distinction this survey is meant to catch, and here it's on the honest side.

RecommendationExtract this connector contract (base.py's ConnectorStatus/ConnectorError/GenericRestMixin pattern) plus the 8 connector implementations as a new Cerebrum block or block family (e.g. retail_connectors_v1) so retail-cluster platforms get real POS/WMS/ERP/CRM/EDI integration surfaces with the same honesty discipline (mock_functional flag, raise-don't-fabricate) instead of only document-text analysis via retail_v2.







cluster Â· connector-infra 1




microsoft_365_connector
354 real lines: a Microsoft365Connection DB model plus Microsoft365Service with typed request/response models for Teams meetingâ€¦


Cerebrum · backend/app/integrations/microsoft_365.py

354 real lines: a Microsoft365Connection DB model plus Microsoft365Service with typed request/response models for Teams meetings (TeamsMeetingRequest/Response), SharePoint folder access (SharePointFolderRequest), and Outlook calendar events (OutlookEventRequest), all against https://graph.microsoft.com/v1.0 with scopes Calendars.ReadWrite and Chat.ReadWrite. This is a distinct capability domain from the Store's onedrive.py (which only covers OneDrive *files*, scope Files.Read) -- Teams/SharePoint/Calendar have no equivalent anywhere in Cerebrum-Blocks.

RecommendationNot an upgrade to an existing block -- this is net-new surface (Teams meetings, SharePoint, Outlook calendar) that could become a new microsoft_teams_calendar (or similar) block in the Store if the enterprise-connector roadmap needs Microsoft 365 beyond files.







cluster Â· core-infra 2




universal_kernel wave1 trust-spine kits (already exist -- do not re-extract)
kernel_wave1_trust_spine.md (CerebrumDev.ai) frames Wave 1 as an UNBUILT extraction mission targeting six capabilities


Cerebrum-Blocks · block_store/kits/universal_kernel/wave1/{identity,authorization_policy,scope_guard,rate_limit_guard,audit_evidence,provenance_verification}/code.py

kernel_wave1_trust_spine.md (CerebrumDev.ai) frames Wave 1 as an UNBUILT extraction mission targeting six capabilities. But block_store/kits/universal_kernel/wave1/ in Cerebrum-Blocks already contains all six, each with code.py + kernel_manifest.json + tests/, catalogued in block_store/kits/universal_kernel/manifest.json as a premium-tier kit. I independently recomputed sha256 for all six code.py files with sha256sum and every manifest hash matched exactly (no forged/stale provenance hashes). Read each kit's code in full: audit_evidence/code.py implements REAL hash-chaining (previous_hash linkage, per-record SHA-256, verify_chain that RAISES AuditIntegrityError on tampered records or broken links -- confirmed by its test suite which asserts the raise, not a status:'ok' passthrough). scope_guard/code.py implements a real tenant/project/resource allow-list check (ScopeViolation raised on out-of-scope access). identity/code.py uses real bcrypt password hashing + PyJWT tokens with fail-closed missing-secret handling (KernelConfigurationError if UNIVERSAL_KERNEL_IDENTITY_SECRET absent). authorization_policy/code.py implements a real role->permission graph with fnmatch action/resource matching and explicit-deny precedence. provenance_verification/code.py does real per-file SHA-256 + deterministic root-hash tree verification (this is the general form of what migration.py/version.py should have been using instead of their TODO stubs). All six are exercised together in tests/universal_kernel/test_trust_chain.py, a 214-line end-to-end integration test spanning Wave-1 + Wave-4 (approval_action), and app/core/container_kit_store.py imports provenance_verification directly -- this is live, wired code, not dead scaffolding.

RecommendationPoint CerebrumDev.ai's Wave-1 trust-spine mission at this existing, tested, hash-verified kit set rather than commissioning new extraction work. If anything is still needed it is (a) wiring these kits INTO the core-infra blocks above (auth.py has no tenant/project scope_guard call; migration.py/version.py could use provenance_verification's file-hash pattern for real integrity checks instead of the fake TODOs) and (b) reconciling with app/blocks/auth.py which is a separate, coarser API-key/RBAC block that does not use these kits at all.






isolation.py != scope_guard (naming collision worth flagging)
isolation.py's own docstring: 'RLIMIT_AS caps VIRTUAL address space..


CerebrumDev.ai · backend/app/cerebrum_product_kernel/isolation.py

isolation.py's own docstring: 'RLIMIT_AS caps VIRTUAL address space... an ABSOLUTE ceiling of 1536 MB was breached the instant the child started... That shipped (The_Fork, after the isolation change).' This is real, battle-tested process-isolation code worth preserving as-is, but it answers a completely different question than block_store/kits/universal_kernel/wave1/scope_guard/code.py (tenant/project/resource access control).

RecommendationNo action required beyond noting the name overlap so nobody assumes CerebrumDev.ai's 'isolation' kernel module already satisfies Wave-1's scope_guard requirement -- it doesn't; they solve different problems.







cluster Â· reasoning-orchestration 1




validation_pipeline (Fork's Pint/empirical-ranges variant)
Cerebrum-Blocks' own docs/validation_block_mapping.md states outright: 'Fork's standalone ValidationPipelineBlock adds Pint dimâ€¦


The_Fork · app/blocks/validation_pipeline.py (745 lines) + config/empirical_ranges.json (verified present via clone, HEAD of default branch)

Cerebrum-Blocks' own docs/validation_block_mapping.md states outright: 'Fork's standalone ValidationPipelineBlock adds Pint dimensional analysis and file-backed empirical ranges... Kit agents that need Fork's Pint checker should install the construction kit bundle or call Fork's block directly.' Verified by cloning The_Fork (shallow, current default branch): app/blocks/validation_pipeline.py is 745 lines and imports pint directly (import pint; _UREG = pint.UnitRegistry()), and config/empirical_ranges.json (35 lines) exists as its companion data file. Neither the file nor its Pint-based logic exists anywhere in Cerebrum-Blocks.

RecommendationExtract The_Fork's app/blocks/validation_pipeline.py + config/empirical_ranges.json into Cerebrum-Blocks as a genuine block (or as an additional stage mode on the existing validation.py ValidationBlock) so its real dimensional/empirical-range checking becomes available platform-wide instead of Fork-only. This is also directly relevant to REASONING_KERNEL.md's UnitAndCurrencyValidator (#6) â€” a more complete implementation already exists, just not in the Store.










Behind its donor
Store upgrades


A block already exists in the Store but is thinner, unregistered, or orphaned relative to a more mature version in a donor repo — grouped by the cluster that assessed it.



repo Â· Cerebrum-Steward 1




Hybrid RRF retrieval (pgvector cosine + Postgres full-text + Reciprocal Rank Fusion) with fail-closed embedding-fingerprint guard
The vendored Store adapter (vendor/blocks/vector_search/block.py, 40 lines) is confirmed to be the standard uniform pass-througâ€¦


Cerebrum-Steward · app/steward/retrieval.py, app/steward/embedding_guard.py, app/steward/embeddings.py

The vendored Store adapter (vendor/blocks/vector_search/block.py, 40 lines) is confirmed to be the standard uniform pass-through adapter with zero domain logic (calls get_block('vector_search').execute()). By contrast, app/steward/retrieval.py implements: (1) real pgvector cosine-distance semantic search (_semantic, lines 56-80, using DocumentChunk.embedding.cosine_distance()); (2) real Postgres lexical search using websearch_to_tsquery + ts_rank_cd (_lexical, lines 83-107); (3) a correct Reciprocal Rank Fusion combining both ranked lists (reciprocal_rank_fusion, lines 109-121, entry['fused'] += 1.0/(k+rank)); (4) two-layer merge logic (combined_layer_search, lines 195-243) that queries an estate-specific layer and a shared platform layer separately, tags each hit with authority_kind/cite_as_policy, and reports an explicit 'insufficiency' flag rather than guessing. embedding_guard.py additionally fails closed with EmbeddingMismatchError if the stored corpus's embedding fingerprint doesn't match the active embedder, and raises if STEWARD_REQUIRE_PRODUCTION_EMBEDDINGS is set but the real embed backend isn't loaded.

RecommendationUpgrade the Store's vector_search/knowledge blocks (or add a new 'hybrid_retrieval' block) with: (a) RRF fusion of vector + lexical search, (b) a fail-closed embedding-fingerprint compatibility guard, (c) a layered/tenant+scope citation model with an explicit insufficiency signal.







repo Â· StockWisePro 3




API key management: scoped permissions, IP allowlisting, expiry, per-request usage log, key rolling
Store's auth.py grants API-key permissions only from a hardcoded 4-role table (self.block_permissions = {Role.ADMIN: ["*"], Rolâ€¦


StockWisePro · app/server/src/routes/apiKeys.ts (394 lines) + app/server/src/utils/encryption.ts (generateApiKey/hashApiKey) + schema.prisma ApiKey/ApiKeyLog models

Store's auth.py grants API-key permissions only from a hardcoded 4-role table (self.block_permissions = {Role.ADMIN: ["*"], Role.PRO: [...], ...}, lines 51-56) with no per-key custom scopes, no IP restriction, and no persisted per-request usage log (it only tracks aggregate rate-limit counters in an in-memory dict wired to a generic 'memory' block). StockWisePro's apiKeys.ts instead lets each key carry its own permissions: string[] (e.g. ['stocks:read','watchlist:read'], line 113), an allowedIps: string[] allowlist validated via zod z.string().ip() (line 16), a per-key expiresAt computed from expiresInDays (lines 100-104), and a persisted ApiKeyLog row per API call (endpoint/method/statusCode/responseTime/ipAddress) queryable via /:id/logs (lines 264-316) â€” none of which exist in auth.py. It also implements atomic key-rolling (/:id/roll, lines 319-391: revoke old + issue new with copied settings) which auth.py's rotate_key does not do.

RecommendationUpgrade auth.py's API-key actions to add: per-key scope strings instead of the 4 fixed roles, an allowed_ips check in _validate_key, an expiry timestamp check, a persisted per-call usage-log write, and a roll_key action that copies scopes/allowlist/expiry onto a freshly generated key while revoking the old one. StockWisePro's Prisma schema (ApiKey/ApiKeyLog) is a usable reference shape.






Persistent, queryable webhook delivery history and per-org webhook limits
Store's webhook.py keeps registered endpoints only in self.endpoints = {} (an in-process dict) â€” registrations are lost on everâ€¦


StockWisePro · app/server/src/routes/webhooks.ts (505 lines), schema.prisma Webhook/WebhookDelivery models

Store's webhook.py keeps registered endpoints only in self.endpoints = {} (an in-process dict) â€” registrations are lost on every restart and there is no delivery-history persistence at all (_send_webhook just returns a result dict; nothing is written to any store). StockWisePro persists both the webhook (prisma.webhook.create, webhooks.ts:138) and every delivery attempt (prisma.webhookDelivery.create, lines 373-390, capturing requestHeaders/requestBody/responseStatus/responseHeaders/responseBody/responseTime/status/errorMessage), exposes a paginated /:id/deliveries history endpoint (lines 411-468) and a per-org webhook-count cap read from org.maxWebhooks (lines 121-133) enforced before creation. Store's webhook.py has no persistence layer, no delivery-history query surface, and no per-tenant limit.

RecommendationWire webhook.py's _register_webhook/_send_webhook to the platform's database block instead of an in-memory dict, add a delivery-record write on every send (success and failure), and expose a paginated delivery-history action â€” using WebhookDelivery's field set as the reference schema.






Rate limiter must actually use a shared/distributed counter store, not an in-process dict
rate_limiter.py declares requires = ["memory", "database"] (line 30) but every counter it actually touches is self.counters: Diâ€¦


StockWisePro · app/server/src/middleware/rateLimiter.ts (201 lines)

rate_limiter.py declares requires = ["memory", "database"] (line 30) but every counter it actually touches is self.counters: Dict[str, Dict] = {} (line 52) and self.buckets/self.custom_limits, also plain instance dicts (every read/write goes through self.counters[...], never self.database_block or a cache client) â€” meaning declared dependencies are unused and limits are per-process: on any horizontally-scaled deployment each process enforces its own separate limit, defeating the purpose of the block, and all counters reset on restart. StockWisePro's rateLimiter.ts instead does real distributed limiting: const pipeline = redis.pipeline(); pipeline.incr(windowKey); pipeline.expire(windowKey, ...); const results = await pipeline.exec(); (lines 65-70), atomic and shared across every process talking to the same Redis instance, plus plan-tiered limits (FREE/STARTER/PROFESSIONAL/ENTERPRISE, lines 12-17) and per-endpoint overrides (lines 20-24).

RecommendationFix rate_limiter.py to actually satisfy its declared database/memory dependency â€” back _check_sliding_window/_check_token_bucket/etc. with the platform's shared cache/database block instead of self.counters, using StockWisePro's Redis INCR+EXPIRE pipeline as the concrete pattern. This is a correctness gap, not just a feature gap.







repo Â· TEKsystems_GlobalRetailMNC 1




Multi-format document export engine (dependency-free PDF + DOCX + XLSX)
_build_pdf() (lines 241-311) hand-writes a valid multi-page PDF from scratch with no external PDF library: constructs PDF objecâ€¦


TEKsystems_GlobalRetailMNC · backend/app/retailops/exports/__init__.py (486 lines)

_build_pdf() (lines 241-311) hand-writes a valid multi-page PDF from scratch with no external PDF library: constructs PDF objects (Catalog/Pages/Page/Font/Content streams), a correct xref offset table, and a trailer â€” real low-level PDF construction, not a wrapper call. _build_docx()/_build_xlsx() (lines 108-184) use python-docx/openpyxl to build real structured documents. document_from_operations_brief / document_from_inventory_risk / document_from_learning_summary (lines 359-486) assemble these exports directly from live action outputs. At 486 lines of working format-generation logic this exceeds the stated 13-line registered document_engine.py by a wide margin and even the 264-line unregistered document_engine_block; verifying whether document_engine_block already covers this PDF-from-scratch path is the concrete next step before replacing it.

RecommendationDiff this exports/__init__.py against app/blocks/document_engine_block.py; if the latter lacks a dependency-free PDF writer, port _build_pdf/_build_docx/_build_xlsx into the Store block and route document_engine.py's registered adapter to the richer implementation.







repo Â· The_Fork 1




Construction-contract RAG retrieval vs generic vector_search/knowledge
The_Fork's retriever does clause-level rescue matching (parent-company-guarantee, commencement-date, schedule-register, spec-tiâ€¦


The_Fork · app/core/rag/retriever.py, app/core/rag/vector_store.py, app/core/rag/coverage_honesty.py

The_Fork's retriever does clause-level rescue matching (parent-company-guarantee, commencement-date, schedule-register, spec-title-identity detectors with explicit chunk_states_* validators), enforces disclosed partial-index honesty (coverage_honesty.py), and has a documented, named production data-leak fix for cross-tenant isolation. A generic Store vector_search/knowledge block, by its registry-adapter description ('wraps app.blocks.X into a synchronous run()'), is very unlikely to carry any of this domain-specific rescue logic or the tenant-isolation incident fix -- this is a functional maturity gap, not just more lines. Recommend the orchestrating agent diff directly against the Store's actual vector_search/knowledge app/blocks files to confirm before replacing wholesale.








repo Â· bim-manager-agent 1




LLM rule extraction with programmatic quote verification and human-approval gate
rule_extraction.py:81-88 does a real substring check -- normalizes whitespace/case and requires the LLM's claimed quote to liâ€¦


bim-manager-agent · app/llm/rule_extraction.py (_validate() L73-88, write_pending()/approve_pending() L125-165)

rule_extraction.py:81-88 does a real substring check -- normalizes whitespace/case and requires the LLM's claimed quote to literally appear in the source document text (quote not in haystack, L87) before a candidate rule is kept; candidates that pass are still approved: False until write_pending()/approve_pending() (L125-165) moves only rows a human has manually flipped to approved: true -- confirmed by tests/unit/test_llm_boundary.py:113-152 showing an invented quote gets dropped and an unapproved candidate never reaches the rule table. By contrast, the Store's app/blocks/llm_enhancer.py only instructs the LLM via prompt text to cite a source_quote (e.g. lines with '<exact text>' placeholders and 'Do not invent numbers' instructions) with no programmatic check anywhere in the file that a returned quote actually appears in the source, and no human-approval gate before output is used -- it trusts the model's self-reported citation.

RecommendationPort the _validate() substring-verification step and the pending/approved two-file human-gate pattern into llm_enhancer.py (or a shared app.llm-style module) so any Store block that asks an LLM to extract facts/rules with citations can verify the citation is real before use, rather than only instructing the model not to invent one.







repo Â· cerebrum-hotelops-v2 1




RFM guest segmentation / CRM / loyalty-tier logic
guest_intelligence/segmentation.py:9-33 rfm_score() computes real recency/frequency/monetary bucket scores and a segment labelâ€¦


cerebrum-hotelops-v2 · guest_intelligence/segmentation.py (rfm_score, RfmScorer), guest_intelligence/crm.py, guest_intelligence/loyalty.py

guest_intelligence/segmentation.py:9-33 rfm_score() computes real recency/frequency/monetary bucket scores and a segment label (champions/loyal/promising/at_risk). Note a leftover dead computation around line 26 (an f value computed once, discarded, then recomputed under a 'simpler deterministic ladder' comment) -- a code-quality smell, not a correctness bug since the dead value is overwritten before use and never read.








cluster Â· document-ocr-pdf 3




drawing_qto
The_Fork's app/blocks/drawing_qto.py has all of this working in production: _try_convert_dwg (lines 1399-1509) actually shellâ€¦


Cerebrum-Blocks · app/blocks/drawing_qto.py

The_Fork's app/blocks/drawing_qto.py has all of this working in production: _try_convert_dwg (lines 1399-1509) actually shells out to the ODA File Converter CLI with xvfb virtual-display handling, env-scrubbing for the subprocess ("audit Â§6.1"), and per-file .err-report diagnostics on failure -- not a stub, a real converter integration with graceful degradation when the CLI isn't installed. _extract_from_pdf (lines 397-587) pulls vector geometry straight from PDF drawings via PyMuPDF's page.get_drawings(), with _extract_rooms doing room-label + OCR-fallback extraction for text-free/scanned plots (calls app.core.doc_index._ocr_pdf_page when the text layer is thin), _extract_title_block (~165 lines, 1055-1220) parsing sheet number/discipline/title-block metadata, _classify_drawing_zone and _discipline_from_number classifying by discipline, and _extract_cross_refs pulling cross-sheet references. None of these eight methods exist in the Store's copy. Store's copy also never calls app.core.file_crypto.open_plaintext, so it can't read files that are encrypted at rest (Fork's does, at multiple points including inside _try_convert_dwg). The one thing Store has that Fork lacks is merge_drawings (cross-sheet line-continuity stitching) -- worth Fork pulling back, but that doesn't offset the gap above.

RecommendationPort The_Fork's _extract_from_pdf, _extract_rooms, _extract_title_block, _classify_drawing_zone, _discipline_from_number, _extract_cross_refs, _try_convert_dwg, and the open_plaintext wrapping into the Store's drawing_qto.py, keeping Store's merge_drawings. As shipped, any client platform built from the Store block can only take quantities off raw DXF files -- DWG and PDF drawings (the overwhelming majority of real submittals) are refused.






document_engine_block
1) Store's DocumentEngineBlock defines _parse_with_platform_ocr but never calls it anywhere in process() (grep confirms zeroâ€¦


Cerebrum-Blocks · app/blocks/document_engine_block.py (+ app/blocks/document_engine/{reasoner.py,mapper.py})

1) Store's DocumentEngineBlock defines _parse_with_platform_ocr but never calls it anywhere in process() (grep confirms zero call sites) -- dead code. Some OCR fallback still happens indirectly because the "pdf" block it delegates to has its own internal tesseract fallback, but Fork's explicit second-layer call through the richer "ocr" block also captures markup/redline detection that pdf.py's internal fallback doesn't do. 2) Store's output never surfaces the raw parsed document text/sheets -- only the reasoner/mapper's structured output (glossary/requirements/constraints/etc, which default to generic construction-ontology values). Fork's version explicitly fixes this (see the long comment at its raw_chunks block): "a BOQ-style xlsx upload reaches the LLM as 4 kB of generic equipment defaults with zero rows from the actual file" -- a documented, previously-real bug that Store's copy still has. 3) Store has no inline-brief (text-only, no file) path; Fork does, for the no-upload scope-extraction flow. 4) Store never calls open_plaintext -- can't read encrypted-at-rest pdf/docx/xlsx inputs; Fork wraps every parser call in it. 5) Store's app/blocks/document_engine/reasoner.py._map_wbs does mapping[target_code].append(...) against a dict pre-seeded only from config's wbs_dictionary keys -- if _wbs_code_for_category or the constraint-unit mapping ever returns a code absent from a client's custom wbs_dictionary, this is a raw KeyError that kills the whole reasoning pass. Fork's reasoner.py already hardened this to mapping.setdefault(target_code, []).append(...), with a comment citing a real probe ("found by the W4 planted-RFP probe with a minimal config") that hit exactly this crash. Store's default config.yaml happens to define codes 5.0/7.0 so the crash doesn't fire on the shipped defaults, but any Factory-generated client with a trimmed WBS dictionary is exposed.

RecommendationSync app/blocks/document_engine_block.py and the document_engine/ package's reasoner.py, mapper.py, and parsers/pdf_parser.py from The_Fork's app/blocks/document_engine.py + app/document_engine/. Priority: the KeyError crash risk in _map_wbs (correctness bug, not just a feature gap) and the missing raw_text passthrough (silently degrades every cost/BOQ document to generic ontology defaults for downstream LLM consumers).






pdf
Fork's pdf.py wraps the actual fitz.open() call in with open_plaintext(pdf_path) as readable_path: so encrypted-at-rest PDFsâ€¦


Cerebrum-Blocks · app/blocks/pdf.py

Fork's pdf.py wraps the actual fitz.open() call in with open_plaintext(pdf_path) as readable_path: so encrypted-at-rest PDFs decrypt to a readable temp file first (app.core.file_crypto.open_plaintext). Store's pdf.py never imports or calls open_plaintext anywhere in its 356 lines (confirmed by grep across the file) -- every one of its three parser engines (pdfplumber/pypdf/PyMuPDF) and the Marker/xlsx/docx paths open pdf_path directly against the raw (possibly encrypted-at-rest) file.

RecommendationAdd the open_plaintext wrap around Store's file-opening paths in pdf.py (mirroring what ocr.py/ocr_v2.py in the same Store already do). This is the only substantive gap; everything else about Store's pdf.py is solid and ahead of the donor.







cluster Â· bim-construction 4




primavera_parser
Store primavera_parser.py: critical_path = [a for a in activities if a.get('total_float_days',999) <= float_threshold] â€” trusâ€¦


The_Fork · app/blocks/primavera_parser.py + app/lib/pm_computations.py

Store primavera_parser.py: critical_path = [a for a in activities if a.get('total_float_days',999) <= float_threshold] â€” trusts P6's own exported float, no graph computed. The_Fork's version imports from app.lib.pm_computations import compute_cpm, parse_xer_full and from app.schemas.cpm import CPMInput, producing cpm.total_duration_days, cpm.critical_path_activity_ids, cpm.near_critical_activity_ids, cpm.per_activity_float, plus calendar/TASKRSRC parsing (the_fork/app/blocks/primavera_parser.py:176-213). THE PUNCHLINE: app/lib/pm_computations.py (672 lines) and app/schemas/cpm.py (122 lines) ALREADY EXIST in Cerebrum-Blocks (both at app/lib/ and duplicated in block_store/kits/construction/bundle/app/lib/) â€” I grepped all of app/ for callers of compute_cpm/pm_computations and found ZERO. The CPM engine is sitting in the Store, unused, and is itself 146 lines behind The_Fork's current copy (672 vs 818 lines).

RecommendationWire primavera_parser.py's execute() to call the existing (but orphaned) app/lib/pm_computations.compute_cpm/parse_xer_full, matching The_Fork's integration, and sync app/lib/pm_computations.py + app/schemas/cpm.py from The_Fork's current version (818 lines) to close the 146-line gap.






boq_processor
Store: accept list is [".xlsx",".xls",".csv"]; hits `{"error": f"Unsupported format: {ext}


The_Fork · app/blocks/boq_processor.py

Store: accept list is [".xlsx",".xls",".csv"]; hits {"error": f"Unsupported format: {ext}. Use .xlsx or .csv"} on PDF, and hardcodes engine="openpyxl" for all excel reads. The_Fork adds .pdf to the accept list and a full _parse_pdf() method (~100 lines, the_fork/app/blocks/boq_processor.py:230+) using pdfplumber table-grid extraction with documented honest-failure behavior for scanned/non-tabular PDFs, plus engine = "xlrd" if ext==".xls" else "openpyxl" (the_fork line ~219) â€” Store's hardcoded openpyxl-only call will fail on legitimate legacy BIFF .xls BOQ files despite claiming to support them. Priced BOQ-as-PDF is a very common real-world delivery format in QS/construction workflows, so this is a material gap, not a cosmetic one. Same pattern shows up in the adjacent drawing_qto.py block (also part of the construction kit): Store's version is 857 lines vs The_Fork's 1963 lines, missing ~24 functions including _extract_from_pdf, _try_convert_dwg, title-block/room/cross-ref extraction, and Levenshtein-based OCR-artifact cleanup for scanned drawings.

RecommendationPort The_Fork's _parse_pdf() and the .xls engine fix into boq_processor.py; separately, port The_Fork's drawing_qto.py PDF/DWG extraction path (same kit, same gap pattern) since Store's version is DXF-only per its own container comment ('Delegate to DrawingQTOBlock: DXF quantity take-off').






spec_analyzer
Store: requires = ["pdf"] only, text-layer extraction only


The_Fork · app/blocks/spec_analyzer.py

Store: requires = ["pdf"] only, text-layer extraction only. The_Fork: requires = ["ocr"] plus an explicit fallback block (the_fork/app/blocks/spec_analyzer.py:127-150) triggered when extracted text falls under ocr_fallback_min_chars (200), with the developer's own comment stating the exact failure mode being fixed: 'Without this, _extract_grades / _extract_materials all return empty lists with status: success â€” the LLM gets "no specs found" for a perfectly valid scanned PDF.' This is precisely the vacuous-success pattern this survey is watching for, caught and fixed upstream but not yet ported to the Store. Function sets are otherwise identical between the two versions; The_Fork's standard-citation regexes are also broader (adds AS/Australian-Standard and IBC-year patterns, fixes BS-vs-BS-EN match ordering, fixes ASTM trailing-year-suffix matching).

RecommendationPort The_Fork's OCR-fallback block and the widened/corrected standards-citation regex set into spec_analyzer.py.






mep_coordination kit (unwired)
block_store/kits/mep_coordination/ contains bundle/app/blocks/*.py (9 modules), a full tests/ directory (test_bcf_export.py, teâ€¦


Cerebrum-Blocks · block_store/kits/mep_coordination/bundle/app/blocks/{geometry_engine,clearance_rules,clash_triage,clash_resolver,bcf_export,connectivity,model_clone,version_diff}.py

block_store/kits/mep_coordination/ contains bundle/app/blocks/*.py (9 modules), a full tests/ directory (test_bcf_export.py, test_clash_resolver.py, test_geometry_engine.py, test_cross_block_integration.py, etc.), routing/golden_matrix.json, fixtures, ACCEPTANCE.md, kit.json, manifest.json, README.md, and docs/MEP_KIT.md written like a shipped feature ('Finds true clashes and clearance violations... proposes checked and sourced resolutions, and hands off a BCF issue package'). geometry_engine.py does real exact-mesh clash+clearance judgement via ifcopenshell + manifold3d boolean intersection (AABB is only a pre-filter, not the decision) and explicitly names why it exists: 'The Fork's existing detector compares axis-aligned bounding boxes... An AABB is the smallest upright box containing a shape. For a diagonal pipe or a sloped duct the box is mostly empty air... That is a false positive.' clearance_rules.py refuses to load any rule missing a cited source clause (raises RuleWithoutCitation). clash_resolver.py explicitly states 'NO LLM IN THE DECISION PATH' and refuses/escalates unsourced moves rather than guessing. bcf_export.py writes real BCF 2.1 zip packages (markup.bcf + viewpoint.bcfv) for Navisworks/Solibri/BIMcollab. I confirmed via grep that app/core/domain_kit_loader.py's _KIT_BLOCK_SPECS dict (the actual source that feeds get_block()/BLOCK_REGISTRY) has no reference to any of these module/tool names â€” the kit is genuinely dead code from the platform's point of view, not just undocumented.

RecommendationAdd a _KIT_BLOCK_SPECS entry (own kit or folded into 'construction') registering these 8-9 tools, add matching block_registry/<name>/ adapters, and make bim_extractor's clash-detection action route through geometry_engine.py instead of its own toy check. This single wiring fix upgrades the platform's actual clash-detection capability by an order of magnitude with zero new code needed.







cluster Â· finance 3




finance_coa_governance
app/blocks/finance_coa_governance.py (261 lines) implements _hierarchy_issues (DFS cycle detection), _mapping_issues (interval-â€¦


Cerebrum-Blocks · app/blocks/finance_coa_governance.py

app/blocks/finance_coa_governance.py (261 lines) implements _hierarchy_issues (DFS cycle detection), _mapping_issues (interval-overlap check per old_account_id), and _resolve_mapping (effective-dated lookup) entirely against caller-supplied JSON arrays -- no CoA version concept, no activation state, no approval gate. Cerebrum-FinanceOps backend/app/finance/service.py:78-137 has a real CoaVersion lifecycle: create_coa_version -> activate_coa_version, which is @audited_action-decorated, calls require_approved_action(db, tenant_id, 'coa_activation', coa_version_id) from backend/app/governance/service.py, enforces 'Only draft CoA versions can be activated', and enforces exactly one active version per tenant/project scope via a DB query before flipping status. That whole draft/active/approval-gated lifecycle has no equivalent anywhere in the Store block.

RecommendationExtend finance_coa_governance (or add a companion finance_coa_lifecycle block) with a stateful draft/active CoA-version concept and an approval-gate hook, modeled directly on Cerebrum-FinanceOps's activate_coa_version + require_approved_action pattern, so Factory-generated products don't have to re-implement CoA activation state machines from scratch the way Cerebrum-FinanceOps did.






finance_import
app/blocks/finance_import.py's _normalize_rows takes data['rows'] as a Python list and returns accepted/rejected in memory; tâ€¦


Cerebrum-Blocks · app/blocks/finance_import.py

app/blocks/finance_import.py's _normalize_rows takes data['rows'] as a Python list and returns accepted/rejected in memory; there is no CSV/XLSX handling anywhere in the file. Cerebrum-FinanceOps backend/app/finance/service.py:317-459 has real file ingestion: _detect_file_type/_parse_import_file (pandas CSV/XLSX parsing), _apply_column_mapping, upload_import_file (creates ImportBatch/ImportRow rows, enqueues a BackgroundJob), and process_import_batch:531-610 which is idempotent (resets prior validation state before re-running), resolves entity_code to LegalEntity, and -- critically -- refuses silent FX conversion: _import_actuals_row:668-682 raises 'Currency conversion requires explicit rate_used' if the record currency differs from the entity's currency and no rate was supplied.

RecommendationExtend finance_import with an optional CSV/XLSX ingestion entry point and batch/idempotency semantics, using Cerebrum-FinanceOps's upload_import_file/process_import_batch and its fail-closed FX-rate requirement as the reference implementation, so the Store block covers the full file-to-normalized-record path rather than just the in-memory mapping step.






finance_v2
app/blocks/finance_v2.py:434-441: _extract_cash_flows(self, text) is docstringed 'Best-effort parse of cash flow series fromâ€¦


Cerebrum-Blocks · app/blocks/finance_v2.py

app/blocks/finance_v2.py:434-441: _extract_cash_flows(self, text) is docstringed 'Best-effort parse of cash flow series from params or text' and _extract_returns is docstringed 'Best-effort parse of return series', but both bodies are literally just return [] with no parsing logic at all. Every call site (_build_analysis:241-249, _analyze_cash_flow:190-194) does params.get('cash_flows') or self._extract_cash_flows(text), so NPV/IRR/VaR/Sharpe silently produce {'value': None, 'error': 'Missing cash flows...'} for every document unless the caller already supplies the numeric series as explicit params -- the 'extract from document text' capability the docstring promises never actually runs.

RecommendationEither implement real cash-flow/return-series extraction from document text (e.g. a numeric-table regex pass) or correct the docstrings to stop claiming text-based best-effort parsing, since as written the promise is silently unfulfilled rather than erroring loudly.







cluster Â· insurance 1




agency_commission_engine
The two files are otherwise byte-identical (628 vs 638 lines) except: Store hardcodes "currency": "USD" in EMBEDDED_FORMULASâ€¦


Cerebrum-Blocks · app/blocks/agency_commission_engine.py

The two files are otherwise byte-identical (628 vs 638 lines) except: Store hardcodes "currency": "USD" in EMBEDDED_FORMULAS and returns self.formulas.get("currency", "USD") verbatim at 3 call sites (lines 268, 300, 325), ignoring any caller-supplied currency. InsureOps' vendored copy carries a 2026-07-22 commit 9c94f9a "Fix commission currency (HKD) and add real AI-written /chat answers" that changed the default to HKD and added a _currency(self, data) helper (diff lines 623-632) which prefers data["currency"] or data["policy"]["currency"] before falling back to the schedule default -- because the HK pilot's commissions were computing in the wrong currency under the naive Store logic. Cerebrum-Blocks HEAD (2026-09-15, e3696bf) is nearly two months newer than that fix and still lacks it -- the fix was never synced back to the Store. Everything else in the file (Decimal-precision schedule math, chargeback proration by days-in-force, override stacking) is genuinely sophisticated and correct; this is a narrow, well-evidenced regression, not a broad stub.

RecommendationPort the _currency() resolution method and the multi-currency-aware calls from InsureOps' kit/bundle/app/blocks/agency_commission_engine.py back into the canonical app/blocks/agency_commission_engine.py in Cerebrum-Blocks, and re-certify/re-sign the block. Also worth a one-time audit for any other blocks where a downstream product's vendored bundle has silently diverged from the Store original (this survey only found this one instance among the 7 files compared).







cluster Â· hotel 3




hotel_v2 (HotelBlockV2) â€” real block, but orphaned in both products
hotel_v2.py is 779 lines of real deterministic logic: keyword-based document-type detection across 8 hotel document types (reseâ€¦


Cerebrum-Blocks · app/blocks/hotel_v2.py (= block_store/kits/hotel_management/bundle/app/blocks/hotel_v2.py)

hotel_v2.py is 779 lines of real deterministic logic: keyword-based document-type detection across 8 hotel document types (reservation/folio/housekeeping/maintenance/revenue/group-contract/OTA/night-audit), real division-based RevPAR/GOPPAR formulas (lines 574-587), and keyword-density risk scoring for overbooking/revenue-leakage/fraud (lines 687-727) â€” not fabricated. However: grep for 'HotelBlockV2(' or '.hotel_v2' across cerebrum-hotelops-v2 finds it referenced ONLY in tests/test_api.py as import app.blocks.hotel_v2  # noqa: F401 asserting vendor_available() â€” never instantiated or .process()-called with real data anywhere in api/, reasoning/, or guest_intelligence/. cerebrum-hotelops (v1) has ZERO references to hotel_v2/HotelBlockV2 anywhere outside scripts/check_vendor_lock.py and tests/test_vendor_lock.py. Both products' actual document-analysis path is generic RAG (hotelops/retrieval.py: ingest_document/retrieve) + LLM (hotelops/llm.py), not this block. grep for ADR/RevPAR/GOPPAR across both products' guest_intelligence/, reasoning/, api/ returns zero hits â€” the metrics this block computes are not surfaced anywhere in the product.

RecommendationEither wire hotel_v2.process() into an actual product endpoint (e.g. api/routes/documents.py's ingest path, so uploaded revenue reports/folios get real ADR/RevPAR/GOPPAR/risk extraction instead of only RAG indexing), or stop counting it as delivered capability â€” the VENDOR_BLOCKS.md/VENDOR.lock ceremony around it (CI-enforced digest checks, 463 vendored files) currently certifies that the file exists and imports cleanly, not that the product uses it.






opera_connector (kit-level stub, not in manifest.json artifacts)
opera_connector.py's fetch_raw() always returns {"stub": True, "message": "Opera connector not yet implemented â€” wire Opera Cloâ€¦


Cerebrum-Blocks · block_store/kits/hotel_management/blocks/opera_connector.py

opera_connector.py's fetch_raw() always returns {"stub": True, "message": "Opera connector not yet implemented â€” wire Opera Cloud API"} regardless of input â€” honest about being incomplete, not a fake-success receipt. It is NOT listed in the kit's manifest.json 'artifacts' (only hotel_v2.py, formula_executor_v2.py, and core modules are officially published), so it isn't actually shipped to clients as-is â€” but it sits unmarked alongside real kit files. Both cerebrum-hotelops and cerebrum-hotelops-v2 independently built real connectors/opera.py: v1's (57 lines) has a genuine fixture/live dual-mode connect->fetch->normalise->emit pipeline with a real httpx.get() live path (connectors/base.py), though its live auth (static X-Client-Id/X-Client-Secret headers) is a naive guess at Oracle Opera Cloud's actual OAuth2 model and is unvalidated. v2's (38 lines) is architecturally more rigorous: explicit MockLevel.MOCK_UNAVAILABLE enum in connectors/base.py, fails closed with a structured refusal envelope, and its docstring states 'Nothing here fabricates guest data' â€” exactly the anti-fabrication discipline this survey is checking for.

RecommendationReplace the Store stub with a real block built on v2's connect/fetch/normalise/MockLevel pattern (safer than v1's unvalidated static-header 'live' path), and either add it to manifest.json artifacts once real, or clearly mark unshipped kit-adjacent files as excluded so they aren't mistaken for delivered capability.






hotel_trigger (kit-level stub, no donor equivalent found)
hotel_trigger.py's process() always returns triggered: False / a stub workflow_payload, version="0.1.0-skeleton"


Cerebrum-Blocks · block_store/kits/hotel_management/blocks/hotel_trigger.py

hotel_trigger.py's process() always returns triggered: False / a stub workflow_payload, version="0.1.0-skeleton". Targeted search for 'queue_threshold' and occupancy-trigger patterns across both product repos returned no hits; cerebrum-hotelops-v2/reasoning/cascade_engine.py covers pre-opening milestone cascades, a different domain (not occupancy/guest-service reactive triggers).

RecommendationLeave flagged as not-yet-implemented rather than silently present as a normal kit file. I did not have budget to search the other ~13 non-hotel donor repos (Cerebrum, TEKsystems_GlobalRetailMNC, etc.) for a transferable trigger/rules-engine pattern that could seed this â€” worth a follow-up pass if this block is prioritized.







cluster Â· retail 3




channel_router
Line 1 docstring: "Insurance channel router with explainable heuristic scoring." Line 83: tags = ["domain", "insurance", "routiâ€¦


Cerebrum-Blocks · app/blocks/channel_router.py

Line 1 docstring: "Insurance channel router with explainable heuristic scoring." Line 83: tags = ["domain", "insurance", "routing", "distribution", "heuristic"]. The entire _CHANNELS catalog (lines 15-52) and _channel_score() logic (lines 210-289) is insurance-specific: channels are agency/direct/mga/partner, scored on account fields like line_of_business, premium, complexity, specialty_risk, partner_referral -- none of which map to retail concepts (online/in-store/marketplace/wholesale/franchise channel mix, order routing, fulfillment channel selection). Grepped TEKsystems_GlobalRetailMNC for "channel" (case-insensitive, all .py under backend/) -- zero hits, so the donor product has no channel-routing capability either; this is not a case of the Store block being 'behind' a donor equivalent, it's that the Store has NO real retail channel-routing block despite having one registered under that exact name.

RecommendationDo not let retail-cluster platform assembly pull this block expecting retail channel logic -- it will silently deliver insurance agency/MGA/direct/partner scoring instead. Either (a) rename/re-namespace it explicitly as an insurance block so it stops appearing as a plausible retail fit, and (b) commission a real retail channel_router block (online/in-store/marketplace/wholesale channel-mix scoring) as new work since no donor repo has one to clone.






traffic_manager
Line 14: layer = 1


Cerebrum-Blocks · app/blocks/traffic_manager.py

Line 14: layer = 1. Line 15: tags = ["security", "governance", "traffic", "core"]. Line 16: requires = ["rate_limiter", "failover", "queue"]. The whole class (_route, _check_circuit_state, _record_failure/_record_success, _resolve_block) governs block-to-block API calls (source/target/payload routing with circuit breakers), matching its docstring 'Governance layer for block-to-block calls' -- 'traffic' here means inter-service API traffic, not retail customer/store traffic. Grepped TEKsystems_GlobalRetailMNC for "traffic" -- zero hits; the donor has no footfall/web-traffic/session-traffic analytics module either.

RecommendationKeep this block classified purely as core/platform governance (layer 1) -- it is solid at what it does and should not be presented to retail-platform assembly as retail-domain traffic analytics. If a retail-cluster platform genuinely needs footfall/session-traffic analysis, that is net-new work; nothing in the surveyed donor repos supplies it.






retail_v2
(1) Registry gap: confirmed absent from block_registry/ (only channel_router and traffic_manager are registered for this clusteâ€¦


Cerebrum-Blocks · app/blocks/retail_v2.py

(1) Registry gap: confirmed absent from block_registry/ (only channel_router and traffic_manager are registered for this cluster), so it is unreachable via the standard block-dispatch path. It IS shipped via a separate route -- block_store/kits/retail/bundle/app/blocks/retail_v2.py is byte-identical (verified with diff) and wired through block_store/kits/retail/manifest.json's container class app.containers.retail.RetailContainer -- but that's a second, parallel distribution mechanism a platform assembler could easily miss if they only scan block_registry/. (2) Metric-wiring gap: all 8 financial metrics (_extract_gross_margin, _extract_inventory_turnover, _extract_sell_through_rate, _extract_shrinkage, _extract_return_rate, _extract_conversion_rate, _extract_aov, _extract_cart_abandonment; lines 397-475) take their numeric inputs (selling_price, cost_price, cogs, average_inventory, etc.) only as pre-parsed params -- they never derive them from the input document text themselves. Since the block's own ui_schema (lines 49-54) advertises 'Paste retail document text...' as the primary input, and process()/_build_analysis() (lines 218-227) call these formulas with params.get("gross_margin", {}) etc. with no text-to-number extraction step in between, every financial metric will return None/'Insufficient inputs' on the advertised plain-text-paste workflow unless a caller separately parses and supplies the raw numbers -- only entity extraction (SKUs, prices-as-strings, suppliers, order numbers) and the separate _extract_money_value() label-based lookup actually pull numbers out of text.

RecommendationRegister retail_v2 in block_registry/ directly (not only via the kit bundle) so it's discoverable through the standard path. Wire the financial-metric formulas to consume values already captured by _extract_money_value()/_extract_prices() instead of requiring separately pre-parsed params, or clearly document in the block's ui_schema that raw numeric fields must be supplied out-of-band -- as shipped, a user who just pastes document text will get populated entities/compliance/risk but an all-None metrics table, which reads as broken even though the code path is honest about why.







cluster Â· medical-legal 3




medical_ehr_connector
Not a stub: authenticate()/fetch_raw()/normalize() genuinely build an httpx.AsyncClient call to FHIR_BASE_URL, handle Bearer/clâ€¦


Cerebrum-Blocks · app/blocks/medical_ehr_connector.py

Not a stub: authenticate()/fetch_raw()/normalize() genuinely build an httpx.AsyncClient call to FHIR_BASE_URL, handle Bearer/client-credential auth, walk a FHIR Bundle's entry[].resource, and build a ConnectorEvent. BUG (reproduced live): the Patient/Observation/MedicationRequest pydantic models in app/core/connector_events.py (lines 102-132) declare snake_case fields (birth_date, effective_date_time, value_quantity, medication_codeable_concept, authored_on, dosage_instruction) with NO alias_generator/populate_by_name/Field(alias=...), while real FHIR resources use camelCase (birthDate, effectiveDateTime, valueQuantity, medicationCodeableConcept, authoredOn, dosageInstruction). I ran Patient.model_validate({'resourceType':'Patient','id':'pat-1','name':[{'family':'Doe','given':['Jane']}],'birthDate':'1990-01-01','gender':'female'}) in this clone and got birth_date: None back -- the value is silently dropped, not an error. Every multi-word field on Observation and MedicationRequest is affected identically, so a real EHR fetch would return clinically empty Observations/MedicationRequests while the block still reports status:success. tests/blocks/test_medical_ehr_connector.py::test_fetch_patient_bundle only asserts normalized_data['count']==1 and event_type -- it never checks any decoded field, so this bug passes CI silently (the same 'reports success without the work being real' pattern flagged elsewhere in this survey).

RecommendationAdd model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True) (or explicit Field(alias=...)) to Patient/Observation/MedicationRequest in app/core/connector_events.py, call model_validate with real camelCase FHIR fixtures, and update the test to assert resources[0]['birth_date'] == '1990-01-01' etc. so this class of bug can't recur silently.






medical_v2
Real: 2021 CKD-EPI race-free eGFR equation is implemented correctly (kappa/alpha/female_factor branch match the published formuâ€¦


Cerebrum-Blocks · app/blocks/medical_v2.py

Real: 2021 CKD-EPI race-free eGFR equation is implemented correctly (kappa/alpha/female_factor branch match the published formula, lines 709-735); BMI computation+interpretation bands are correct (694-707); PHI is genuinely tokenized for patient_id/DOB via _redacted_entity() returning 'REDACTED' rather than the raw value (312-322, 332-356). Fabricated/overclaimed: 'wells_score' (832-852), 'chads2_vasc' (854-893) and 'child_pugh' (895-905) are each reduced to '+1 point per keyword match' with no relation to the real point-weighted clinical criteria (e.g. actual Wells DVT criteria assign different weights per item and include a -2 for 'alternative diagnosis at least as likely'; this implementation can never go negative and weighs every hit equally). All 5 risk_scores categories -- readmission/medication_error/infection/fall/mortality (978-1046) -- use one identical formula score = min(1.0, len(found)/3.0), i.e. keyword-density counting presented as a named clinical risk score, not any validated instrument (e.g. real readmission risk uses LACE/HOSPITAL with weighted variables). The class docstring claims 'PHI is detected but returned in redacted/tokenized form to avoid leaking patient identifiers,' but diagnoses/medications/allergies/vitals/procedures/providers/facilities (358-574) all return literal Â±20-60 char context windows straight from the source text -- these can easily contain a name, MRN, or DOB sitting next to the matched term, so the redaction claim is false for the majority of extracted entity types. Separately, the ICD-10 'sanity' filter (363-367) excludes first-letters I/O/U/X/Y/Z as invalid, but only U is actually reserved in ICD-10-CM -- O (pregnancy), X/Y (external causes/injury) and Z (factors influencing health status) are valid category letters the filter silently discards. The block is unregistered (no block_registry/medical_v2.py, confirmed) and has zero test coverage.

RecommendationEither replace wells_score/chads2_vasc/child_pugh/cardiac_risk with citations to the real weighted criteria (or an off-the-shelf validated calculator) or rename/relabel them clearly as unvalidated heuristics so callers don't mistake them for clinical decision support; extend PHI redaction to the context windows of all entity types, not just patient_id/DOB; fix the ICD-10 first-letter filter; add a test suite before considering registration.






legal_v2
Real: legal citation regexes match actual Bluebook-style reporter formats (U.S., F.2d/F.3d, Cal.2d, N.E.2d, 400-419), statute râ€¦


Cerebrum-Blocks · app/blocks/legal_v2.py

Real: legal citation regexes match actual Bluebook-style reporter formats (U.S., F.2d/F.3d, Cal.2d, N.E.2d, 400-419), statute regexes match real USC/CFR shapes with the section symbol (421-437), case/docket numbers match the real federal N:NN-cv-NNNNN format (383-398), monetary-value extraction normalizes million/billion/thousand multipliers and classifies amounts by surrounding context (454-494), and indemnification-cap/termination-notice extraction pulls real numeric terms (536-598). Same fabrication pattern as medical_v2: all 5 risk_scores categories -- litigation/regulatory/reputational/financial/ip (704-742) -- use the identical formula score = min(1.0, len(found)/3.0), i.e. keyword-hit counting with no legal weighting or precedent modeling, presented as named risk scores. CONCRETE BUG (verified by reading, not simulated): _lk = LegalKnowledge() is a module-level singleton (line 20), and process() calls _lk.set_custom_rules(custom_rules) (line 99), which in app/core/legal_knowledge.py's set_custom_rules (149-151) does self.custom_rules = rules or [] -- an in-place replacement on the one shared instance, not a per-call/per-request value. In an async server handling concurrent requests, two callers supplying different custom_rules in the same window race on this shared instance: one tenant's custom compliance/risk rules can silently apply to, or be clobbered by, another concurrent request's rules on a block that is supposed to be stateless per invocation. The identical pattern exists in medical_v2.py (_mk = MedicalKnowledge()). The output_schema (LegalAnalysis in app/core/schema_registry.py, lines 120-133) is a loose {'type':'object', 'required':[]} bag for every nested field -- documentation only, no real validation, and structurally identical to MedicalAnalysis/RetailAnalysis/HotelAnalysis, confirming template generation rather than legal-specific type design. legal_v2 IS registered in block_registry/ (confirmed) but has zero test coverage anywhere in the repo.

RecommendationMove the LegalKnowledge/MedicalKnowledge instances to be per-request (constructed inside process(), or pass custom_rules as a call argument instead of mutating shared state) to close the cross-request race; either source the risk_scores from real legal-risk weighting or relabel them as unvalidated heuristics; add a test suite given it is already registered and reachable in production.







cluster Â· aviation 4




aviation_v2 (document analysis engine)
app/blocks/aviation_v2.py (615 lines), extends DomainBlockV2, deterministic regex+math (no LLM)


Cerebrum-Blocks · app/blocks/aviation_v2.py

app/blocks/aviation_v2.py (615 lines), extends DomainBlockV2, deterministic regex+math (no LLM). Real document-type auto-detection over 8 doc types (maintenance_log, flight_manual, MEL, incident_report, training_record, dispatch_release, lease_agreement, airworthiness_directive) via keyword sniffing on first 5000 chars. Real entity extraction (aircraft registrations, serials, engine models, part numbers, flight hours, airports, crew, operators) via regex with context windows and dedup. Real formula-based metrics (utilization, dispatch reliability, OTP, MTBF/MTTR, fuel burn/hr, payload utilization, lease rate factor) via a shared _safe_divide helper with error handling for missing inputs. Compliance checks (EASA/FAA Part 121/ICAO SMS/ETOPS/carbon-offsetting) and 5-category + overall risk scoring both delegate to app/core/aviation_knowledge.py's AviationKnowledge class, which is a genuine (if simple) keyword-hit-density scorer (score = min(1.0, hits/3.0)) over curated per-category keyword lists (app/core/aviation_knowledge.py, 168 lines) -- not a stub, not an always-same-value gate. BUG FOUND: _extract_airports uses re.finditer(r"\b([A-Z]{3,4})\b", text, re.IGNORECASE) -- the IGNORECASE flag makes [A-Z] match lowercase too, so this regex fires on almost any 3-4 letter word in the document, not just IATA/ICAO airport codes; it will produce heavy false-positive noise in the airports entity list. This block IS the one bundled for sale: block_store/kits/aviation/manifest.json packages aviation_v2 + formula_executor_v2 as the 'Aviation & Aerospace Suite' kit (price_cents: 0, status: available).

RecommendationFix the airport-extraction regex (drop re.IGNORECASE or require an explicit airport-context keyword nearby, matching the pattern used by _extract_aircraft_registrations). Otherwise this block is real and does not need replacing. Consider wiring the other 7 aviation blocks (see next findings) into the same kit or a companion 'Aviation Conversational Agent' kit, since they are already registered in the block dispatcher and fully tested.






aviation_grounding_gate
app/blocks/aviation_grounding_gate.py (471 lines)


Cerebrum-Blocks · app/blocks/aviation_grounding_gate.py

app/blocks/aviation_grounding_gate.py (471 lines). Extracts numeric figures from a drafted answer via regex (currency symbols/codes, weight/volume units, %, miles/points, time units), filters out years and ordinals, then scores each figure 0.0-1.0 by checking (a) raw value present in corpus text and (b) value found within a 50-char window of a unit-compatible term in the corpus (handles $2,500 vs 2500 vs 2,500 normalization). Verdict is pass / flag-as-estimate / block based on score vs configurable thresholds (default 0.75/0.5), and additionally force-blocks safety-critical queries (fare/weight/fuel/capacity/cargo/revenue/loyalty) that expect a number but produced none. Verified NOT vacuous: ran the actual pytest suite (tests/blocks/test_aviation_grounding_gate.py, 8 tests) live -- all 8 pass, and they assert genuinely different verdicts for cited-vs-fabricated figures (e.g. a fabricated '1,250 SAR' fare with no matching citation correctly returns verdict=block, allowed_response=None; the same figure present in a citation returns verdict=pass). The shared platform module it complements, app/core/grounding.py, implements a parallel evaluate_grounding()/check_scope_refusal() with an append-only JSONL audit log (verdict_log_path()) and a real regex-based scope-refusal list (medication dosing, structural sign-off, legal filing, in-flight emergencies) checked BEFORE retrieval.

RecommendationNo fix needed on correctness grounds. Recommend: (1) register this block (and its 6 dependents below) into block_registry/ so it appears in the Store's standard 108-block catalogue, since it is currently only reachable via the app/blocks/__init__.py BLOCK_REGISTRY dispatch dict, not the adapter-file convention used elsewhere; (2) flag this pattern to whoever owns medical_v2/pharma_v2 as a reference implementation for safety-critical grounding, since it is unusually rigorous for this codebase.






aviation_chat_server (WebSocket transport)
app/blocks/aviation_chat_server.py (465 lines)


Cerebrum-Blocks · app/blocks/aviation_chat_server.py

app/blocks/aviation_chat_server.py (465 lines). Correctly holds no domain logic itself (as its docstring claims): validates auth, loads/saves conversation history via a memory block dependency, checks scope-refusal BEFORE calling the orchestrator (so out-of-scope questions like in-flight emergencies never reach the LLM), builds a fixed 3-step orchestrator chain (vector_search -> chat -> aviation_grounding_gate) that callers cannot override (steps are server-defined, not caller-suppliable -- this is enforced in code, preventing a caller from stripping out the grounding step), and fails closed: if the orchestrator returns no verdict, _normalise_orchestrator_result forces verdict=block rather than falling back to an ungrounded answer. Every verdict (including blocks) is persisted via persist_verdict(). Tests (tests/blocks/test_aviation_chat_server.py) pass live.

RecommendationRegister into block_registry/ alongside the gate. No functional fix needed.






aviation_cargo_kit / aviation_cx_kit / aviation_loyalty_kit / aviation_pss_kit / aviation_revenue_kit
Read all 5 files in full (cargo: 240 lines, cx: 232, loyalty: 232, pss: 238, revenue: 241)


Cerebrum-Blocks · app/blocks/aviation_cargo_kit.py, aviation_cx_kit.py, aviation_loyalty_kit.py, aviation_pss_kit.py, aviation_revenue_kit.py

Read all 5 files in full (cargo: 240 lines, cx: 232, loyalty: 232, pss: 238, revenue: 241). Each is structurally identical: same input/output schema shape, same _resolve_block/_retrieve/_generate_answer/_ground/_error methods almost line-for-line, differing only in (a) the domain config string, (b) the wording of the system prompt passed to the chat block (e.g. cargo says 'Do not invent weights, dimensions, limits, or handling rules'; loyalty says 'Do not invent points, miles, tier thresholds, or benefits'), and (c) the query_type string forwarded to the grounding gate (cargo, general, loyalty, fare, revenue). None of the 5 contains cargo-specific, CX-specific, loyalty-specific, PSS-specific, or revenue-specific extraction/computation logic beyond that prompt string -- all actual domain reasoning is delegated to an external chat (LLM) block and the shared aviation_grounding_gate. This is a legitimate and safe architecture (RAG + LLM + hard grounding gate), but it means these 5 'kits' are effectively 1 kit x 5 prompt variants, not 5 independently-engineered domain modules. tests/blocks/test_aviation_domain_kits.py (206 lines) exercises all 5 and passes live (part of the 22/22 pass run).

RecommendationNot a quality problem (each does what it claims: read-only, grounded, gated), but flag as a maintainability/DRY issue -- 5 files to keep in sync for what is really one pattern. If the Store values genuine per-domain depth (e.g. cargo weight/balance math, loyalty tier-threshold tables) over LLM-mediated RAG, these would need real domain-specific computation added; as-is they are safe but shallow beyond the shared gate.







cluster Â· connector-infra 4




mcp_adapter
mcp_adapter.py builds its own 'tool catalog' (list_tools/describe actions) by iterating BLOCK_REGISTRY


Cerebrum-Blocks · app/blocks/mcp_adapter.py

mcp_adapter.py builds its own 'tool catalog' (list_tools/describe actions) by iterating BLOCK_REGISTRY. But the REAL, working MCP server is app/mcp_server.py -- a separate, more complete implementation using the official mcp SDK (mcp.server.Server, stdio_server, SseServerTransport) that builds its own JSON-Schema tool list via _build_tools()/get_all_blocks(), is mounted at /mcp in app/main.py (line 255-257) wrapped in asgi_auth_required, and adds a 'cerebrum-chain' meta-tool for multi-block chaining. app/mcp_server.py does NOT import or call MCPAdapterBlock anywhere. mcp_adapter.py's own docstring says 'the SSE endpoint /mcp/sse (see app/routers/mcp.py)' -- app/routers/mcp.py does not exist anywhere in the repo (confirmed via ls/grep); the real mount point is app/mcp_server.py, not a router. mcp_adapter IS registered in app/blocks/__init__.py's BLOCK_REGISTRY (line 114) so it's reachable via normal block dispatch and gets auto-exposed as just another generic MCP tool by mcp_server.py's loop, but its own internal catalog-building logic is never consumed by anything -- it's a second, redundant self-description mechanism with a stale docstring pointing at a file that doesn't exist. It is also absent from block_registry/ (no Docker-packaging manifest dir, unlike 105 other blocks).

RecommendationDelete mcp_adapter.py's redundant catalog logic (or the whole block) and fix/remove the stale app/routers/mcp.py reference; app/mcp_server.py is already the real, working MCP-server foundation to build enterprise-connector work on top of -- extend _build_tools()/_dispatch_tool() there, not mcp_adapter.py.






webhook
Store's webhook.py (197 lines) is real and solid for OUTBOUND delivery: SSRF guard via validate_public_url, HMAC-SHA256 signingâ€¦


Cerebrum · backend/app/core/security/webhook_security.py

Store's webhook.py (197 lines) is real and solid for OUTBOUND delivery: SSRF guard via validate_public_url, HMAC-SHA256 signing, exponential-backoff retries over aiohttp. But it is outbound-only -- self.endpoints is a plain in-process dict (lost on every restart, no persistence) and there is no code anywhere in the block to receive or verify an INCOMING webhook. Cerebrum's backend/app/core/security/webhook_security.py (355 lines) implements exactly that missing half: WebhookManager with persisted WebhookDelivery records and retry_failed_deliveries(max_age_hours), WebhookSignature.verify_signature() with a Stripe-style timestamp-tolerant HMAC header parser, verify_incoming_webhook(), and a WebhookMiddleware ASGI class for validating inbound webhook calls end-to-end.

RecommendationPort verify_incoming_webhook/WebhookMiddleware and the persisted WebhookDelivery/retry_failed_deliveries pattern from Cerebrum's webhook_security.py into the Store's webhook block (or a new inbound-webhook block) -- the enterprise-connector work will need to both send AND receive+verify webhooks, and only the send half currently exists in the Store.






google_drive
Store's google_drive.py (287 lines) is real and solid but narrow: user-mode OAuth2 access/refresh token only (drive.readonly),â€¦


The_Fork · app/routers/drive.py + app/core/gdrive_service.py

Store's google_drive.py (287 lines) is real and solid but narrow: user-mode OAuth2 access/refresh token only (drive.readonly), list + download, with a query-injection guard. The_Fork has a materially richer Drive connector system built on top of the same file: app/routers/drive.py (739 lines) adds a full OAuth connect/callback/status/disconnect lifecycle with a signed, CSRF-protected state token, a background recursive folder-indexing job (/v1/projects/{id}/drive/index-folder) that walks a Drive folder into a project, and a /drive/import endpoint. app/core/gdrive_service.py (601 lines) adds a SERVICE-ACCOUNT mode entirely absent from the Store block -- unattended access for nightly hydration jobs (mints short-lived tokens from a GCP service-account key), folder walk, exact-filename lookup, and a fallback path for downloading large public files via Google's confirm-token/HTML-scrape flow (download_public_file_bytes). Confirmed real (not scaffolding) by function count and docstrings citing the specific unattended-job use case.

RecommendationThe Store's google_drive.py only covers per-user OAuth; for the connector-infra work (which will need unattended/service-to-service Drive access, not just a logged-in user's session) port gdrive_service.py's service-account token-minting path and drive.py's CSRF-protected OAuth lifecycle + folder-indexing pipeline into the Store, either as an extension of google_drive.py or a new google_drive_connector block.






android_drive
49-line HONEST stub: process() unconditionally returns {status:'error', error:'not_implemented', detail:'android_drive is a stuâ€¦


Cerebrum-Blocks · app/blocks/android_drive.py

49-line HONEST stub: process() unconditionally returns {status:'error', error:'not_implemented', detail:'android_drive is a stub: no ADB or REST bridge is wired... Do not build on this block until an integration exists.'}. This is corroborated independently in two other places in the org: The_Fork's own CONSTRUCTION_LEDGER.md labels it 'android_drive HONEST-STUB' in its block census, and Cerebrum-Blocks' docs/GUARDRAILS.md documents the exact contract 'android_drive | not implemented | error: not_implemented'. This is a genuinely honest stub (unlike the fake-success gates this session has found elsewhere) -- it correctly reports failure rather than fabricating device data. A targeted search across every owner repo for an ADB/REST Android-storage implementation found none.

RecommendationZero functional risk of being mistaken for real (it self-reports as not implemented), but it is zero capability. No donor exists to copy from -- flagging as store_upgrade because it needs net-new ADB-or-REST-bridge implementation, not a clone.







cluster Â· core-infra 8




migration
_create_backup (line ~445) is '# TODO: Implement actual backup' and returns only a fabricated backup_id; _restore_backup (lineâ€¦


Cerebrum-Blocks · app/blocks/migration.py

_create_backup (line ~445) is '# TODO: Implement actual backup' and returns only a fabricated backup_id; _restore_backup (line ~456) is a bare 'pass'; _ensure_migrations_table (line ~350) is 'pass' with '# TODO: Create table if not exists'; _load_current_version (line ~356) is hardcoded to always return '0.0.0.' The built-in migration _migration_001_up/_down (lines 372-380) are '# TODO: Create core tables' / '# TODO: Drop tables' yet UNCONDITIONALLY RETURN {'created_tables': ['blocks','users','config']} / {'dropped_tables': [...]}. _record_migration (line 428) has '# TODO: Save to database' and only appends to an in-process list. The three seed functions (_seed_default_data/_seed_demo_data/_seed_test_data, lines 465-475) all return {'count': 0} placeholders. _run_migration will call this chain and report migrations_run>0 / success:True while nothing touches an actual database -- a restart silently reverts current_version to 0.0.0 with no record anything ever ran.







health_check
_check_external_apis (line 326-349): comment '# Simulated check - in production would do actual HTTP request' then hardcodes reâ€¦


Cerebrum-Blocks · app/blocks/health_check.py

_check_external_apis (line 326-349): comment '# Simulated check - in production would do actual HTTP request' then hardcodes results[name] = {'healthy': True, ..., 'status': 'unknown'} for every configured API, never issuing a request. _run_probe (line 378-410): comment '# Simulate probe check based on type' then for every probe type (http/tcp/other) sets healthy = True unconditionally -- a registered probe against a dead endpoint will always report healthy. deep_check aggregates these into the overall 'healthy' status, so the block's headline diagnostic is partly fabricated even though database/memory/disk checks (lines 271-376) ARE real.







queue
Docstring (line 1) claims 'Supports Redis (production) or memory (edge/local)'


Cerebrum-Blocks · app/blocks/queue.py

Docstring (line 1) claims 'Supports Redis (production) or memory (edge/local)'. _enqueue (line 89) does write to Redis via memory_block when use_redis is set. But _dequeue (line 126-138): 'if self.use_redis: # Would use Redis BRPOP \n pass' -- literally does nothing and falls through to return None. The background _worker loop (line 163) only iterates self._queues (the in-memory dict), so jobs enqueued to the declared 'production' Redis backend are never dequeued or processed by this block at all.







config
default_config (line 20-23) declares config_file: 'config/blocks.json' and env_prefix: 'CEREBRUM_'


Cerebrum-Blocks · app/blocks/config.py

default_config (line 20-23) declares config_file: 'config/blocks.json' and env_prefix: 'CEREBRUM_'. Neither string is referenced anywhere else in the file: _load_defaults (line 38) only builds a hardcoded Python dict; there is no os.getenv(env_prefix...) lookup and no file read of config_file. The block is a static in-memory dict with get/set, not the config-file+env-override system its own schema advertises.







rate_limiter
self.counters / self.buckets (lines 52-53) are plain Python dicts; self.memory_block / self.database_block are never referencedâ€¦


Cerebrum-Blocks · app/blocks/rate_limiter.py

self.counters / self.buckets (lines 52-53) are plain Python dicts; self.memory_block / self.database_block are never referenced anywhere in the 386-line file (grep confirms zero uses). Every limit resets on process restart and is not shared across replicas/workers. auth.py's own _check_rate_limit (app/blocks/auth.py lines 149-194), by contrast, DOES persist through memory_block -- so the Store already has a more production-safe rate limiter sitting inside auth.py, making this dedicated rate_limiter block a redundant, less-durable second implementation of the same capability.







version
__init__ (lines 44-49) stores everything in plain dicts


Cerebrum-Blocks · app/blocks/version.py

__init__ (lines 44-49) stores everything in plain dicts. _legacy_initialize (line 51-64) only prints and has '# TODO: Create database tables' comments listing the schema it should have (versions, dependencies, changelogs, migrations) but never creates or touches them. A restart of the process wipes the entire published-version history for every block in the Store's own versioning system -- ironic given this block exists specifically to let the Factory manage block versions.







monitoring
self.providers (line 38-40) is hardcoded to {'kimi': {...}} only -- no register_provider/add_provider method exists anywhere inâ€¦


Cerebrum-Blocks · app/blocks/monitoring.py

self.providers (line 38-40) is hardcoded to {'kimi': {...}} only -- no register_provider/add_provider method exists anywhere in the 299-line file, so this cannot monitor any other service without editing source. It is effectively an LLM-router health scorer for one provider, not general APM/metrics for the platform (no HTTP endpoint tracing, no arbitrary-service registration).







billing
_upgrade_plan (line 252-266): 'if self.auth_block: # Would update user role \n pass' -- then unconditionally returns {'upgradedâ€¦


Cerebrum-Blocks · app/blocks/billing.py

_upgrade_plan (line 252-266): 'if self.auth_block: # Would update user role \n pass' -- then unconditionally returns {'upgraded': True, 'plan': new_plan, ...} even though nothing was changed anywhere. A caller checking the response would believe the plan was upgraded when it was not.








cluster Â· reasoning-orchestration 13




formula_executor
574 lines


Cerebrum-Blocks · app/blocks/formula_executor.py

574 lines. Real sandboxed exec() with a locked-down namespace (_SAFE_BUILTINS, __builtins__:{}), a 10-formula construction library with actual formulas (concrete volume, rebar weight, EVM SPI/CPI/EAC, etc.), optional routing to an external Docker sandbox runner via SANDBOX_RUNNER_URL with graceful in-process fallback, and a real Pint-based dimensional/currency unit validator (_validate_units, ~130 lines) that detects target units from free text and does actual UnitRegistry conversion/dimensionality checks. Registered in block_registry/formula_executor/ with block.py+Dockerfile+block.json.

RecommendationNone needed â€” flag as a positive baseline. This block already implements a real fragment of REASONING_KERNEL.md's DeterministicFormulaExecutor (#5) and UnitAndCurrencyValidator (#6).






formula_executor_v2
318 lines implementing a full LLM-codegen -> RestrictedPython sandbox -> retry loop with per-session code caching, a real threaâ€¦


Cerebrum-Blocks · app/blocks/formula_executor_v2.py

318 lines implementing a full LLM-codegen -> RestrictedPython sandbox -> retry loop with per-session code caching, a real thread-joined timeout wrapper around the Plan-3 sandbox (app/core/sandbox.py, 322 lines, real), and grounding classification (app/core/formula_definitions.py, 297 lines) that tags every result as grounded/user_specified/model_generated against a kernel-tier + domain-overlay definition set. This is a real, deeper implementation of DeterministicFormulaExecutor than formula_executor v1, and its formula_definitions.py module is effectively a working FormulaRegistry (#4) + partial AuthorityResolver (#12)/ProvenanceRecorder (#21) fragment referencing CerebrumDev.ai kernel tier #200/#202 by name. But block_registry/formula_executor_v2/ has ONLY block.json â€” no block.py, no Dockerfile â€” so it cannot actually be dispatched through the standard block-execution path today.

RecommendationComplete the registration (generate block.py adapter + Dockerfile) so this real capability becomes reachable. This directly narrows REASONING_KERNEL.md's 'unbuilt' FormulaRegistry/DeterministicFormulaExecutor/AuthorityResolver gap â€” the code exists, it just isn't wired in.






sympy_reasoning
212 lines


Cerebrum-Blocks · app/blocks/sympy_reasoning.py

212 lines. Registered, produces real severity-ranked variance recommendations with z-scores and action items driven by actual input data (boq_data vs historical_benchmarks). However 'sympy' usage is decorative: every value is wrapped in sp.Float(x) and combined with plain +/-/* â€” no sp.Symbol, no equation solving, no CAS features are ever used. This is ordinary float arithmetic dressed as 'symbolic variance analysis.'

RecommendationEither use real symbolic capability (e.g., solve tolerance/threshold equations symbolically) or rename/redescribe as a statistical variance engine so the block's stated sophistication matches its code.






project_reasoner
305 lines implementing a real UNDERSTAND->PLAN->EXECUTE->DELIVER agent loop: builds a plan prompt, validates the LLM reply agaiâ€¦


Cerebrum-Blocks · app/blocks/project_reasoner.py

305 lines implementing a real UNDERSTAND->PLAN->EXECUTE->DELIVER agent loop: builds a plan prompt, validates the LLM reply against a Pydantic ExecutionPlan schema, runs it through a real PlanExecutor (app/core/plan_executor.py, 171 lines), then writes a DELIVER answer bounded per-step to avoid truncation, with controlled degrade messages (no raw exceptions ever surface to the caller) and document-excerpt-grounded fallback when planning fails. This is a working instance of REASONING_KERNEL.md's ReasoningPlanner (#14) â€” but block_registry/project_reasoner/ has no block.py/Dockerfile, so it cannot be dispatched today.

RecommendationComplete registration. This block is a direct, already-built answer to the 'ReasoningPlanner' component the reasoning-kernel mission lists as unbuilt.






smart_orchestrator
361 lines


Cerebrum-Blocks · app/blocks/smart_orchestrator.py

361 lines. Implements real TF-IDF vectorization and cosine similarity from scratch (no library), a 39-action keyword corpus, file-extension routing, parallel-group detection, and skill-hint enrichment via a wired skills block. Registered with block.py+Dockerfile. No stub patterns found.

RecommendationNone â€” solid as-is.






adaptive_router
497 lines, registered


Cerebrum-Blocks · app/blocks/adaptive_router.py

497 lines, registered. The scoring math (success-rate/latency/cost weighted composite, time-of-day pattern learning, A/B hash-based assignment) is real, hand-tuned code, not a stub. BUT: (1) _get_providers_for_task hardcodes exactly one real provider per task type ('kimi' for chat, 'local' for embed, 'stability' for image) â€” there is nothing to meaningfully route between, so 'adaptive routing' and 'exploration/exploitation' are largely vacuous in production. (2) _load_historical_data is a literal TODO stub: # TODO: Load from analytics \n pass â€” despite requires=['analytics','monitoring','failover'] and the class docstring claiming it 'Learns from historical performance', no historical data is ever actually loaded from analytics; all state (self.history, self.provider_scores, self.time_patterns) is in-process only and resets on restart.

RecommendationEither wire _load_historical_data to a real analytics/persistence backend (matching learning_engine.py's storage_backend injection pattern) or relabel the block's claims to match reality: single-provider heuristic scorer with no cross-restart memory.






orchestrator
566 lines, registered


Cerebrum-Blocks · app/blocks/orchestrator.py

566 lines, registered. Builds a full execution graph upfront with per-step type validation (build_execution_graph), real DataTransformer-based type conversion with fallback compatibility rules, per-step progress callbacks, memory persistence of intermediate outputs, and graceful traffic-manager routing/queueing integration. No stub patterns found.

RecommendationNone â€” solid as-is. Its type-validated step-chain model is workflow-adjacent (partial overlap with REASONING_KERNEL.md's WorkflowEngine #9) but lacks approval gates, permitted-actor checks, or state-machine semantics, so it should be considered a head start, not a replacement, for WorkflowEngine.






learning_engine
575 lines, registered


Cerebrum-Blocks · app/blocks/learning_engine.py

575 lines, registered. Real numpy least-squares coefficient fitting (_fit_linear) with a non-numpy fallback, a genuine tier-promotion ladder (bronze/silver/gold/platinum) driven by execution counts, a separate CredibilityScorer/CredibilityRecord system (app/core/credibility.py) with drift detection that compares live accuracy against accuracy-at-promotion, MLflow metric logging on drift (best-effort, never breaks the caller), and a Celery retrain-enqueue path with an honest pending_retrains fallback queue when Celery/broker is unavailable. This is one of the most substantive blocks in the cluster.

RecommendationNone â€” solid as-is. Its drift-detection/credibility-tier logic is a real, usable building block toward REASONING_KERNEL.md's VerificationOracleRunner (#19)/AssumptionRegistry (#17) concepts and should be considered during the reasoning-kernel build rather than reinvented.






recommendation_template
303 lines, registered


Cerebrum-Blocks · app/blocks/recommendation_template.py

303 lines, registered. Category/severity are derived from actual input fields (variance_pct, delay_days, test_result, risk_level, carbon_kg), produces data-driven action items per category/severity combination, and supports a custom-rule overlay loaded from a JSON file path (RECOMMENDATION_RULES_PATH) with a real condition matcher (eq/gt/lt/gte/lte/between/contains). Not a stub.

RecommendationThis condition-matching rule engine is a real, working fragment of REASONING_KERNEL.md's RuleEngine (#7)/DecisionTableEngine (#8) â€” worth reusing rather than building those from zero.






historical_benchmark
234 lines, registered


Cerebrum-Blocks · app/blocks/historical_benchmark.py

234 lines, registered. The core lookup (14 hardcoded USD unit rates x location factors x project-type factors) is honestly self-described in the module docstring as 'intentionally simple: no drifting hardcoded RS-Means snapshot, no ML model.' However, the 'record' action returns {"status": "success", ... "message": "Rate sample recorded (stub â€” learning_engine integration pending)."} â€” it reports success for an action that persists nothing at all. The stub is labeled in the message text (not deceptive on close reading), but a caller checking only status would believe a rate sample was actually recorded.

RecommendationWire 'record' to the already-working learning_engine.record_correction path (learning_engine.py in this same cluster) instead of returning status:success for a no-op, or change the status to something other than 'success' until it's wired.






context_broker
195 lines, registered


Cerebrum-Blocks · app/blocks/context_broker.py

195 lines, registered. Real CRUD (get/set/merge/clear) delegating to a wired memory block â€” no stub logic in the actions themselves. But its ui_schema.output.fields advertises drive_files/chat_history/construction_data keys that _get_context never returns (only {session_id, memory} is ever produced) â€” the declared output contract doesn't match actual behavior.

RecommendationLow priority: either implement the advertised drive_files/chat_history/construction_data aggregation or trim the ui_schema to match what the block actually returns.






agent_swarm
786 lines, registered


Cerebrum-Blocks · app/blocks/agent_swarm.py

786 lines, registered. Real topological-sort dependency resolution with wave-based parallel execution (asyncio.gather), a real tool-calling loop bounded by _MAX_TOOL_ITERATIONS with MCP-contract-based direct block dispatch (no HTTP), per-agent tool allowlisting resolved lazily against BLOCK_REGISTRY (explicitly avoiding importing every block to save memory on constrained dynos), and vector-memory storage of swarm results with an HTTP-Chroma fallback. Demo-mode fallback when no agents/tasks are supplied is clearly labeled 'DEMO ONLY' rather than pretending to be real output. No stub or vacuous-success patterns found.

RecommendationNone â€” solid as-is.






safety_world_detector
112 lines


Cerebrum-Blocks · app/blocks/safety_world_detector.py

112 lines. A genuine YOLO-World (ultralytics) ONNX detector wrapper â€” real class-name extraction from baked weights, real bbox/confidence parsing from model output, and an honest docstring explicitly disclaiming that it makes any 'violation'/'non-compliance' judgment (that's left to an application layer with a human in the loop). However it is a plain Python class, NOT a UniversalBlock/TypedBlock subclass â€” no process()/execute() block contract, no name/version/tags â€” so it cannot be registered in block_registry as-is; it's presumably a helper consumed internally by some other (unlisted) safety-inspection block. It also has a hard external dependency on a pre-baked .onnx file via SAFETY_WORLD_WEIGHTS that is not present anywhere in this repo â€” default_detector() silently returns None if unset/missing, so its real-world behavior is entirely dependent on an artifact this repo doesn't ship.

RecommendationIf this is meant to be store capability, wrap it as a proper UniversalBlock/TypedBlock with a process() method and register it; otherwise document clearly which registered block(s) actually consume it, since none of the other 14 blocks in this cluster reference it.







cluster Â· media-vision-misc 3




discovery
discovery.py declares requires=["vector","analytics"] and default_config["recommendation_model"]="embedding_similarity", and itâ€¦


Cerebrum-Blocks · app/blocks/discovery.py

discovery.py declares requires=["vector","analytics"] and default_config["recommendation_model"]="embedding_similarity", and its docstring says 'Uses vector similarity ... for recommendations'. But _recommend_stack (line ~299) does keywords = use_case.lower().split(); matches = sum(1 for kw in keywords if kw in goal_lower) with a literal comment # Simple keyword matching (TODO: use embeddings), and _semantic_search (line ~394) does any(term in profile.name.lower() for term in query_terms) substring checks. Neither method, nor _index_block, ever calls the vector or vector_search block (grep for 'vector'/'embedding' in the file only turns up TODOs and the hardcoded catalogue text). block_profiles itself is a ~166-line hardcoded dict built in _index_builtin_blocks, not populated from any real index.

RecommendationRoute recommend/search through vector.py's cosine-similarity search (already implemented with real sentence-transformers embeddings and a working _cosine_similarity) or vector_search.py's TF-IDF search instead of keyword substring matching. Either remove the 'embedding_similarity' / 'vector similarity' claims from the docstring and config, or actually implement them â€” currently the block's stated capability and its code diverge.






review
async def _verify_purchase(self, data: Dict) -> Dict (line 282) reads: `# TODO: Verify actual purchase/usage in billing systeâ€¦


Cerebrum-Blocks · app/blocks/review.py

async def _verify_purchase(self, data: Dict) -> Dict (line 282) reads: # TODO: Verify actual purchase/usage in billing system followed immediately by key = (user_id, block_id); self.verified_purchases.add(key); return {"verified": True, ...}. It unconditionally marks any (user_id, block_id) pair as a verified purchase with zero check against billing/database, regardless of whether the user ever bought or used the block. This flag almost certainly gates a 'verified purchase' badge shown on the block store to buyers â€” a marketplace trust signal that is fabricated as written.

RecommendationCall the billing block (review.requires includes no billing dep currently â€” add one) to confirm an actual transaction/usage record for (user_id, block_id) exists before setting verified=True; return verified:false with a reason when it does not.






sandbox
_execute_python (line 180) only reaches real isolation if os.getenv("SANDBOX_RUNNER_URL") is set; otherwise it falls throughâ€¦


Cerebrum-Blocks · app/blocks/sandbox.py

_execute_python (line 180) only reaches real isolation if os.getenv("SANDBOX_RUNNER_URL") is set; otherwise it falls through to in-process exec(code, safe_globals) where safe_globals["__builtins__"] is built by _get_restricted_builtins (line 260), which just copies dir(__builtins__) minus a blocklist of names. This is Python's well-documented broken 'restricted exec' pattern â€” any object graph left reachable (e.g. via ().__class__.__bases__[0].__subclasses__()) still exposes the full interpreter regardless of which builtin names are removed, so untrusted code can escape. Separately, _validate_code (line 458), which is supposed to catch dangerous code before execution, does plain substring checks like if pattern in code for "eval(", "exec(", "__import__" etc. â€” trivially bypassed by string concatenation, getattr-based indirection, or just not containing the literal substring. Given sandbox is validation.py's declared execution engine (requires=["sandbox","database"]) for certifying third-party block code, the store's actual isolation boundary for untrusted submissions is weak by default.

RecommendationMake the external SANDBOX_RUNNER_URL path (real process isolation) mandatory rather than best-effort-with-insecure-fallback for any code that isn't already trusted/first-party, and replace the substring _validate_code with the ast-based analysis pattern already used in validation.py's own security scan.






Also checked â€” nothing to report


- repo Â· Cerebrum I could not directly inspect the Store's 108 registered app/blocks/ file contents in this session (only the survey brief's summary list of names was available), so I cannot make a concrete before/after comparison of over


- repo Â· The_Level The context's Store inventory lists a validation_pipeline app/blocks file (unregistered) but its purpose/contents were not available to compare against in this survey (the Store repo itself was not re-cloned in this sess







Not stocked
Connector & MCP targets


Enterprise systems (construction, ERP, hospitality, CRM, HR/procurement) with no Store connector today, plus what already exists partially in a product repo.



repo Â· Cerebrum 1




External enterprise systems referenced/integrated
Two distinct tiers found


backend/app/integrations/*.py (real, live-wired), backend/app/stubs/{aconex,primavera,procore,slack}.py (explicit mock stubs), backend/app/vdc/{revit_link,tekla_link,navisworks,solibri}.py (BIM desktop-plugin mocks)

Two distinct tiers found. (1) REAL, working OAuth2/REST integrations, live-wired into FastAPI routes: Procore (integrations/procore.py, hits real api.procore.com), QuickBooks + Xero (accounting.py, erp.py), Salesforce + HubSpot (crm.py), DocuSign (esignature.py), Box + Dropbox (file_storage.py), Microsoft 365 -- Teams/SharePoint/Outlook (microsoft_365.py), Slack (slack.py, hits slack.com/api), Zapier (zapier.py). Also oddly, viabtc.py -- a real client for the ViaBTC bitcoin mining pool API (pool.viabtc.com), unrelated to construction, presumably vestigial from a different project iteration. (2) EXPLICIT STUBS honestly self-labeled as such: backend/app/stubs/aconex.py ('Oracle Aconex Stub' -- hardcoded mock mail/document lists, e.g. _mail_items with fixed 'M001'/'M002' entries, methods literally return message="...(stub - not stored)") and backend/app/stubs/primavera.py ('Oracle Primavera P6 Stub' -- hardcoded mock projects/activities/resources). (3) BIM desktop-plugin integrations (Revit, Tekla, Navisworks) that are architecturally reasonable (Revit/Tekla/Navisworks have no public cloud REST API, so a plugin-bridge design is correct) but are currently implemented as mocks -- revit_link.py's RevitAPIClient.get_model_info/export_elements literally comment 'In practice, this would query Revit API' and return hardcoded/loop-generated fake data (e.g. project_name: 'Sample Project', 100 synthetic wall/floor/door elements).

RecommendationThe real Procore/QuickBooks/Salesforce/DocuSign/etc. connectors are directly extractable. The Aconex/Primavera stubs and Revit/Tekla/Navisworks mocks are honest placeholders showing intended integration surface but should NOT be represented as working connectors if pulled into the Store -- they'd need real API/plugin-bridge work first.







repo Â· Cerebrum-Steward 3




Honest stub connectors for CMMS, IoT, document vault, smart-home
app/connectors/__init__.py declares 4 connectors all with {"honest": true, "status": "not_implemented"}: cmms_stub, iot_stub, dâ€¦



app/connectors/__init__.py declares 4 connectors all with {"honest": true, "status": "not_implemented"}: cmms_stub, iot_stub, document_vault_stub, smart_home_placeholder. Each individual file (app/connectors/cmms_stub.py etc., 3 lines each) is literally just STATUS = "not_implemented" with a docstring 'Honest stub connector'. README.md explicitly states: 'Smart-home / IoT connectors are honest placeholders -- labeled as such, not wired to live devices.' No CMMS product (e.g. a specific vendor like UpKeep, Fiix, Maintenance Connection) is named anywhere in code; 'CMMS' is used only as the generic category.

RecommendationIf the Factory wants real CMMS/IoT/document-vault connectors, these stub files are the intended extension points (app/connectors/) -- they mark exactly where integration work should land, but nothing here is presently a working connector to clone.






Mews / Cloudbeds referenced only as citation sources in a hospitality knowledge pack (not integration code)
manifest.json lines 18-19 list 'https://www.mews.com/en/blog/hotel-checklist' and 'https://www.cloudbeds.com/articles/hotel-tasâ€¦


app/steward/packs/steward_service_core_open_v1/manifest.json:18-19

manifest.json lines 18-19 list 'https://www.mews.com/en/blog/hotel-checklist' and 'https://www.cloudbeds.com/articles/hotel-task-management/' as content-source URLs for a bundled SOP knowledge pack (housekeeping/turndown/arrival docs under app/steward/packs/steward_service_core_open_v1/mini/*.md). No Mews/Cloudbeds client, auth, or endpoint code exists anywhere in the repo.

RecommendationNo action. A real Opera/Cloudbeds/Mews PMS integration is not present in this repo despite the domain (private estate + hospitality SOPs) being adjacent to hotel PMS use cases.






Slack/Telegram/Webhook notification config schema is a vendored Store block, not Steward's own work
block.json describes 'Send notifications via Telegram, Email, Webhook, or Slack' and exposes a 'Slack Webhook Url' field -- butâ€¦


vendor/blocks/notification/block.json:3,159

block.json describes 'Send notifications via Telegram, Email, Webhook, or Slack' and exposes a 'Slack Webhook Url' field -- but this is the vendored copy of the Store's own generic notification block (paired with the standard 40-line adapter pattern confirmed in vendor/blocks/notification/block.py), not something Cerebrum-Steward built. No Slack-specific code exists in app/.

RecommendationNo action needed.







repo Â· InsureOps 1




Simulated carrier/AMS/CRM connectors (Guidewire, Salesforce, Applied Epic, VUE) plus Slack webhook
Confirmed by the repo's own docs/FEATURE_TECHNICAL_AUDIT.md line 308: 'Connector payloads: Mock only (Guidewire / SFDC / Applieâ€¦


InsureOps · backend/app/insureops/integrations/service.py lines 26-48 (_CONNECTOR_META), lines 215-234 (_mock_connector_payload sets 'simulated': True, 'external_api_calls': 0), lines 169-206 (_read_rows_from_path, real SFTP-style file-drop CSV/JSON parsing, no network calls); also backend/app/insureops/integrations/actions.py, backend/app/insureops/block_consumers.py, backend/app/insureops/models.py (ConnectorStub), backend/tests/test_feeds_f7.py; kit/bundle/app/blocks/notification.py lines 357-377 has a real Slack webhook POST (_send_slack, httpx call with real error branches) reading SLACK_WEBHOOK_URL from env.

Confirmed by the repo's own docs/FEATURE_TECHNICAL_AUDIT.md line 308: 'Connector payloads: Mock only (Guidewire / SFDC / Applied Epic / VUE shapes)' and line 553: 'Live carrier APIs parked (F7 mocks)' -- an honest self-disclosure, not a false completeness claim like the vacuous gates this session has flagged elsewhere.

RecommendationIf building real connectors later, the _remap_row/_rows_from_payload field-aliasing logic in integrations/service.py is a reasonable adapter shape to wire real Guidewire/Salesforce/Applied Epic/VUE API clients into without changing the bordereaux ingestion path. The Slack notification path in notification.py is already real and could be exercised directly given a SLACK_WEBHOOK_URL.







repo Â· TEKsystems_GlobalRetailMNC 3




Power BI connector â€” real Azure AD OAuth2 + live Power BI REST call
backend/app/retailops/integrations/connectors/power_bi.py:85-105 _acquire_token() performs a genuine Azure AD client-credentialâ€¦



backend/app/retailops/integrations/connectors/power_bi.py:85-105 _acquire_token() performs a genuine Azure AD client-credentials POST (httpx.AsyncClient) and parses a real access_token; test_connection() (lines 107-132) uses that token to GET https://api.powerbi.com/.../groups/{workspace_id} and reports CONNECTED/ERROR based on the real HTTP response. Dataset publish and semantic-model refresh are explicitly returned as status:'not_implemented' honest stubs (lines 157-174), and get_embed_url returns a labeled 'placeholder' embed URL (lines 175-181) rather than fabricating a live dashboard.

RecommendationN/A â€” noted as the most genuinely 'live' connector found; worth using as the template for how partial connectors should honestly report capability.






SAP / ERP and Microsoft Entra ID â€” explicit honest stubs (no vendor adapter)
backend/app/retailops/integrations/connectors/sap_erp.py:1-7 docstring states 'No SAP/ERP vendor adapter is implemented'; pull(â€¦



backend/app/retailops/integrations/connectors/sap_erp.py:1-7 docstring states 'No SAP/ERP vendor adapter is implemented'; pull()/push() (lines 77-90) unconditionally raise ConnectorError(error_code='not_implemented') even when credentials are present â€” status can reach CONFIGURED but data operations are refused, never fabricated. entra_id.py mirrors this pattern for SSO/roles/groups (RetailOps currently authenticates via server-resolved X-User-Id/X-Tenant-Id/X-Project-Id headers per README.md:231).

RecommendationN/A â€” flagged only as an aspirational/stub integration target (SAP S/4HANA, SAP ECC, Microsoft Entra ID) referenced in vendor_examples and README.md:224-231; no adapter exists to extract.






POS, Inventory/WMS, Supplier EDI, CRM/Loyalty â€” mock_functional deterministic connectors
pos.py (NCR/Toshiba/D365 Commerce, mock_functional=True, line 41), inventory_wms.py (Manhattan/Blue Yonder/RELEX, mock_functionâ€¦



pos.py (NCR/Toshiba/D365 Commerce, mock_functional=True, line 41), inventory_wms.py (Manhattan/Blue Yonder/RELEX, mock_functional=True line 42, with a real to_inventory_risk_rows() adapter at line 127 that feeds live deterministic data straight into retail.analyse_inventory_risk), supplier_edi.py (Ariba/EDI 850/856/810, mock_functional=True line 35), crm_loyalty.py (Salesforce Loyalty / Generic CRM, vendor_examples line 35). All generate deterministic synthetic data rather than calling a real vendor API â€” genuinely labeled as mock, not misrepresented as live.

RecommendationThese are real vendor-name references and a functioning mock/deterministic-data contract (useful as connector scaffolding/interface), but no live vendor API calls exist; treat as connector targets for future real adapters, not as extractable integration code.







repo Â· The_Fork 3




Oracle Aconex CDE integration (real, functional)
app/core/cde/aconex.py documents live Oracle endpoints ('Resource server: https://api.aconex.com', 'POST {LOBBY}/auth/token', 'â€¦



app/core/cde/aconex.py documents live Oracle endpoints ('Resource server: https://api.aconex.com', 'POST {LOBBY}/auth/token', 'GET /api/projects/{projectid}/mail', 'POST /api/projects/{projectid}/mail/search (API News May 2026)') and implements real XML parsing (xml.etree.ElementTree), mail/document/RFI/transmittal listing, and posting RFI drafts. app/core/cde/factory.py wires a fail-closed selector: fake adapter for tests (CDE_ADAPTER=fake), live AconexCdeClient when ACONEX_ENABLED+credentialed, else NotConfiguredCdeClient that raises on every call rather than fabricating data. app/blocks/aconex.py is the block wrapper; app/core/cde/protocol.py's module docstring states 'Vendor-neutral CDE client. Procore / ACC can implement the same door later' -- an explicit, currently-unimplemented extension point named for Procore and Autodesk Construction Cloud (ACC). Tested via tests/test_cde_connector.py, tests/test_cde_cm_rules.py.







Autodesk BIM 360 / Azure Digital Twins / AVEVA Connect (aspirational, explicitly disclosed as non-functional)
app/containers/construction/documents.py's digital_twin_sync() builds real API payloads and connection strings for three platfoâ€¦



app/containers/construction/documents.py's digital_twin_sync() builds real API payloads and connection strings for three platforms but self-discloses it never calls them: connection_strings dict includes 'bim360': 'https://developer.api.autodesk.com/modelderivative/v2/designdata/{project_id}', 'azure': 'https://{project_id}.api.weu.digitaltwins.azure.net', 'aveva': 'connect.aveva.com/{project_id}', with an explicit honesty comment in-code: 'the name overpromises: this PREPARES the platform sync payloads... but does NOT push to any live digital-twin platform. status:"success" means "prepared", not "synced".' and a returned field 'sync_status': 'prepared_not_pushed'.







Primavera P6 (file-format connector, not a live API)
app/blocks/primavera_parser.py: 'Parse Primavera P6 .xer schedule files: low-float activities, milestones, resource definitionsâ€¦



app/blocks/primavera_parser.py: 'Parse Primavera P6 .xer schedule files: low-float activities, milestones, resource definitions' with a real XER tokenizer/parser in app/lib/pm_computations.py (parse_xer_full/parse_xer), tested by tests/test_xer_truncation_disclosure.py.








repo Â· ThreadForge 1




Primavera P6 XER export, MS Project MSPDI export, Autodesk IFC4/DXF/PCF format support (all offline file-format, no live vendor API), AVEVA/Hexagon named but explicitly WALLed off
export_xer() in schedule_io.py writes real Primavera-format tab-separated records (ERMHDR header, %T PROJECT/%T TASK/%T TASKPREâ€¦


ThreadForge/src/threadforge/exporters/schedule_io.py (export_xer, lines 69-95: ERMHDR/%T TASK/%T TASKPRED records), src/threadforge/exporters/ifc.py (321 lines, ifcopenshell-based IFC4 writer), src/threadforge/exporters/ifc_in.py (371 lines, IFC4 ingest to AABB/capsule), src/threadforge/exporters/dxf.py (117 lines, ezdxf-based DXF writer), src/threadforge/generators.py (PCF text writer ~lines 880-950), WALLS.md sections 1-5

export_xer() in schedule_io.py writes real Primavera-format tab-separated records (ERMHDR header, %T PROJECT/%T TASK/%T TASKPRED tables with proj_id/task_code/task_name/task_type/start_date/end_date fields) that re-parse to a task count matching exported IWPs (per README acceptance item A29 and tests/test_schedule_export.py). MSPDI export is validated against a vendored Microsoft Project XSD (fixtures/public/mspdi/mspdi.xsd). These are genuine, structurally-correct file-format exports usable by real P6/MSP installs -- not live API calls; WALLS.md explicitly states 'Agent tools never call vendor clouds' for Autodesk/AVEVA/Hexagon, and there is no OAuth/API-key/HTTP-client code anywhere in the repo pointing at those vendors' live services. IFC4 output uses the real ifcopenshell library; DXF output uses ezdxf; PCF output is a structurally-valid-but-not-ISOGEN-certified subset (generators.py explicitly labels it 'not Autodesk-certified').

RecommendationIf the Factory wants a Store connector_target for construction-schedule interop, ThreadForge's XER/MSPDI exporters are a ready-made, tested (tests/test_schedule_export.py, test_exporters.py) reference for a Primavera P6 / MS Project export connector block -- distinct from any live-API integration since none exists here.







repo Â· stockwise-whatsapp 1




Twilio WhatsApp webhook integration
src/server.ts:16-27 implements the actual Twilio 'When a message comes in' webhook: POST /whatsapp reads req.body.Body/From (Twâ€¦


stockwise-whatsapp · stockwise-whatsapp/src/server.ts, src/twiml.ts, src/config.ts, README.md

src/server.ts:16-27 implements the actual Twilio 'When a message comes in' webhook: POST /whatsapp reads req.body.Body/From (Twilio's form-encoded field names), calls handleMessage, and replies with TwiML XML via res.set('Content-Type','text/xml').send(twiml(reply)). src/twiml.ts:13-15 hand-builds the <Response><Message> XML envelope Twilio requires (with XML-escaping at twiml.ts:4-10). README.md 'Go live (Twilio Sandbox)' section documents the 3 manual steps to wire a real Twilio Sandbox WhatsApp number to this webhook (join code, sandbox 'When a message comes in' URL, Render deploy). Notably, config.ts:14-16 declares twilioAuthToken and publicUrl for validating Twilio's X-Twilio-Signature header, but grep across src/ shows these two fields are never read anywhere outside config.ts -- signature validation is declared as a config option but not implemented, matching the README's own 'Optional... recommended in production' framing (it is not falsely claimed as done, just absent).

RecommendationIf the Factory wants a general WhatsApp/Twilio connector block (none of the 108 registered blocks or the ~140 app/blocks/ files is a Twilio/WhatsApp adapter by name), this is a small, clean, working reference implementation to extract: webhook receiver -> TwiML responder, ~90 lines total (server.ts+twiml.ts). It would need the missing X-Twilio-Signature verification added before treating it as production-grade, and its business logic (handler.ts) is StockWise-specific so only the transport layer (server.ts/twiml.ts pattern) generalizes, not the whole repo.







repo Â· stockwisepro-bot 4




WhatsApp (Twilio)
Real Twilio webhook handler (POST /api/whatsapp) reusing the same OpenBox engine and OCR service, downloads media from Twilio mâ€¦


src/web/whatsapp.ts (280 lines), src/web/server.ts:72

Real Twilio webhook handler (POST /api/whatsapp) reusing the same OpenBox engine and OCR service, downloads media from Twilio media URLs with basic auth (whatsapp.ts:175-183), and implements its own greeting/intent parser (parseIntent(), lines 48+).







Alpaca Markets (brokerage/trading API)
Dedicated API client and Telegram command wired to Alpaca's paper/live trading REST API for account/position data.


src/api/alpaca.ts (198 lines), src/commands/alpaca.ts (75 lines), .env.example (ALPACA_API_KEY/ALPACA_SECRET_KEY/ALPACA_PAPER)

Dedicated API client and Telegram command wired to Alpaca's paper/live trading REST API for account/position data.







Databento (institutional live market data)
Dedicated client plus a full integration doc describing the market-data feed integration.


src/api/databento.ts (134 lines), docs/DATABENTO_INTEGRATION.md, .env.example (DATABENTO_API_KEY)

Dedicated client plus a full integration doc describing the market-data feed integration.







Financial Modeling Prep (FMP), Google Cloud Vision, Brave Search, DuckDuckGo, Ollama, Upstash/Redis, Telegram Bot API
All present as live, keyed integrations with real request code and .env-documented credentials (not stubs) - none are enterprisâ€¦


src/api/fmp.ts (260 lines), src/services/ocr.ts:354-379 (Cloud Vision), src/api/brave.ts, src/api/duckduckgo.ts, src/services/llm.ts, src/services/cache.ts, .env.example

All present as live, keyed integrations with real request code and .env-documented credentials (not stubs) - none are enterprise systems like Procore/SAP/Salesforce/DocuSign/hotel-PMS/P6/BIM 360/QuickBooks/Workday/Slack; a grep across the repo for those specific enterprise-system names only turned up unrelated stock tickers (SAP, Workday, Autodesk, Salesforce appear solely as scoreable equities in data/stock_universe.json and data/investor_sheet.json, not as integrations).








repo Â· teamhub-test-platform 4




Moonshot / Kimi (moonshot.ai) â€” live, working
block_store/kits/universal_kernel/wave2/llm_provider/moonshot.py implements a full HTTP client against https://api.moonshot.ai/â€¦



block_store/kits/universal_kernel/wave2/llm_provider/moonshot.py implements a full HTTP client against https://api.moonshot.ai/v1/chat/completions (DEFAULT_BASE_URL, line 32), gated by MOONSHOT_API_KEY. app/wiring.py:172-180 (make_llm) switches to this live provider automatically when MOONSHOT_API_KEY is set in the environment, falling back to the deterministic stub otherwise.

Recommendationn/a â€” informational; flagged because it's a genuine enterprise/SaaS integration point worth cataloguing.






Stripe â€” explicitly referenced but NOT connected (aspirational)
app/server.py:564: "Billing itself is pending (Stripe) â€” trial plan is the launch default." and README.md: 'Billing itself (Sâ€¦



app/server.py:564: "Billing itself is pending (Stripe) â€” trial plan is the launch default." and README.md: 'Billing itself (Stripe) is not connected â€” the trial plan is the launch default.' The billing_entitlement kit (block_store/kits/universal_kernel/wave3/billing_entitlement/code.py) only tracks quota/usage counters in memory; there is no Stripe API call anywhere in the repo (confirmed by grep across all .py files).

RecommendationThis is a genuine, named connector gap (Stripe billing) that the platform explicitly flags as unbuilt â€” a real target for the Factory's connector layer.






Slack + SMTP â€” real transport hooks, fail-closed when unconfigured
block_store/kits/universal_kernel/wave3/notification_mailer/code.py: NotificationChannel enum includes slack = "slack" (lineâ€¦



block_store/kits/universal_kernel/wave3/notification_mailer/code.py: NotificationChannel enum includes slack = "slack" (line 16); _send_webhook(message, slack_mode=True) (line 73) routes Slack messages through a generic webhook transport keyed off WEBHOOK_URL/injected transport object. SMTP is configured via real env vars (SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASSWORD, lines 55-58) and _send_email (lines 84-90) requires an injected transport object or returns an honestly-labeled honesty="no_transport_fallback" dict rather than silently claiming delivery. No real SMTP/Slack SDK call is implemented in this neutral kit â€” it's a real integration point/interface, not a live connector.

RecommendationInterface-level connector target; would need a concrete Transport implementation (smtplib or requests-based Slack webhook POST) to become live.






PostgreSQL / pgvector â€” referenced but stubbed, no real SQL
block_store/kits/universal_kernel/wave2/vector_store/code.py PgvectorStore.__init__ (lines 111-115) only checks that DATABASE_Uâ€¦



block_store/kits/universal_kernel/wave2/vector_store/code.py PgvectorStore.__init__ (lines 111-115) only checks that DATABASE_URL is set and raises VectorStoreError if missing â€” it then inherits VectorStore's in-memory behavior unchanged (no actual pgvector query anywhere). Likewise PostgresJobStore in wave2/durable_jobs/code.py:178-183 requires DATABASE_URL but its own comment admits 'Backend stub: no actual table operations in this neutral kit.' Both classes are aspirational connector shells, not real persistence.

RecommendationFlag as aspirational/named-but-unimplemented; do not report as a working Postgres integration.







cluster Â· hotel 1




Hotel ancillary-system connector suite (maximo, micros, grms, gaming_cms, loyalty_lms)
All five files (26-59 lines each in v2, 37-59 in v1) implement genuine normalise() mappers following the same connect/fetch/norâ€¦


cerebrum-hotelops-v2 (and cerebrum-hotelops) · cerebrum-hotelops-v2/connectors/{maximo,micros,grms,gaming_cms,loyalty_lms}.py

All five files (26-59 lines each in v2, 37-59 in v1) implement genuine normalise() mappers following the same connect/fetch/normalise/emit contract as opera.py: maximo.py (IBM Maximo facilities/maintenance), micros.py (Oracle MICROS POS), grms.py, gaming_cms.py (casino gaming CMS), loyalty_lms.py. The Store's hotel_management kit only has a stub for opera_connector â€” none of these five exist there in any form, stubbed or otherwise. v2's versions additionally inherit the honest MockLevel.MOCK_UNAVAILABLE fail-closed base from connectors/base.py (same discipline noted for opera.py above).

RecommendationClone this connector family into the Store as new blocks alongside (or replacing) the current single opera_connector stub in the hotel_management kit, using v2's connectors/base.py + these five files as the reference implementation for the fail-closed/no-fabrication pattern.







research Â· construction-project-mgmt 4




Aconex (Oracle Construction and Engineering)
Store repo (Cerebrum-Blocks, HEAD e3696bf): zero hits for 'aconex' anywhere in app/blocks, app/containers, or block_registry â€”â€¦



Store repo (Cerebrum-Blocks, HEAD e3696bf): zero hits for 'aconex' anywhere in app/blocks, app/containers, or block_registry â€” confirmed absent. The_Fork repo (bopoadz-del/The_Fork) has a full, real CDE (Common Data Environment) subsystem at app/core/cde/: protocol.py (vendor-neutral CdeClient ABC), aconex.py (339 lines -- real httpx AsyncClient, real OAuth2 client_credentials grant against the documented Oracle Lobby endpoint POST {LOBBY}/auth/token with Basic-auth client_id:client_secret, real endpoint paths matching Oracle's public API docs: GET /api/projects/{id}/mail, GET /api/projects/{id}/register, POST /api/projects/{id}/mail/ multipart XML for Create Mail, XML response parsing via xml.etree.ElementTree), config.py (real env-var driven config: ACONEX_ENABLED, ACONEX_CLIENT_ID/SECRET, ACONEX_LOBBY_URL, ACONEX_API_BASE), factory.py (get_cde_client() resolves fake/aconex/NotConfigured -- fails closed, raises CdeNotConfiguredError rather than fabricating an RFI register when credentials are absent -- read in full, no stub-success pattern), poll.py (event polling/dedup), fake.py (in-memory test double), and app/blocks/aconex.py (147-line UniversalBlock wrapper exposing operations: status, list_mail, list_documents, list_rfis, list_transmittals, get_mail, get_document, sync, post_rfi/post_mail). This is real, tested (tests/test_cde_connector.py, test_cde_cm_rules.py exist), and explicitly designed to never invent local data when the OAuth handshake fails -- the opposite of the vacuous-success pattern this survey has been flagging elsewhere.

RecommendationAuth model: OAuth2 client_credentials against Oracle's Construction and Engineering Lobby (already implemented, documented). Integration pattern: port app/core/cde/ (protocol.py, aconex.py, config.py, factory.py, fake.py, errors.py, types.py, poll.py, ingest.py) from The_Fork into the Store as a new registered block family, keeping the CdeClient ABC so a future Procore adapter is a second class implementing the same interface (the protocol.py docstring already names this as the intent: 'Procore / ACC can implement the same door later'). First concrete block to build: register 'aconex' in block_registry/ wrapping the existing app/blocks/aconex.py logic verbatim (it is already a UniversalBlock, layer 4, tags=['integration','cde','aconex','construction']) -- this is close to a direct lift, not a rewrite. Expose it externally via mcp_adapter.py's existing tool-catalog synthesis (it already auto-advertises any registered block's get_actions() as an MCP tool enum, so the aconex block's operations would surface for free once registered) -- no new MCP plumbing needed for outbound exposure; mcp_consumer.py's stdio-MCP-client pattern is not the right fit here since Aconex has no MCP server, only a documented REST/XML API.






Procore
Store repo: zero hits for 'procore' in any .py file



Store repo: zero hits for 'procore' in any .py file. The_Fork repo: exactly one hit in the entire codebase -- app/core/cde/protocol.py line 1: '"""Vendor-neutral CDE client. Procore / ACC can implement the same door later."""' -- a docstring aspiration, no class, no endpoint, no auth code. bim-manager-agent repo: zero hits. This is stated explicitly as a greenfield recommendation, not an existing partial reference beyond that one design note.

RecommendationAuth model: OAuth2 authorization_code (Procore requires a registered app + user consent redirect, not client_credentials -- differs from Aconex, so it cannot reuse Aconex's token flow verbatim, only the CdeClient shape). Integration pattern: implement a second CdeClient (e.g. app/core/cde/procore.py) against Procore's REST API (company/project/tool-scoped endpoints: /rest/v1.0/projects/{id}/rfis, /submittals, /documents, /schedule) mirroring aconex.py's fail-closed structure (raise CdeNotConfiguredError rather than fabricate). First concrete block to build: a 'procore' block registered the same way as the recommended 'aconex' block, selected via the existing factory.py's adapter-resolution pattern (CDE_ADAPTER=procore) so a generated platform can point at either vendor through the same downstream code (RFI sync, document cache, CM overlay) without touching call sites -- the vendor-neutral protocol.py ABC already anticipated this.






Autodesk Construction Cloud / BIM 360 (APS)
Store repo: one substantive hit, app/containers/construction/documents.py digital_twin_sync() method (lines ~1005-1052) -- readâ€¦



Store repo: one substantive hit, app/containers/construction/documents.py digital_twin_sync() method (lines ~1005-1052) -- read in full. It builds a hardcoded 'connection_strings' dict containing a BIM 360 Model Derivative URL string (f"https://developer.api.autodesk.com/modelderivative/v2/designdata/{project_id}") and an 'authentication_required: OAuth2' label, but the method never makes an HTTP call -- no httpx/requests import in the file, no token exchange, no real request. It fabricates 'operations' and 'api_payloads' entirely in Python from input data and returns the Autodesk URL as descriptive metadata only -- this is a template/placeholder, not a working connector, and should not be cited as existing integration. The_Fork's app/blocks/bim_extractor.py (646 lines) is real and substantial but is local-file IFC geometry/quantity extraction only (IFC_CATEGORY_MAP, bounding-box clash detection); its only Autodesk mentions are advisory text telling users to export proprietary Revit/Navisworks files to IFC first, or 'use the Autodesk Platform Services' -- again no live API call. bim-manager-agent repo: zero hits for autodesk/forge/bim360/APS at all despite the name suggesting relevance.

RecommendationAuth model: OAuth2 (3-legged for user-context data, 2-legged client_credentials for service access) against Autodesk Platform Services (APS, formerly Forge) -- token endpoint https://developer.api.autodesk.com/authentication/v2/token. Integration pattern: follow the google_drive.py block's template exactly (Store's app/blocks/google_drive.py, verified real: httpx.AsyncClient with Bearer token, refresh_token grant against a documented OAuth token URL, base_url-scoped client) -- build an aps_client.py the same shape, hitting the Data Management API (GET /project/v1/hubs, /projects, /folders) for drawing/file listings and the Model Derivative API (the URL already hardcoded in documents.py) for viewable/geometry extraction, replacing the current fabricated digital_twin_sync payload with real calls. First concrete block to build: 'acc_connector' (or fold into a renamed 'digital_twin_sync' once it actually calls the API) that lists ACC/BIM 360 project folders and downloads/derives model data -- feed its output into the existing bim_extractor.py's IFC pipeline so cloud-hosted models get the same quantity/clash extraction currently only available for manually uploaded files.






Primavera P6 EPPM web services
Read app/blocks/primavera_parser.py in full (239 lines, also present verbatim in The_Fork and in block_store/kits/construction/â€¦



Read app/blocks/primavera_parser.py in full (239 lines, also present verbatim in The_Fork and in block_store/kits/construction/bundle/). Confirmed: it opens a local file path (self._resolve_file_path), rejects anything not ending .xer, and parses XER's flat-file table format (_parse_xer, _extract_activities, _extract_wbs, _extract_resources) -- no network I/O, no imports of httpx/requests, no mention of P6 EPPM's actual SOAP/REST web-services API (Primavera's live product exposes a P6 EPPM Web Services SOAP API and, in newer versions, a REST API under /p6ws/) anywhere in the file or its docstring. This is exactly the file-format-parsing-only situation the task description anticipated needed confirming, and it is confirmed. Nothing in the Store, The_Fork, or bim-manager-agent references live P6 EPPM web services.

RecommendationAuth model: P6 EPPM's native web services use SOAP with WS-Security username/password tokens (older deployments) or, in current Oracle Primavera Cloud, OAuth2 client_credentials similar to Aconex's Lobby pattern (Oracle unified several Lobby-based products under the same construction-and-engineering identity platform). Integration pattern: do not extend primavera_parser.py itself (it is correctly scoped as a pure file parser, tags=['domain','construction','schedule','primavera','xer','cpm'] with layer=3 -- mixing live API calls into a Layer-3 domain block would blur the Store's adapter/domain separation). Instead build a new Layer-4 integration block (e.g. 'p6_eppm_connector') mirroring the CdeClient shape used for Aconex, wrapping P6's REST/SOAP endpoints for activities, resource assignments, and WBS, and feed its output through the same _extract_activities/_extract_wbs shape primavera_parser.py already produces so downstream consumers (smart_orchestrator.py, pm_computations.py) don't need to know whether the schedule came from an uploaded .xer or a live P6 pull. First concrete block to build: a thin 'p6_eppm_status'-style block that only proves the OAuth/WS-Security handshake and lists activities read-only, following primavera_parser's output schema, before attempting any write-back (P6 write operations carry real schedule-corruption risk and should stay out of a first cut).







research Â· erp-sap 5




SAP
Nothing real exists anywhere surveyed



Nothing real exists anywhere surveyed. Store (Cerebrum-Blocks) block_store/connector_registry.py is a generic lifecycle registry with zero domain logic ('Domain-specific connectors (ERP, CRM, etc.) extend BaseConnector ... without leaking domain assumptions'); app/blocks/core/base_connector.py is an abstract auth->fetch->normalize->envelope pattern with no SAP code. The closest real-named artifact in the whole survey is TEKsystems_GlobalRetailMNC/backend/app/retailops/integrations/connectors/sap_erp.py, which is an explicit, self-declared 'honest stub': docstring reads 'No SAP/ERP vendor adapter is implemented in this pilot... without real credentials it reports disabled; with credentials present it reports configured but still declines to pull/push, since no vendor client exists yet.' Its pull()/push() literally raise ConnectorError(error_code='not_implemented'). Cerebrum/backend/app/integrations/erp.py defines ERPType.SAP as an enum value but only QuickBooksService is implemented in that file -- no SapService class exists. No OData call, no BAPI/RFC library (e.g. pyrfc), and no SAP API Business Hub reference exist in any of the 20+ donor repos or the Store.

RecommendationBuild against SAP OData v2/v4 via SAP Gateway / SAP API Business Hub (e.g. API_JOURNALENTRYITEMBASIC_SRV for GL, API_PURCHASEORDER_PROCESS_SRV, API_BUSINESS_PARTNER), not BAPI/RFC -- OData is REST/JSON and is what S/4HANA Cloud (the SKU mid-market buyers actually reach) publishes, whereas BAPI/RFC requires the licensed, compiled SAP NetWeaver RFC SDK (pyrfc) and on-prem NetWeaver connectivity, making it the 'exotic' option the brief asked to avoid. Auth: OAuth2 client-credentials against SAP's XSUAA/Cloud Identity service for S/4HANA Cloud, or Basic+CSRF-token for on-prem Gateway. First block to build: sap_odata_connector.py extending the Store's existing app/blocks/core/base_connector.py BaseConnector contract (or mirroring TEKsystems' pull/normalize/push RetailConnector shape), pulling GL journal-entry rows and feeding them straight into the Store's already-real app/blocks/finance_import.py 'gl' source_type field_map (voucher_id, gl_account, cost_centre, posting_date, fiscal_period, currency_code, net_amount -> canonical FinanceOps schema) -- that normalizer already exists and works, only the extraction side is missing.






Oracle NetSuite
Nothing found beyond a bare enum value: Cerebrum/backend/app/integrations/erp.py::ERPType.NETSUITE exists as a string constantâ€¦



Nothing found beyond a bare enum value: Cerebrum/backend/app/integrations/erp.py::ERPType.NETSUITE exists as a string constant with zero corresponding service class, zero OAuth code, and zero mention anywhere else across the Store or the other 15+ donor repos checked. Explicitly a greenfield recommendation.

RecommendationUse SuiteTalk REST Web Services with Token-Based Authentication (OAuth 1.0a TBA: consumer key/secret + token id/secret) against standard record endpoints, rather than RESTlets, which require deploying custom SuiteScript inside each customer's NetSuite account -- a much higher-friction ask for a mid-market integration than TBA against NetSuite's already-published REST record API. First block: netsuite_connector.py pulling account, vendorBill, and journalEntry SuiteTalk REST records and normalizing them into the Store's finance_import.py 'gl' and 'budget' source_types.






Microsoft Dynamics 365
No file anywhere in the survey references Dynamics 365, Business Central, Finance & Operations, or Dataverse by name



No file anywhere in the survey references Dynamics 365, Business Central, Finance & Operations, or Dataverse by name. The nearest adjacent artifact, TEKsystems_GlobalRetailMNC/backend/app/retailops/integrations/connectors/entra_id.py, is itself an admitted stub: 'No SSO federation ... is implemented in this pilot ... it never calls Microsoft Graph or performs a token exchange.' So even the Azure AD auth plumbing a Dynamics connector would reuse does not exist yet -- fully greenfield, though entra_id.py's env-var naming convention (RETAILOPS_ENTRA_TENANT_ID / _CLIENT_ID / _CLIENT_SECRET) is a usable template.

RecommendationTarget Dynamics 365 Business Central specifically (the actual mid-market SKU, as opposed to Finance & Operations, which serves upper-enterprise) via its OData v4 API, authenticating with OAuth2 client-credentials against a Microsoft Entra ID app registration. First block: dynamics_bc_connector.py pulling the generalLedgerEntries and vendors OData v4 entities and normalizing into finance_import.py's 'gl' source_type, following the tenant_id/client_id/client_secret shape entra_id.py already stubs.






QuickBooks
Cerebrum/backend/app/integrations/erp.py::QuickBooksService AND Cerebrum/backend/app/integrations/accounting.py::QuickBooksServâ€¦


Cerebrum · backend/app/integrations/erp.py, backend/app/integrations/accounting.py

Cerebrum/backend/app/integrations/erp.py::QuickBooksService AND Cerebrum/backend/app/integrations/accounting.py::QuickBooksService are two separate, genuinely real implementations: correct Intuit OAuth2 authorization-code flow (appcenter.intuit.com/connect/oauth2 for auth, oauth.platform.intuit.com/oauth2/v1/tokens/bearer for token exchange/refresh), real QBO query-API calls ('SELECT * FROM Account/Vendor/Customer'), and real invoice/bill creation payloads. Both are exported from integrations/__init__.py and imported into api/v1/endpoints/integrations.py. However: (1) the two QuickBooksService classes have diverged (different constructors, one tracks ERPAccountMapping/ERPSyncLog, the other doesn't) -- a clone_candidate needing reconciliation; (2) the live HTTP endpoint POST /integrations/connectors/quickbooks/connect returns a hardcoded placeholder string ('https://appcenter.intuit.com/connect/oauth2?client_id=YOUR_CLIENT_ID') instead of calling either service's real get_auth_url(), so the working service-layer code is currently unreachable end-to-end despite being real underneath. No QuickBooks block exists in the Store's block_registry/ or app/blocks/ at all.

RecommendationDo not rebuild. Reconcile the two QuickBooksService implementations into one, fix api/v1/endpoints/integrations.py's connect_connector handler to call the real get_auth_url()/handle_auth_callback() methods instead of the placeholder string, then promote the consolidated service as a certified Store block (accounting_connector, provider=quickbooks) -- the OAuth2 authorization-code + refresh-token pattern it already implements is the correct model and is production-grade, it just needs de-duplication and to be actually wired to the route that calls it.






Xero
Cerebrum/backend/app/integrations/accounting.py::XeroService is a genuine implementation: correct identity.xero.com/connect/tokâ€¦


Cerebrum · backend/app/integrations/accounting.py

Cerebrum/backend/app/integrations/accounting.py::XeroService is a genuine implementation: correct identity.xero.com/connect/token OAuth2 endpoint, correct api.xero.com/api.xro/2.0 base URL with the required Xero-tenant-id header, real token refresh, real invoice creation and chart-of-accounts retrieval, and it is wired through AccountingService.get_service('xero'). But api/v1/endpoints/integrations.py's GET /connectors list and POST /connectors/{id}/connect handler only enumerate procore, slack, quickbooks, docusign, microsoft365, salesforce, and box -- there is no 'xero' entry or branch anywhere in that 498-line endpoints file, so a real, working Xero connector is completely unreachable from the product's own API despite the backend code being correct.

RecommendationNo new build needed for the integration logic itself -- add the missing 'xero' entry to the /connectors list and a connect_connector branch that calls XeroService's real OAuth flow (mirroring the quickbooks fix above), then promote XeroService alongside the reconciled QuickBooks service as a second provider variant of a Store accounting_connector block.







research Â· hospitality-pms 3




Oracle Hospitality Opera / OHIP
cerebrum-hotelops/connectors/opera.py:14-20 -- OperaConnector.live_headers() sends {'X-Client-Id':..., 'X-Client-Secret':...} aâ€¦


bopoadz-del/cerebrum-hotelops and bopoadz-del/cerebrum-hotelops-v2 · cerebrum-hotelops/connectors/opera.py; cerebrum-hotelops/connectors/base.py; cerebrum-hotelops/KNOWN_LIMITATIONS.md; cerebrum-hotelops-v2/connectors/opera.py; cerebrum-hotelops-v2/connectors/base.py; cerebrum-hotelops-v2/KNOWN_LIMITATIONS.md

cerebrum-hotelops/connectors/opera.py:14-20 -- OperaConnector.live_headers() sends {'X-Client-Id':..., 'X-Client-Secret':...} as plain custom headers. Real Oracle OHIP requires OAuth2 client_credentials grant (POST to a token endpoint) producing a Bearer token, PLUS mandatory x-hotelid and x-app-key headers on every call -- none of that exists here. base.py's fetch_live() (lines 63-67) does a bare httpx.get with those wrong headers, so this code would 401 against any real OHIP tenant. tests/test_connectors.py:9-15 (test_opera_normalises_and_emits_guest_events) only asserts result['mode']=='fixture' -- the live path has never been run against anything. hotelops/settings.py:67-70 live_enabled() correctly gates on OPERA_BASE_URL/OPERA_CLIENT_ID/OPERA_CLIENT_SECRET all being set and fixture_mode being off, but nothing in either repo ever sets them (checked .env.example in both -- vars are listed empty/commented). cerebrum-hotelops-v2/connectors/opera.py explicitly regressed further: mock_level=MOCK_UNAVAILABLE, connect() raises ConnectorError unconditionally, fetch() returns a fail-closed 'refusal' envelope with data:[] and never attempts HTTP -- the docstring says 'no live system in this deployment... only connect/fetch change' when a live integration is added. KNOWN_LIMITATIONS.md in both repos documents this as an intentional gate, not a hidden stub (v1: 'Live Opera Cloud / OHIP -- requires OPERA_BASE_URL...'; v2: 'No live connector integrations... Opera... All are honest mock_unavailable adapters'). The Store's block_registry (per already-verified facts) has NO 'opera' or 'pms' block at all -- this connector lives only inside the product repos, unvendored, so it isn't even a candidate for promotion to a certified Store block yet.

RecommendationAuth model: implement real OHIP OAuth2 client_credentials (token endpoint call, cache/refresh Bearer token) plus mandatory x-hotelid and x-app-key headers on every resource call -- the current X-Client-Id/X-Client-Secret headers must be replaced, not extended. Integration pattern: keep the existing connect->fetch->normalise->emit shape from cerebrum-hotelops/connectors/base.py (it's the more capable of the two -- v1 at least has a working live path scaffold, unlike v2's permanently-disabled fail-closed version) but fix live_headers()/live_url() to match the real OHIP REST contract, and add a token-refresh layer. First concrete block to build: extract this into a genuinely certified Store block, e.g. block_registry/opera_ohip_connector wrapping a new app/blocks/opera_ohip_connector.py that implements real OAuth2 token exchange + the reservations/in_house/housekeeping resource fetches already normalised in cerebrum-hotelops/connectors/opera.py -- reusing its normalise() logic (lines 22-53) which is legitimate mapping code, just missing a correct live transport underneath it.






Cloudbeds
Nothing found -- greenfield recommendation, stated explicitly


bopoadz-del/cerebrum-hotelops and bopoadz-del/cerebrum-hotelops-v2

Nothing found -- greenfield recommendation, stated explicitly. Case-insensitive grep for 'cloudbeds' across both cerebrum-hotelops and cerebrum-hotelops-v2 (excluding .git) returned zero hits in code, docs, fixtures, or env examples.

RecommendationAuth model: Cloudbeds API is OAuth2 authorization-code (not client-credentials) with per-property scoped access tokens and refresh tokens -- this needs a token-storage/refresh component the current connector base classes don't have (both cerebrum-hotelops and -v2 base.py assume a single static client secret, not a user-consent OAuth flow). Integration pattern: same normalise-to-HotelEvent shape as the Opera connector (reservations/in_house/housekeeping topics already defined in cerebrum-hotelops/connectors/opera.py can be reused as the target canonical schema) but fetch/connect need an OAuth2-authorization-code adapter, not the existing client-credentials-shaped BaseConnector.live_headers(). First concrete block to build: a new connectors/cloudbeds.py (or a certified Store block app/blocks/cloudbeds_connector.py) implementing the OAuth2 authorization-code + refresh-token dance against Cloudbeds' /api/v1.2/getReservations and /api/v1.2/getHotelDetails endpoints, normalising into the same guest.booking.reservation / ops.pms.in_house topics the Opera connector already emits -- this reuse of the canonical event taxonomy is the main leverage point.






Mews
Nothing found -- greenfield recommendation, stated explicitly


bopoadz-del/cerebrum-hotelops and bopoadz-del/cerebrum-hotelops-v2

Nothing found -- greenfield recommendation, stated explicitly. Case-insensitive grep for 'mews' across both repos (excluding .git) returned zero hits. Confirmed the existing connector architecture (BaseConnector.fetch() in both base.py files) is pull/poll-only -- there is no inbound-webhook receiver route anywhere in api/routes/ of either repo (checked cerebrum-hotelops/api/routes/ and cerebrum-hotelops-v2/api/routes/ directory listings: actions.py, documents.py, engineering.py, guest.py, licensing.py, operational.py, pre_opening.py -- no webhook/inbound endpoint file in either).

RecommendationAuth model: Mews Connector API uses a simple static AccessToken + ClientToken pair in the JSON request body (not OAuth2, not headers) -- simpler than Opera or Cloudbeds, but the request pattern (POST with token pair in body to endpoints like /api/connector/v1/reservations/getAll) doesn't fit the existing live_url()/live_headers() GET-with-headers shape in BaseConnector at all, so this needs its own request builder rather than reusing live_headers(). Integration pattern: given Mews' native webhook push model, pair a thin polling connector (for initial backfill, reusing the connect->fetch->normalise->emit shape) with a new inbound webhook route (api/routes/pms_webhook.py) that receives Mews' event notifications directly onto the same event_bus HotelEvent topics -- this is the one PMS of the three where the product's own event-bus architecture is a genuine structural advantage. First concrete block to build: a webhook-receiving endpoint plus connectors/mews.py normaliser mapping Mews reservation/service-order events onto the existing guest.booking.reservation / ops.pms.in_house / ops.pms.housekeeping topics already defined in cerebrum-hotelops/connectors/opera.py, so all three PMS connectors converge on one canonical schema the rest of hotelops (guest_intelligence, reasoning/market_router.py) already consumes.







research Â· crm-collaboration 5




Salesforce
Store (Cerebrum-Blocks, HEAD e3696bf): grep -rli 'salesforce' across block_registry/ and app/blocks/ returns nothing â€” greenfieâ€¦


bopoadz-del/Cerebrum · backend/app/integrations/crm.py

Store (Cerebrum-Blocks, HEAD e3696bf): grep -rli 'salesforce' across block_registry/ and app/blocks/ returns nothing â€” greenfield, confirmed. Donor repo bopoadz-del/Cerebrum has a real, substantive SalesforceService in backend/app/integrations/crm.py (385 lines): OAuth2 authorization-code + refresh_token flow against https://login.salesforce.com/services/oauth2/token, SOQL queries via /services/data/v58.0/query, create_lead(), create_opportunity(), get_accounts(), and a construction-specific sync_project_to_opportunity() that upserts a Salesforce Opportunity keyed on a custom Project_ID__c field. It is not dead code: backend/app/api/v1/api.py:388 mounts the router that imports CRMService, and Cerebrum/docs/BACKEND_API_AUDIT.md lists login.salesforce.com in its external-hosts table, i.e. this was audited as a live outbound dependency. Contrast: InsureOps/backend/app/insureops/integrations/service.py also defines an 'sfdc' connector, but its own _CONNECTOR_META dict labels it description: 'Simulated Salesforce producer / opportunity feed' â€” an honestly-labeled mock bordereaux generator with no OAuth or real API call, not a real integration.

RecommendationPort Cerebrum's SalesforceService into a Store block, not InsureOps's mock. Auth model: OAuth2 authorization-code grant with refresh_token, credentials stored per-tenant (Cerebrum's CRMConnection table is the schema to follow). First concrete block: crm_salesforce.py implementing action=create_lead / create_opportunity / sync_project_to_opportunity as a layer-5 integration block, registered with the same 40-line adapter pattern as every other Store block.






HubSpot
Store: grep -rli 'hubspot' returns nothing â€” greenfield


bopoadz-del/Cerebrum · backend/app/integrations/crm.py

Store: grep -rli 'hubspot' returns nothing â€” greenfield. Donor repo Cerebrum's crm.py also contains HubSpotService (same file as Salesforce): create_contact(), create_deal(), get_pipelines(), sync_project_to_deal(). Caveat found by reading the code, not assumed: it targets HubSpot's legacy v1 REST endpoints (/contacts/v1/contact, /deals/v1/deal, /crm-pipelines/v1/pipelines/deals), which HubSpot has since deprecated in favor of the v3 CRM API â€” so this is real, non-trivial logic but not a drop-in port; it needs an API-version upgrade during extraction.

RecommendationPort the same file's HubSpotService but rewrite the endpoint calls against HubSpot's current v3 CRM API (/crm/v3/objects/contacts, /crm/v3/objects/deals) and prefer a private-app token or OAuth2 (v3 supports both) over the old v1 auth model. First concrete block: crm_hubspot.py with create_contact / create_deal / sync_project_to_deal actions, sibling to crm_salesforce.py so a single 'crm' family shares one connection-credential shape.






Slack
Read app/blocks/notification.py (Store, real code): _send_slack() only POSTs text/blocks to a single preconfigured Incoming Webâ€¦


bopoadz-del/Cerebrum · backend/app/integrations/slack.py

Read app/blocks/notification.py (Store, real code): _send_slack() only POSTs text/blocks to a single preconfigured Incoming Webhook URL (SLACK_WEBHOOK_URL) â€” one-way, no OAuth, no per-tenant workspace. Read app/blocks/mcp_consumer.py (Store, unregistered but real, 85 lines): generic stdio MCP client that could in principle call an external Slack MCP server, but that's generic infra, not Slack-specific logic. Donor repo Cerebrum has a full SlackService in backend/app/integrations/slack.py (569 lines): Slack OAuth v2 install flow (oauth.v2.access), HMAC-SHA256 inbound request-signature verification using hmac.compare_digest (constant-time, correctly implemented) plus a 5-minute replay-timestamp check, slash-command dispatch (/cerebrum task|project|status|help), and a SlackChannelMapping table that routes project events (task_created, task_completed, document_uploaded, rfi_created) to specific channels via notify_project_event(). This is materially more capable than what's in the Store today and is wired into the live integrations router (api/v1/api.py:388).

RecommendationThe right extension point is a new stateful Slack connector block, not an extension of notification.py â€” notification.py is a fire-and-forget dispatcher with no token storage or inbound routes, which OAuth app + slash commands require. Build crm_slack.py (or collab_slack.py) from Cerebrum's SlackService: OAuth v2 bot-token per tenant, signing-secret verification for the inbound slash-command route, and channel-mapping-driven event routing. First concrete action to ship: send_notification (already-working code, lowest risk) before layering slash commands.






Microsoft Teams
Store: grep -rli 'teams|microsoft.?365|graph.microsoft' across app/ and block_registry/ returns nothing at all â€” fully greenfieâ€¦


bopoadz-del/Cerebrum · backend/app/integrations/microsoft_365.py

Store: grep -rli 'teams|microsoft.?365|graph.microsoft' across app/ and block_registry/ returns nothing at all â€” fully greenfield, more so than Slack (Store has no webhook-only fallback for Teams the way it does for Slack). Donor repo Cerebrum has Microsoft365Service in backend/app/integrations/microsoft_365.py (354 lines): OAuth2 client-credential/refresh flow against Microsoft Graph, create_teams_meeting(), send_teams_message(), create_sharepoint_folder()/upload_sharepoint_file(), create_outlook_event(), and construction-flavored sync_project_to_teams() (creates a Teams channel per project) plus send_project_notification(). Same live router mount as the CRM/Slack/DocuSign services; Cerebrum/docs/BACKEND_API_AUDIT.md lists https://graph.microsoft.com/v1.0 in its external-hosts table, confirming this was treated as a real outbound dependency, not aspirational code.

RecommendationBuild collab_msteams.py as a new Store block modeled directly on Microsoft365Service. Auth model: OAuth2 against Microsoft Graph (app registration, client_id/secret, delegated or app-only scopes such as ChannelMessage.Send and OnlineMeetings.ReadWrite depending on whether messages are sent as the app or as a user). First concrete block/action: send_teams_message â€” it's the direct Teams analogue of what notification.py already does for Slack/email/webhook, so it slots into the existing notification-channel mental model even though it needs its own OAuth token store.






DocuSign
Store: grep -rli 'docusign|esignature|hellosign|adobesign|pandadoc' across app/ returns nothing; ls app/blocks/ | grep -i 'signâ€¦


bopoadz-del/Cerebrum · backend/app/integrations/esignature.py

Store: grep -rli 'docusign|esignature|hellosign|adobesign|pandadoc' across app/ returns nothing; ls app/blocks/ | grep -i 'sign|approv|contract' also returns nothing. Donor repo Cerebrum has a real, wired-in DocuSignService in backend/app/integrations/esignature.py (431 lines, explicitly tagged 'Item 312: E-signature integrations'): OAuth2 code exchange against https://account-d.docusign.com (DocuSign's demo/sandbox auth host â€” would need swapping for the production host or a JWT grant before go-live), create_envelope() (builds a documents+recipients+routingOrder envelope and POSTs to /restapi/v2.1/accounts/{id}/envelopes), get_envelope_status(), void_envelope(), and handle_webhook() which updates envelope status from DocuSign's Connect webhook callbacks and writes a SignatureEventLog audit row per event â€” a genuine (not vacuous) status-tracking table, distinct from the fake always-'ok' audit blocks this survey has flagged elsewhere. IMPORTANT GAP found by reading the code: _download_and_encode(), the method meant to fetch the actual PDF and base64-encode it for the envelope's documentBase64 field, is unimplemented â€” it returns '' with the comment 'In production, download from S3 or storage. For now, return placeholder.' As written, create_envelope() would send DocuSign an envelope with an empty document body.

RecommendationPort as esignature_docusign.py, but treat the placeholder-document gap as the actual first task, not an afterthought: wire _download_and_encode to the Store's real document storage (document_engine_block.py's file path, or whatever file_storage.py analog exists) before this connector is usable at all â€” a straight copy-paste would silently produce broken envelopes. Auth model: for production use DocuSign's JWT Grant (server-to-server, no per-user OAuth dance) instead of the demo authorization-code flow this donor code uses. First concrete block/action once the document-fetch gap is closed: create_envelope for a single contract/change-order document type.







research Â· hr-supply-chain 2




Workday / BambooHR (HR/HRIS systems, generically)
store_ref/app/blocks/hr_v2.py (615 lines) docstring states explicitly: 'Uses deterministic regex + math, no LLM calls, no exterâ€¦



store_ref/app/blocks/hr_v2.py (615 lines) docstring states explicitly: 'Uses deterministic regex + math, no LLM calls, no external APIs' -- it is a pure text-analysis block (accepts TextContent/PDFContent, outputs HRAnalysis via regex over pasted document text). It has zero HTTP client code, zero auth, zero vendor SDK. The block_store/kits/hr/ bundle (manifest.json) vendors only ['pdf','ocr','chat','image','hr_v2','formula_executor_v2'] -- no connector block at all, unlike the finance kit (market_data_connector.py, sec_edgar_connector.py), medical kit (medical_ehr_connector.py, real/registered), and hotel_management kit (opera_connector.py, honest stub). Grepped all 15 assigned donor repos (Cerebrum, Cerebrum-FinanceOps, Cerebrum-Steward, InsureOps, Me-Agent, StockWisePro, TEKsystems_GlobalRetailMNC, The_Fork, The_Level, ThreadForge, bim-manager-agent, cerebrum-hotelops, cerebrum-hotelops-v2, stockwise-whatsapp, stockwisepro-bot, teamhub-test-platform) for Workday/BambooHR/Coupa/SAP Ariba/ADP/Gusto/Rippling/Greenhouse/Lever/iCIMS/SuccessFactors/UKG/UltiPro/etc: every apparent hit was a false positive on substring collision (e.g. 'ultipro' inside 'multiprocessing', 'WORKDAY' inside DEFAULT_HOURS_PER_WORKDAY calendar constants, 'Workday Inc.' as a NASDAQ stock-universe ticker entry in stockwisepro-bot/data/stock_universe.json, 'hr_connector' inside 'medical_ehr_connector'). Stated explicitly: nothing found, greenfield recommendation.

RecommendationAuth model: OAuth2 client-credentials (Workday RaaS/SOAP or REST API uses OAuth2 + tenant-scoped endpoint; BambooHR uses a simple API-key-as-basic-auth model) -- store per-tenant credentials the same way Cerebrum/backend/app/integrations/erp.py's ERPConnection SQLAlchemy model does (base_url, api_key/api_secret, refresh_token, token_expires_at, is_connected, last_error), which is a real, working OAuth+refresh pattern already proven in the Cerebrum donor repo (QuickBooksService: authorization-code exchange, token refresh, 200/error handling via httpx-style requests calls -- not a stub). Integration pattern: subclass app/blocks/core/base_connector.py's BaseConnector (auth -> fetch_raw -> normalize -> ConnectorEvent envelope), following the already-registered medical_ehr_connector.py as the structural template (real httpx client, env-based default_config, _RESOURCE_MODELS dispatch table) rather than the honest-stub opera_connector.py pattern. First concrete block to build: 'bamboohr_connector' (simpler REST+API-key auth than Workday's SOAP/OAuth complexity) fetching /v1/employees/directory and /v1/time_off/requests, normalized into a new HREvent/Employee schema in app/core/connector_events.py or hr_types.py, registered in block_registry/ alongside the existing medical_ehr_connector precedent, and wired into the existing hr_v2 kit's HRContainer as an optional live-data source feeding hr_v2's regex-based analysis instead of only pasted text.






SAP Ariba / Coupa (procurement/supply-chain systems beyond Procore)
store_ref/app/blocks/supply_chain_v2.py (same file family as hr_v2.py) docstring: 'Uses deterministic regex + math, no LLM callâ€¦



store_ref/app/blocks/supply_chain_v2.py (same file family as hr_v2.py) docstring: 'Uses deterministic regex + math, no LLM calls, no external APIs' -- also pure text analysis, zero connectivity. block_store/kits/supply_chain/manifest.json vendors no connector block either. No 'Procore' string appears anywhere in the Store repo (contrary to the task brief's premise that Procore integration already exists -- it does not, at least not under that name). HOWEVER: TEKsystems_GlobalRetailMNC/backend/app/retailops/integrations/connectors/supplier_edi.py (121 lines, real, read) is an explicitly-labeled 'Mock-functional' connector for 'Ariba / EDI 850/856/810 / Portal' with a working RetailConnector/GenericRestMixin base class: it reports ConnectorStatus.MOCK when RETAILOPS_EDI_BASE_URL/RETAILOPS_EDI_API_KEY env vars are unset, and genuinely attempts a live REST call (rest_get_json) when they are set -- no fabrication either way. The same repo's sap_erp.py (94 lines) is an 'Honest stub' that even declines to fake data when credentials ARE present ('Credentials detected, but a real SAP/ERP adapter is not implemented ... No connection was attempted'), and inventory_wms.py covers Manhattan/Blue Yonder/RELEX WMS in the same style. Separately, Cerebrum/backend/app/integrations/erp.py (449 lines) defines an ERPType enum including SAP/NETSUITE/DYNAMICS but only implements QuickBooksService concretely (real OAuth2 code-exchange + token refresh + live API calls) -- it does not touch Ariba/Coupa, confirming procurement-SaaS auth is unimplemented Store-wide and product-wide.

RecommendationAuth model: Ariba uses SAP's OAuth2 client-credentials against the SAP Business Network / Ariba Network API gateway; Coupa uses OAuth2 client-credentials against a tenant-specific *.coupahost.com instance. Both fit the same ERPConnection-style credential model already proven in Cerebrum/backend/app/integrations/erp.py. Integration pattern: do not design from scratch -- port TEKsystems_GlobalRetailMNC's RetailConnector/GenericRestMixin abstraction (status()/badges()/health()/test_connection()/pull()/normalize()/push() with explicit ConnectorStatus.MOCK vs CONFIGURED vs DISABLED states and read_only + mock_functional flags) into app/blocks/core/base_connector.py's BaseConnector, since the Store's current BaseConnector only has a binary authenticate()/fetch_raw() shape and lacks this honest three-state maturity signaling that TEKsystems already uses in production-pilot code. First concrete block to build: 'ariba_edi_connector' modeled directly on supplier_edi.py -- normalize purchase-order/ASN data (promised vs actual delivery, short/damaged/rejected quantities) into the existing SupplyChainAnalysis output schema in app/core/supply_chain_types.py, ship it with the same mock/live dual-mode (RETAILOPS-style env vars renamed to SUPPLYCHAIN_EDI_*) so it is honestly labeled Mock-functional rather than claiming a live Ariba adapter that doesn't exist yet.







research Â· mcp-pattern-fit 7




Oracle Aconex (CDE / document & correspondence system of record)
Read in full: thefork_survey/app/core/cde/protocol.py (abstract CdeClient with list_mail/list_documents/list_rfis/post_mail), aâ€¦



Read in full: thefork_survey/app/core/cde/protocol.py (abstract CdeClient with list_mail/list_documents/list_rfis/post_mail), aconex.py (339 lines, real httpx calls to documented Oracle endpoints api.aconex.com, OAuth2 lobby token exchange, XML mail construction with proper escaping), poll.py (poll_cde_events walks the client, dedupes, feeds a CM overlay), config.py (fail-closed unless ACONEX_ENABLED + credentials present), and connectors.py router (HTTP surface: GET/POST /v1/projects/{id}/connectors, sync and RFI-post endpoints). protocol.py's own docstring says: 'Vendor-neutral CDE client. Procore / ACC can implement the same door later.' This is not agent-facing tool access -- it's scheduled/on-demand batch sync of a document/correspondence register feeding a local RAG cache and CM (change-management) overlay.

RecommendationKeep as a plain OAuth2 + polling Store block (promote app/core/cde/* out of The_Fork into a Store-level 'cde_connector' block, matching the existing Store OAuth pattern already used by onedrive.py/google_drive.py). Do NOT wrap it as an MCP server -- an LLM agent polling a mail/register endpoint on every turn is wasteful and loses the CM-overlay/dedup logic that only makes sense as a background job. First concrete block: extract AconexCdeClient + the CdeClient ABC into block_registry/cde_connector/ so hotelops/other verticals can reuse the same vendor-neutral door.






Procore
Cerebrum/backend/app/integrations/procore.py (496 lines): genuine OAuth2 authorization-code flow with real HTTP calls (requestsâ€¦



Cerebrum/backend/app/integrations/procore.py (496 lines): genuine OAuth2 authorization-code flow with real HTTP calls (requests.post to https://login.procore.com/oauth/token, requests.get to https://api.procore.com/rest/v1.0/me and /companies), SQLAlchemy models for ProcoreConnection/ProcoreProjectMapping, sync_projects()/sync_project_data() methods -- this is real, not a stub. Separately, Cerebrum/backend/app/stubs/procore.py is an explicit, clearly-labeled in-memory mock ('Stub for Procore API... mock data') used for local/offline dev -- the two coexist honestly (real client + labeled mock), not a fake-gate situation. The_Fork's own CDE docstring explicitly flags Procore as the next vendor to slot into the same abstraction ('Procore / ACC can implement the same door later').

RecommendationPlain Store block with OAuth2 authorization-code flow + scheduled/on-demand REST sync, NOT an MCP server. Concrete first block: port Cerebrum's integrations/procore.py into a ProcoreCdeClient(CdeClient) implementing The_Fork's existing protocol.py interface (list_documents/list_mail/list_rfis/post_mail map directly onto Procore's Drawings/Correspondence/RFI endpoints) so construction verticals get Aconex and Procore behind one interchangeable door, exactly as the docstring anticipates.






SAP / generic ERP (S/4HANA, ECC, NetSuite, Sage, Viewpoint, QuickBooks, Dynamics)
Two different maturity levels found: (1) retail_cluster_agent/teksystems_retail (and its duplicate teksystems_agent/TEKsystems_â€¦



Two different maturity levels found: (1) retail_cluster_agent/teksystems_retail (and its duplicate teksystems_agent/TEKsystems_GlobalRetailMNC) backend/app/retailops/integrations/connectors/sap_erp.py is an explicitly-labeled 'honest stub' -- read_only=True, mock_functional=False, test_connection() always returns ok:false with a message stating no vendor adapter exists; it never fabricates financial data. (2) Cerebrum/backend/app/integrations/erp.py (449 lines) is further along -- real requests.post/get calls (5 call sites) against a generically-modeled ERPConnection/ERPType enum (Sage/Viewpoint/QuickBooks/SAP/NetSuite/Dynamics) with sync_accounts(), but is NOT vendor-specific in the way Procore's client is (no hard-coded SAP OData/BAPI endpoints), so it reads as a generic scaffold rather than a working SAP client. No live SAP OData/IDoc integration exists anywhere in the surveyed repos.

RecommendationScheduled ETL Store block per vendor family, using OAuth2 client-credentials or API-key auth against each ERP's REST/OData layer (SAP: OData/ODP services; QuickBooks/NetSuite: their REST APIs), landing normalized GL/PO/vendor-master rows on a schedule (nightly/hourly), following the same fail-closed honesty pattern already set by sap_erp.py's stub (report 'not configured', never invent numbers). First concrete block: build a real SapODataConnector against S/4HANA's public OData API (read-only: GL line items + PO headers) to replace the current honest-stub, reusing Cerebrum's ERPConnection OAuth model as the credential store.






IBM Maximo (CMMS / facilities asset management -- hotel_v2 vertical)
cerebrum_hotelops_survey/cerebrum-hotelops/connectors/maximo.py (58 lines) is real: live_url()/live_headers() call {maximo_baseâ€¦



cerebrum_hotelops_survey/cerebrum-hotelops/connectors/maximo.py (58 lines) is real: live_url()/live_headers() call {maximo_base_url}/os/{resource} with an apikey header, normalise() parses asset records into HotelEvent objects on the hotelops event bus, and explicitly guards scope by rejecting records whose source_system is aconex/procore/bim ('Asset {id} source {src} is out of scope') -- a genuine cross-connector boundary check, not a rubber-stamp.

RecommendationLeave as-is: plain API-key/OAuth Store connector feeding an internal event bus. If real-time asset alerts are later needed, add Maximo's own outbound webhook/event notification as a receiver (see webhook.py gap below) rather than polling via MCP.






Primavera P6 / Primavera Cloud (EPPM)
Cerebrum-Blocks app/blocks/primavera_parser.py (239 lines): real XER table parser (critical path, milestones, resource loading)â€¦



Cerebrum-Blocks app/blocks/primavera_parser.py (239 lines): real XER table parser (critical path, milestones, resource loading) but strictly file-upload-driven (ui_schema input type 'file', .xer only) -- it never talks to Oracle Primavera Cloud's live REST API. No live Primavera Cloud connector found in any surveyed repo.

RecommendationKeep primavera_parser.py as the batch file-ingest Store block it already is. If/when a live Primavera Cloud sync is needed (for orgs that don't export .xer manually), build it as another OAuth2 + REST-polling ETL block following the exact same CdeClient-style interface as Aconex/Procore -- P6 EPPM's API exposes bulk schedule/resource data, the same shape as document registers, not discrete agent-tool calls.






Google Drive / OneDrive / local filesystem / Slack / Microsoft Teams (agent-facing document & chat access)
Confirmed from connector-infra cluster + my own read of mcp_consumer.py and mcp_server.py: mcp_consumer.py is a real MCP CLIENTâ€¦



Confirmed from connector-infra cluster + my own read of mcp_consumer.py and mcp_server.py: mcp_consumer.py is a real MCP CLIENT (official mcp SDK, spawns npx @modelcontextprotocol/server-<name> over stdio) so Cerebrum agents can already reach any official MCP server (Google Drive, Slack, etc.) on demand. Separately, app/mcp_server.py (391 lines, read in full) is a real, generic MCP SERVER that auto-exposes every block in BLOCK_REGISTRY (including google_drive.py, onedrive.py, local_drive.py) as MCP tools over stdio or SSE, with a working cerebrum-chain tool for multi-step agent calls. So the Store already supports both directions: consuming external MCP tool servers AND exposing its own OAuth blocks as MCP tools.

RecommendationFor these systems, keep the existing plain OAuth blocks (google_drive.py, onedrive.py, local_drive.py) for bulk/scheduled operations, but their MCP exposure via app/mcp_server.py is already correct and needs no new block -- just document that agent-facing Drive/OneDrive/local-file access should go through mcp_server.py's tool interface while bulk sync/backup jobs call the blocks directly. For Slack/Teams (not found implemented anywhere in the surveyed repos), the right pattern is to let mcp_consumer.py spawn the official @modelcontextprotocol/server-slack (or Microsoft's Teams MCP server) rather than building a bespoke Slack/Teams block -- this is exactly the ad hoc, agent-driven case MCP was designed for, and no existing partial reference was found so this is a greenfield recommendation (add an allowlist entry per the connector-infra cluster's hardening gap before wiring it to untrusted input).






Real-time push events (Procore/Slack/GitHub/Maximo webhooks) -- inbound half
Per the connector-infra cluster's own finding (confirmed by summary provided): webhook.py (197 lines) has real SSRF guard, HMACâ€¦



Per the connector-infra cluster's own finding (confirmed by summary provided): webhook.py (197 lines) has real SSRF guard, HMAC signing, and aiohttp retry/backoff for OUTBOUND webhook delivery, but has no inbound-receive endpoint to accept webhooks FROM Procore/Slack/GitHub/Maximo. No inbound webhook receiver was found anywhere else in the surveyed repos either.

RecommendationBuild a generic inbound webhook-receiver Store block (HMAC/signature verification keyed per source, e.g. Procore's X-Procore-Signature, GitHub's X-Hub-Signature-256, Slack's X-Slack-Signature) that normalizes verified payloads onto the same event bus pattern already used by maximo.py's HotelEvent, so Procore/Slack/Maximo can all push real-time events into Cerebrum without polling. This complements, not replaces, the OAuth+ETL blocks recommended above for bulk sync of the same systems.






Also checked â€” nothing to report


- repo Â· Cerebrum-FinanceOps grep across all .py/.ts/.tsx/.md files for Procore/Aconex/SAP/Opera/Cloudbeds/Mews/Salesforce/DocuSign/Primavera/Autodesk/BIM 360/QuickBooks/Workday/Slack/NetSuite/Xero/Stripe/Plaid/ERP found zero named-vendor hits anywh


- repo Â· The_Level Searched the whole repo for Procore/Aconex/SAP/Opera/Cloudbeds/Mews/Salesforce/DocuSign/Primavera/BIM 360/Autodesk/QuickBooks/Workday/Slack (case-insensitive, .py/.md/.yaml) -- the only hit was scripts/scan_secrets.py:38







Methodology

82 agents: 16 repo scouts, 12 Store-block cluster audits, 6 connector-research passes, then an independent verify agent re-checked every raw finding against the actual source before it counted — 167 confirmed, 14 refuted or downgraded on re-check. Nothing here was cloned, merged, or pushed; this is survey output only.







