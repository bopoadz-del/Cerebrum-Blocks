# FastAPI & Server (lightweight for edge)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.25.0

# Image Processing
pillow>=10.1.0

# Vector Search - ZVec for ARM64 (Alibaba lightweight)
zvec>=0.1.0
chromadb-client>=0.4.18
sentence-transformers>=2.2.2
numpy>=1.24.0

# Local LLM via Ollama (no heavy transformers)
# OCR - PaddleOCR (ARM64 optimized)
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# PDF Processing
pymupdf>=1.23.0

# No cloud-only dependencies (OpenAI, Anthropic, etc.)
