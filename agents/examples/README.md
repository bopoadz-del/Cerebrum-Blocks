# Examples (test shape templates)

Not product demos. These files define the **required shape** of tests that
`test-writer` must emit:

- `contract_test_example.py` — Pillar A Point 4  
- `seam_test_example.py` — Pillar A Point 5 (keystone)

They use fictional mini-blocks only.
